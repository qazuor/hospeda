# Since pnpm v11, only auth/registry settings are read from .npmrc; every
# other setting (like engine-strict) must live in pnpm-workspace.yaml under
# its camelCase name. See `engineStrict` there — kept explicit (default is
# already `false`, but pnpm 11 silently ignores this file for that key).
