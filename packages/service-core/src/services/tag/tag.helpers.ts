/**
 * @fileoverview Helper functions for Tag operations.
 *
 * Per SPEC-086 D-002 and D-018:
 * - `slug` column is removed from the user-tag system. No slug generation needed.
 * - Cross-type name collision check is enforced at service layer (D-018).
 */
