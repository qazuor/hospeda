/**
 * External listing platform enum — identifies the third-party platform
 * where an accommodation has a public listing.
 *
 * - GOOGLE: Google Maps / Google Travel listing
 * - BOOKING: Booking.com listing
 * - AIRBNB: Airbnb listing
 * - OTHER: Any other platform not explicitly listed above
 */
export enum ExternalPlatformEnum {
    GOOGLE = 'GOOGLE',
    BOOKING = 'BOOKING',
    AIRBNB = 'AIRBNB',
    OTHER = 'OTHER'
}
