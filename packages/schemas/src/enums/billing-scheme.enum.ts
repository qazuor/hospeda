/**
 * Billing scheme enum for pricing plans
 * Defines how products are billed to customers
 */
export enum BillingSchemeEnum {
    /** One-time payment billing */
    ONE_TIME = 'one_time',
    /** Recurring subscription billing */
    RECURRING = 'recurring'
}
