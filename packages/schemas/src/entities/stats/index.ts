/**
 * Stats Entity Schemas
 *
 * Aggregate read-only schemas surfacing platform-wide counters and metrics
 * to public consumers (web footer trust signals, marketing landing pages).
 */

export * from './public-stats-summary.schema.js';
