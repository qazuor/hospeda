/**
 * AI artifact storage module.
 *
 * Persists conversation history, generated embeddings, cached completions, and
 * long-running session state so the engine can resume across HTTP requests.
 *
 * @module ai-core/storage
 */
