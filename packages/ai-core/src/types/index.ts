/**
 * Shared AI type definitions.
 *
 * Provider-agnostic interfaces and discriminated unions shared across all
 * ai-core sub-modules: messages, tool calls, stream chunks, usage records, etc.
 *
 * @module ai-core/types
 */
