/**
 * AI execution engine module.
 *
 * Orchestrates prompt construction, provider dispatch, streaming, retries,
 * and structured output parsing across all capabilities.
 *
 * @module ai-core/engine
 */
