/**
 * AI configuration module.
 *
 * Env-validated configuration, provider credentials, model selection defaults,
 * and feature flags consumed by all other ai-core sub-modules.
 *
 * @module ai-core/config
 */
