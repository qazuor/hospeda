import { CheckCircle } from '@phosphor-icons/react';
import { createPhosphorIcon } from '../../create-phosphor-icon';

export const AvailableBookingIcon = createPhosphorIcon(CheckCircle, 'available-booking');
