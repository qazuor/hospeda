import { GearSix } from '@phosphor-icons/react';
import { createPhosphorIcon } from '../../create-phosphor-icon';

/**
 * ConfigurationIcon icon component
 *
 * @example
 * ```tsx
 * import { ConfigurationIcon } from '@repo/icons';
 *
 * // Basic usage
 * <ConfigurationIcon />
 *
 * // With custom size and color
 * <ConfigurationIcon size="lg" color="#3B82F6" />
 *
 * // With Tailwind classes
 * <ConfigurationIcon className="text-blue-500 hover:text-blue-600" />
 * ```
 */
export const ConfigurationIcon = createPhosphorIcon(GearSix, 'configuration');
