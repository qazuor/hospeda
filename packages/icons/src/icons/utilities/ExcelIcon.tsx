import { FileXls } from '@phosphor-icons/react';
import { createPhosphorIcon } from '../../create-phosphor-icon';

/**
 * ExcelIcon icon component
 *
 * @example
 * ```tsx
 * import { ExcelIcon } from '@repo/icons';
 *
 * // Basic usage
 * <ExcelIcon />
 *
 * // With custom size and color
 * <ExcelIcon size="lg" color="#3B82F6" />
 *
 * // With Tailwind classes
 * <ExcelIcon className="text-blue-500 hover:text-blue-600" />
 * ```
 */
export const ExcelIcon = createPhosphorIcon(FileXls, 'excel');
