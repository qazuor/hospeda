import { UploadSimple } from '@phosphor-icons/react';
import { createPhosphorIcon } from '../../create-phosphor-icon';

/**
 * UploadIcon icon component
 *
 * @example
 * ```tsx
 * import { UploadIcon } from '@repo/icons';
 *
 * // Basic usage
 * <UploadIcon />
 *
 * // With custom size and color
 * <UploadIcon size="lg" color="#3B82F6" />
 *
 * // With Tailwind classes
 * <UploadIcon className="text-blue-500 hover:text-blue-600" />
 * ```
 */
export const UploadIcon = createPhosphorIcon(UploadSimple, 'upload');
