import { UserSwitch } from '@phosphor-icons/react';
import { createPhosphorIcon } from '../../create-phosphor-icon';

/**
 * Icon for user impersonation / switching.
 *
 * @example
 * ```tsx
 * <UserSwitchIcon size="md" weight="duotone" />
 * ```
 */
export const UserSwitchIcon = createPhosphorIcon(UserSwitch, 'user-switch');
