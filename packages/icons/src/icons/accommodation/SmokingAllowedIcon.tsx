import { Cigarette } from '@phosphor-icons/react';
import { createPhosphorIcon } from '../../create-phosphor-icon';

/**
 * Smoking allowed icon — indicates that smoking is permitted in an accommodation.
 * Uses Phosphor Cigarette icon.
 */
export const SmokingAllowedIcon = createPhosphorIcon(Cigarette, 'smoking-allowed');
