import { Door } from '@phosphor-icons/react';
import { createPhosphorIcon } from '../../create-phosphor-icon';

/**
 * Bedrooms icon — represents the number of bedrooms in an accommodation.
 * Uses Phosphor Door icon.
 */
export const BedroomsIcon = createPhosphorIcon(Door, 'bedrooms');
