import { SpeakerSlash } from '@phosphor-icons/react';
import { createPhosphorIcon } from '../../create-phosphor-icon';

export const QuietEnvironmentIcon = createPhosphorIcon(SpeakerSlash, 'quiet-environment');
