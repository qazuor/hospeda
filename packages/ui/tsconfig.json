{
    "extends": "@repo/typescript-config/react-library.json", // Hereda del base (que tiene paths base)
    "compilerOptions": {
        // Considera heredar outDir si es consistente en todos los paquetes
        "outDir": "dist"
        // Otras opciones son heredadas del react-library.json y base.json
    },
    "include": ["src"],
    "exclude": ["dist", "build", "node_modules"]
}
