---
title: Resultados de las sondas 01, 02 y 03 — Mercado Pago sandbox
linear: HOS-1352
statusSource: linear
created: 2026-09-15
status: CURRENT
phase: 1C
environment: sandbox
measuredAt: 2026-09-15
---

# Sondas 01, 02 y 03 — resultados

**Ejecutadas**: 2026-09-15 contra el **sandbox** de Mercado Pago, con las credenciales de
aplicación de prueba que entregó el owner.

- `probe-01-preapproval-unauthorized.sh` — todo lo que se puede medir sin autorizar.
- `probe-02-authorized-lifecycle.sh` — el ciclo de vida de una suscripción **autorizada**.
- `probe-03-saved-card-recreate.sh` — si se puede recrear sin pedir la tarjeta de nuevo.

Request y response completos de cada llamada quedaron en el directorio de salida de cada
sonda (§59). **Las credenciales no están en el repo ni en los scripts**: se leen del entorno.

> Estos resultados **caducan** (`S-MP-02`). Se re-verifican antes de FASE 10.

---

## 0. El hallazgo que atraviesa todo lo demás

**Mercado Pago acepta cambios que no aplica, y responde `200`.**

**Cinco** casos confirmados, todos con HTTP de éxito y sin ningún aviso:

| Qué se mandó | Qué respondió | Qué quedó realmente |
|---|---|---|
| `items` con dos líneas al crear | `201` | El campo **no vuelve en la respuesta**. Un solo monto |
| `frequency: 12` sobre una autorizada | `200`, con el monto sí actualizado | **`frequency` siguió en 1** |
| `frequency: 3` sobre otra autorizada, aislado | `200` | **`frequency` siguió en 1** |
| Tokenizar una tarjeta guardada sin el código de seguridad | `201`, token generado | **El token no sirve**: la suscripción falla con `400` |
| `notification_url` propia al crear un preapproval | `201` | **Vuelve `null` en la relectura.** No existe webhook por suscripción |

Consecuencia de diseño, y es el hallazgo más importante de FASE 1C hasta acá:

> **Un `200` de Mercado Pago no significa que el cambio se haya aplicado.** Toda mutación
> tiene que ir seguida de una **relectura y una comparación campo por campo** contra lo que se
> pidió. Un reconciliador que confíe en el código de estado va a reportar éxito sobre cambios
> que nunca ocurrieron.

Se verificó dos veces, de forma aislada, antes de escribirlo.

---

## 1. Lo que quedó VERIFICADO

### Preapproval

- **`PA-1` creación por API** — `201`, `status: pending`, `init_point` presente, sin cobrar.
- **`PA-2` linking con nuestro dominio** — `external_reference` se acepta y vuelve en la
  respuesta y en el `GET`. **Pero ver `RC-1`: no se puede buscar por él.**
- **`PA-3` autorización** — **se puede autorizar por API, sin navegador**: `POST /preapproval`
  con `card_token_id` y `status: "authorized"` devuelve `201` con `status: authorized`.
  **Y cobra en el acto**: `summarized.charged_quantity: 1`, `charged_amount: 1500`,
  `last_charged_date` igual al instante de creación. El pago quedó `approved` / `accredited`.
- **`PA-5` cancelación** — `PUT {status:"cancelled"}` funciona y es **irreversible**:
  reintentar da `400 "You can not modify a cancelled preapproval."`, y sobre una autorizada
  cancelada, `400 "Invalid transition from cancelled to authorized"`.

### Frecuencias (§19)

Las cuatro del §19 existen, **todas como `frequency_type: "months"`**:

| Ciclo | Cómo se expresa | Resultado |
|---|---|---|
| Mensual | `frequency: 1, months` | `201` |
| Trimestral | `frequency: 3, months` | `201` |
| Semestral | `frequency: 6, months` | `201` |
| Anual | `frequency: 12, months` | `201` |

Dos cosas más, medidas:

- **`frequency_type: "years"` NO existe**: `400 "Invalid value for frequency type, valid ones
  are [days, months]"`.
- **Una frecuencia arbitraria se acepta**: `frequency: 5, months` devolvió `201`. O sea que
  **los cuatro ciclos del §19 son una elección de Hospeda, no un límite del proveedor**.

### Montos

- **`PC-2` piso de ARS 15**: un monto de `1` da `400 "Cannot pay an amount lower than $ 15.00"`.
- Monto `0` y monto negativo: `400 "Invalid value for transaction amount, must be a positive
  number"`.

### Cambios sobre una suscripción ya autorizada

- **`PC-1` el monto SÍ se puede mutar**: 1500 → 2200 → 15 → 1500, todos `200`, verificado por
  relectura en cada paso.
- **`PC-3` NO requiere nuevo consentimiento del usuario**: la mutación se aplica sola, la
  suscripción sigue `authorized` y conserva su medio de pago.
- **`EX-4` la FRECUENCIA no se puede cambiar** — y falla en silencio (ver §0).

### Compensación en días

- **`EX-7` crear con la primera fecha corrida**: `auto_recurring.start_date` a +20 días dejó
  `next_payment_date` en esa fecha.
- **`EX-8` y se respeta TRAS autorizar** — que es la que decidía. Una suscripción creada
  `authorized` con `start_date` a +20 días quedó con `next_payment_date` en esa fecha y
  **`summarized.charged_quantity: null`: no cobró nada al crearse**, a diferencia de la
  autorizada sin `start_date`.
- Detalle a tener en cuenta: **Mercado Pago modela eso como un trial**. Al mandar una
  `start_date` futura, la respuesta trae un objeto que nosotros no pedimos:
  `free_trial: {frequency: 20, first_invoice_offset: 20, frequency_type: "days"}`.
  Diferir el primer cobro y "dar un trial" son **el mismo mecanismo** del lado del proveedor.

### Convivencia y reconciliación

- **`EX-6` dos suscripciones autorizadas del mismo pagador conviven** sin conflicto.
- **`RC-1` el `GET /preapproval/{id}` es confiable**: reflejó `cancelled` inmediatamente
  después del `PUT`.
- **`RC-2` hay historial de pagos**, por dos caminos que funcionan:
  `GET /authorized_payments/search?preapproval_id={id}` y
  `GET /v1/payments/search?external_reference={ref}`.

### Pausa — sólo las transiciones

- **`PS-1` pausar** (`PUT {status:"paused"}`) y **`PS-3` reanudar** (`PUT
  {status:"authorized"}`) devuelven `200` y el estado cambia.

---

## 2. Lo que quedó NOT_SUPPORTED

| Fila | Qué se intentó | Evidencia |
|---|---|---|
| **`EX-5`** | Que una autorización cubra **más de un monto** | `auto_recurring` como array → `400 "Parameters passed are invalid"`. Campo `items` con dos líneas → `201` **y se descarta en silencio** |
| **`EX-4`** | Cambiar el **ciclo** de una suscripción autorizada | `200` dos veces, y `frequency` no se movió de 1 |
| — | `frequency_type: "years"` | `400`, válidos sólo `[days, months]` |
| — | Bajar el monto a **cero** (cortesía por monto cero) | `400`, debe ser positivo. **El piso real es ARS 15** |

---

## 3. Lo que quedó PARCIAL, y por qué

**`RC-1` — el `search` de preapprovals ignora `external_reference` en silencio.**

Medido de forma aislada:

| Consulta | Total devuelto |
|---|---|
| `/preapproval/search` sin filtro | 111 |
| `…?external_reference=` con un valor **que existe** | **111** |
| `…?external_reference=` con basura | **111** |
| `…?status=pending` | 12 |
| `…?status=` con basura | 0 |

`status` **sí** filtra. `external_reference` **no**: se ignora y devuelve todo, paginado.

Y `external_reference` es, por `PA-2`, **nuestro único vínculo entre un preapproval y nuestro
dominio**. Consecuencia directa: la reconciliación **no puede buscar por nuestra referencia**
— hay que guardar el id del proveedor localmente y leer por `GET` por id, o paginar todo
filtrando por `status`.

Asimetría medida, y útil: **`/v1/payments/search` SÍ filtra por `external_reference`**
(79 sin filtro · 1 con el nuestro · 0 con basura). El defecto es del endpoint de preapprovals,
no de la API entera.

**`EX-9` — tokenizar una tarjeta ya guardada exige el código de seguridad.**

Se midió con la sonda 03, y es el caso que decidió `DEC-SUB-005`:

| Variante | Tokenizar | Crear la suscripción | Qué quedó realmente |
|---|---|---|---|
| `card_id` solo | `201`, token generado | **`400`** *"Card token was generated without cvv validation"* | nada |
| `card_id` + `security_code` | `201` | **`201`** | `authorized`, **ciclo 3 meses**, ARS 4000, primer cobro a +18 días, **sin cobrar al crear** |

**`EX-10` verificado por relectura independiente**, no por el código de estado del `POST`.

Es el **cuarto caso** del patrón del §0: el endpoint de tokenización devuelve `201` para un
token que después no sirve. El `201` describe que se creó un token, no que ese token valga para
lo que uno quiere hacer.

Nota: `POST /preapproval` con `card_id` **a nivel raíz** devuelve `400 "Invalid request data"`.
El camino que funciona es tokenizar primero y mandar `card_token_id`.

---

## 3 ter. La URL de webhook es de sólo lectura por API

Medido: `GET /applications/{client_id}` **sí** devuelve la configuración, incluido
`notifications_callback_url`. Pero `PUT`, `POST` y `PATCH` sobre ese mismo recurso devuelven
**`403 Forbidden`**, incluso escribiendo el valor que ya tenía.

Consecuencia operativa: **cambiar el destino de los webhooks sólo se puede hacer desde el panel
de desarrolladores**, a mano. No hay forma programática de apuntarlos a un entorno de pruebas
y devolverlos después, lo que vuelve el cambio una operación manual y por lo tanto olvidable.

Se leyó, y es el dato que confirma lo de abajo:
`notifications_callback_url = https://staging-api.hospeda.com.ar/api/v1/webhooks/mercadopago?source_news=webhooks`

---

## 3 bis. El webhook es por APLICACIÓN, no por suscripción

Medido: `POST /preapproval` con `notification_url` devuelve `201` y la relectura la trae en
`null`. **No se puede enrutar el webhook de una suscripción concreta.**

Consecuencia de diseño, y no es menor: **una aplicación de Mercado Pago tiene una sola URL de
webhook**. Entornos distintos (producción, staging, un banco de pruebas) **necesitan
aplicaciones de Mercado Pago distintas**, o se pisan entre sí.

Se comprobó en vivo: la aplicación de sandbox usada en estas sondas **tiene su webhook apuntado
al staging de Hospeda**, así que los eventos de las sondas 01 a 03 llegaron al billing de
staging sin que la sonda lo pidiera. Staging los rechazó correctamente — no encontró
suscripción local y no registró nada — pero quedaron **3 eventos `invoice.updated` en su cola
de reintentos**.

**Y eso vuelve a staging un mal instrumento para medir webhooks**, por una razón que no es
operativa sino de método: lo que se observa en sus logs es **la interpretación de nuestro código
actual**, no lo que mandó el proveedor. Ejemplo concreto de esta misma medición: el proveedor
mandó `type=subscription_authorized_payment` (se ve en la URL del POST) y el log de staging lo
llama `invoice.updated`. **La capa legacy renombra el evento antes de que se lo pueda ver.**

Medir Mercado Pago a través del código que este programa viene a reemplazar es exactamente la
contaminación que el §58 prohíbe.

---

## 3 quater. Webhooks: lo que se pudo medir sin endpoint propio

**No se consiguió un endpoint público propio.** Tres túneles distintos levantaron conexión y
obtuvieron URL, y **los tres devolvieron `404` desde el borde de Cloudflare**: desde este
entorno no funcionan. Y la URL de webhook **no se puede cambiar por API** (ver 3 ter).

Pero la aplicación ya apunta a staging, y sus eventos quedan en una tabla. De ahí salen tres
mediciones reales — **con una limitación que hay que tener presente**.

### La limitación

Lo que esa tabla guarda **ya está normalizado por nuestro código**: el tipo se registra como
`invoice.updated`, que es un nombre nuestro, no de Mercado Pago — el proveedor mandó
`subscription_authorized_payment`. **No hay acceso a los headers** (y por lo tanto no a la
firma), ni a los bytes crudos.

Lo que **sí** es del proveedor y sobrevive a la normalización: el **id del evento**, el **id del
recurso** y el **instante de llegada**. Las tres mediciones de abajo usan sólo eso.

### `WH-4` — reintenta ante un error, con backoff creciente

Staging devolvió `500` a un evento (no encontraba la suscripción local, correctamente, porque
las sondas no están en su base). Mercado Pago lo reintentó:

| Entrega | Id del evento | Llegada | Desde la anterior |
|---|---|---|---|
| 1 | `137621652438` | 09:22:45.840 | — |
| 2 | `137621583760` | 09:22:47.377 | **1,5 s** |
| 3 | `137621773702` | 09:40:34.433 | **17,8 min** |
| 4 | `137457067769` | 10:15:52.042 | **35,3 min** |

Las cuatro son del **mismo recurso** (`7031956616`).

### `WH-1` — hay entregas duplicadas

Las dos primeras, separadas por **1,5 segundos**, son un duplicado casi inmediato, de otra
naturaleza que los reintentos con backoff.

### El hallazgo que condiciona el diseño

> **Cada reentrega trae un `provider_event_id` DISTINTO.**

Cuatro entregas del mismo hecho, cuatro ids. **Deduplicar por id de evento no sirve**: cada
reintento pasaría el filtro como si fuera nuevo, y un cobro se registraría cuatro veces.

La clave de idempotencia tiene que derivarse del **tipo más el id del recurso**, no del id del
evento. Es un insumo directo de `M-CONC-02` (no-retroceso de estado) y de `M-MAIL-02`
(deduplicación de envíos).

**Advertencia sobre esta medición**: es indirecta y de una sola muestra. Que las cuatro entregas
sean reintentos del mismo hecho — y no cuatro hechos distintos sobre el mismo recurso — es una
**inferencia** razonable (mismo recurso, `500` en el medio, backoff creciente), no algo que se
haya leído del proveedor. Se confirma con un endpoint propio, que es lo que falta.

---

## 4. Lo que sigue en UNKNOWN, y qué haría falta

| Bloque | Por qué no se pudo |
|---|---|
| **Renovaciones** (`RN-1..3`) | Requieren que pase un ciclo real |
| **Grace** (`GR-1..3`) | Requieren un cobro fallido y esperar la política de reintentos |
| **Pausa, efectos reales** (`PS-2`, `PS-4`, `PS-5`, `PS-6`) | La pausa de la sonda duró **1,3 segundos**: alcanza para probar las transiciones, **no** para saber qué pasa con las fechas de cobro a lo largo de una pausa real. `next_payment_date` no se movió, pero de una pausa de un segundo **no se puede concluir nada** sobre §26.4 |
| **Webhooks** (`WH-1..5`, `EX-2`) | Hace falta un endpoint público que reciba los POST del proveedor. Hoy no hay ninguno disponible para la sonda |
| **Reembolsos** (`RF-1..3`) | Se intentaron sobre el cobro real de ARS 1500 y el endpoint devolvió **`401 "Unauthorized use of live credentials"`**. Eso **no dice que el proveedor no soporte reembolsos**: dice que estas credenciales no pueden pedirlos. La capacidad sigue sin medir |
| **Correos del proveedor** (`EX-3`) | Requiere observar la casilla del comprador de prueba |
| **Cortesía** (`CT-1`, `CT-3`) | `CT-2` quedó parcialmente respondida: bajar el monto funciona, pero **el piso es ARS 15, no cero** |

---

## 5. Qué cambia esto en las decisiones ya tomadas

### `DEC-SUB-001` — reemplazada por `DEC-SUB-005`

La decisión dice que cualquier cambio de ciclo se aplica ya, **compensando en días** corriendo
la primera fecha de cobro. Eso presuponía poder mutar el ciclo de la suscripción vigente.

**No se puede**: `EX-4` es `NOT_SUPPORTED`, y encima falla en silencio.

Pero el plan B declarado (*"el cambio de ciclo espera a la renovación"*) **no es la única
salida**, porque `EX-8` salió `VERIFIED`: se puede **cancelar la vieja y crear una nueva con la
primera fecha de cobro corrida** por los días ya pagados. El efecto para el cliente es
exactamente el que la decisión buscaba.

El obstáculo de esa salida se midió con la sonda 03 y **resultó chico**: recrear **no exige
recargar la tarjeta**, sólo el **código de seguridad** de la que el cliente ya autorizó.

Por eso `DEC-SUB-005` mantiene la política de `DEC-SUB-001` intacta y sólo cambia el
mecanismo: **crear la nueva primero, cancelar la vieja después**. Ese orden importa, porque
cancelar es irreversible (`PA-5`) y dos autorizadas del mismo pagador conviven sin conflicto
(`EX-6`), así que el estado intermedio no es destructivo y el inverso sí lo sería.

### `DEC-ENT-004` y `DEC-GRANT-001` — el riesgo declarado no cambia

Las dos cancelan sin reembolso. Se confirmó que **cancelar es irreversible** en los dos
estados, así que la implicación registrada — "si después cancela el plan comercial se queda sin
la suscripción que tenía" — es correcta y no depende de una suposición.

### `BD-MP-03` (cambio de precio) — quedó de hecho respondida

`PC-1` y `PC-3` salieron `VERIFIED`: **el monto de una suscripción autorizada se puede cambiar,
y no hace falta nuevo consentimiento del usuario del lado del proveedor**. El §29 puede
cumplirse sin re-autorización técnica.

Lo que el proveedor permita no es lo que la ley permita: el aviso previo, el precio anterior y
nuevo, la fecha efectiva y el derecho a cancelar del §29 siguen siendo obligatorios. Acá sólo
se cerró la pregunta técnica.

### `BD-MP-02` (cortesía) — una estrategia quedó acotada, no descartada

Bajar el monto funciona, **pero el piso es ARS 15**: no se puede llegar a cero por ese camino.
Una cortesía "sin cobrar" por reducción de monto no existe; a lo sumo hay una cortesía de
ARS 15 por ciclo. Las otras estrategias (pausar, cancelar y recrear al final, cobrar y
reembolsar) siguen sin medirse.

### `BD-MP-04` (addons recurrentes) — la pregunta de fondo quedó cerrada

`EX-5` es `NOT_SUPPORTED`: **una autorización cubre un solo monto**. Un addon recurrente no
puede sumarse como una línea aparte dentro de la misma suscripción.

Lo que sí se puede es **mutar el monto total** (`PC-1`, `VERIFIED`, sin re-consentimiento). O
sea que la vía para un addon recurrente es *subir el monto de la suscripción del plan*, no
crear una línea nueva — con todo lo que eso implica para mostrar el detalle al cliente y para
conciliar. La decisión es de diseño y **no se toma acá**.

---

# Segunda tanda del 2026-09-15 — sondas 05 y 07

Corridas a las 12:26–12:31 contra el mismo sandbox. La 05 **arranca un reloj**
(deja suscripciones vivas para leer a las 24 h); la 07 responde en el acto.

> **Aclaración sobre las credenciales, que corrige un supuesto.** El modo de
> pruebas actual de Mercado Pago **no usa credenciales con prefijo `TEST-`**: se
> arma creando un usuario vendedor de prueba y, bajo él, una aplicación propia,
> cuyas credenciales empiezan con `APP_USR-` y son **indistinguibles por su
> forma** de unas productivas. Lo que sí distingue, y lo afirma el proveedor, es
> `GET /users/me` → `tags: ["test_user", …]`. El vendedor de estas corridas es
> `TESTUSER1461768820173121923` (id `3497260543`), y el comprador es **otra**
> cuenta de prueba.

## 6. El ciclo diario existe y se ejecuta — el reloj arrancó

`frequency: 1, frequency_type: "days"` se acepta, **se autoriza y queda así**,
verificado por relectura en los seis sujetos:

| | |
|---|---|
| estado | `authorized` |
| ciclo releído | `1 days` |
| cobro al crear | **1 cobro**, ARS 2000 |
| `next_payment_date` | **el día siguiente**, misma hora |

Esto es lo que vuelve medibles en 24 h las diecisiete filas que esperaban un
ciclo. Confirma además, ahora de punta a punta, lo que `FR-4` sólo insinuaba:
**los cuatro ciclos del §19 son una elección de Hospeda, no un límite del
proveedor**.

Sujetos vivos, con su id (`/tmp/mp-probe-05/manifiesto.json`):

| slug | id | qué mide | estado al arrancar |
|---|---|---|---|
| `renov-ok` | `930a7596…` | `RN-1` | `authorized`, ARS 2000 |
| `renov-falla3` | `0e678ead…` | `RN-2`, `GR-*` | `authorized`, ARS 2000 |
| `pausa-real` | `747cc456…` | `PS-2`/`4`/`5`/`6` | **`paused`** |
| `sin-autorizar` | `bf9b6feb…` | `EX-1` | `pending` |
| `monto-baja` | `5e4c5e5c…` | `DW-1`/`DW-2` | ARS **1000** |
| `monto-sube` | `4f60c31c…` | `UP-1`/`UP-2` | ARS **4000** |
| `cortesia-piso` | `30c03cca…` | `CT-1`/`CT-3` | ARS **15** |

## 7. Las tres mutaciones se aplicaron, y ninguna cobró ni movió la fecha

Verificado por relectura en cada una: 2000 → 1000, 2000 → 4000 y 2000 → 15,
las tres `200` y las tres **efectivas**. En los tres casos
`next_payment_date` **no se movió** y `charged_quantity` **siguió en 1**.

O sea: **subir el monto no cobra la diferencia en el acto**. Es un insumo
directo de `UP-2`, que se cierra mañana cuando se vea *cuánto* cobra el
proveedor en el ciclo siguiente.

## 8. `PC-2` tiene techo, no sólo piso

`400 "Cannot pay an amount greater than $ 2000000.00"`. El rango real de una
suscripción es **ARS 15 a ARS 2.000.000**.

## 9. `PA-4` — una tarjeta que va a rechazar NO llega a crear la suscripción

Dos intentos, con los dos titulares de rechazo de las tarjetas de prueba:

| titular | qué hace | resultado |
|---|---|---|
| `FUND` | rechaza por fondos insuficientes | `400 CC_VAL_433 Credit card validation has failed` |
| `OTHE` | rechaza por error general | `400 CC_VAL_433 Credit card validation has failed` |

La validación de la tarjeta ocurre **antes** de que exista la suscripción, así
que el rechazo se manifiesta como una creación fallida, no como una suscripción
autorizada que después no puede cobrar.

**Consecuencia para `RN-2` y `GR-1..3`, y es un problema de método**: *no se
puede fabricar una renovación fallida eligiendo una tarjeta mala*. Queda un
camino, y es el sujeto `renov-falla3`: nacer con una tarjeta buena y subirle el
monto a algo que no se pueda pagar. Si mañana tampoco falla, `RN-2` y `GR-*`
necesitan otra idea — y eso también es un hallazgo.

## 10. Un `card_token` sirve para UNA sola suscripción

`400 "Card token was used, please generate new"` a partir del segundo uso. La
primera corrida de la sonda 05 tokenizó una vez y reusó: **perdió cinco de siete
sujetos**.

Importa más allá de la sonda: `DEC-SUB-005` (cancelar y recrear) y cualquier
reintento de creación necesitan **tokenizar de nuevo cada vez**. Un reintento
que reusa el token del intento anterior falla siempre, y falla con un mensaje
que no se parece a "reintentaste".

## 11. Qué se puede hacer sobre una suscripción PAUSADA — sonda 07

La sonda 05 devolvió `400 "You can not modify a paused preapproval."` al
re-pausar una ya pausada. Ese mensaje sugiere algo mucho más grande que lo que
se había probado, así que se midió, **con control**:

| Sobre el MISMO sujeto | HTTP | Qué quedó |
|---|---|---|
| pausar | `200` | `paused` |
| **cambiar el monto estando pausada** | **`400`** *"You can not modify a paused preapproval."* | **ARS 2000: no cambió** |
| reanudar | `200` | `authorized` |
| **cambiar el monto ya reanudada** | **`200`** | **ARS 3000: cambió** |

El cuarto paso es el control y es el que hace válida la conclusión: el mismo
payload que la pausa rechaza, la suscripción reanudada lo acepta. **Lo que
bloquea es el estado `paused`, no la operación.**

Y sobre otro sujeto: **cancelar una pausada SÍ funciona** (`200` → `cancelled`).
La pausa no deja al cliente atrapado sin poder darse de baja.

### Una medición que casi se concluye mal

El primer intento de esta sonda se comió un **`429 local_rate_limited`**
exactamente en el paso que decidía. Un `429` **no dice que la operación esté
prohibida**: dice que no llegó a evaluarse. Tomarlo por un `NOT_SUPPORTED`
habría inventado una limitación inexistente. La sonda ahora reintenta con
backoff ante `429`, y la conclusión de arriba es de la corrida limpia.

Es el reverso exacto del §0: allá un `2xx` no probaba que algo se aplicara; acá
un error no prueba que algo esté prohibido. **Ningún código de estado, de
ninguna familia, alcanza por sí solo.**

---

# Tercera tanda del 2026-09-15 — sondas 08 y 09, con receptor propio

El owner repuntó el webhook de la aplicación de prueba al Worker de la sonda 08
(verificado por relectura contra Mercado Pago: `notifications_callback_url` ya
apunta ahí). **Por primera vez se ve lo que el proveedor manda, y no lo que
nuestro código dice que mandó.**

La sonda 09 ejecuta una secuencia conocida —crear, cambiar monto, pausar,
reanudar, cancelar— anotando el instante de **envío** de cada acción. Tres
corridas, con 1,4 s, 60 s y 90 s de separación.

## 12. `WH-2` — las demoras son reales y muy variables

Entre **0,6 s y 32 s** para la misma clase de acción. No es una demora fija ni
un techo estable.

Consecuencia de método, que costó dos corridas aprender: **un experimento que
atribuye un evento a una acción necesita espaciar las acciones más que la
demora máxima**. Con 1,4 s la atribución era imposible; con 20 s seguía siendo
ambigua; con 90 s es una lectura directa.

## 13. `EX-2` — el cuerpo trae `version`, y eso cambia el diseño

Los eventos de `subscription_preapproval` traen un campo **`version`**, que es
un **contador monótono por recurso**. En la corrida de 90 s, el mismo
preapproval llegó con `version` 5, 9, 11 y 12, en el orden causal exacto de las
acciones.

Esto **amplía** lo que se había concluido antes. Aquello seguía siendo cierto
—el id del evento cambia en cada reentrega, así que no sirve para deduplicar—
pero se había medido sobre la tabla ya normalizada de staging, que **no guarda
este campo**. Con `version` no sólo se puede deduplicar: se puede **descartar
un evento viejo que llega tarde**, que es exactamente lo que pide `M-CONC-02`.

## 14. La firma: por fin visible

```
x-signature: ts=1789487640,v1=693dcbc4c437daffeb990da338869d0ee24d1ec4c4642acf20ea45acbd9fec57
x-request-id: 07262cff-5377-4ec8-a417-5cb2577112e4
```

Llega en **todas** las entregas reales. Hasta esta medición era literalmente
inobservable: la tabla de staging no guarda headers.

Lo que falta para cerrar la fila: **la clave secreta de webhook**, que vive en
el panel de desarrolladores. Sin ella se puede ver la firma pero no
*verificarla*, y ver no es verificar.

## 15. `live_mode: true` — en una cuenta de prueba

```json
{"action":"payment.created","live_mode":true,"user_id":"3497260543", …}
```

Ese evento es de la cuenta de prueba (`tags: ["test_user"]`) y **se declara
productivo**. **No se puede distinguir el entorno por `live_mode`.**

Un handler que filtre por ese campo trataría los eventos de sandbox como
reales. Es del mismo género que el prefijo `TEST-`: en Mercado Pago, varias de
las señales que *parecen* indicar entorno no lo indican.

## 16. Hay operaciones que NO emiten webhook, y una importa mucho

Medido con 90 s entre acciones, o sea sin ambigüedad de atribución:

| Acción | ¿Notifica? | Demora |
|---|---|---|
| crear (con cobro) | **sí** — `payment`, `subscription_authorized_payment` y `subscription_preapproval` | 2–7 s |
| **cambiar el monto** | **NO** | — |
| pausar | **sí** | 17,9 s |
| reanudar | **sí** | 7,6 s |
| cancelar | **sí** | 2,0 s |

**La mutación del monto no produce ninguna entrega.** Noventa y un segundos de
ventana, cero eventos, y la siguiente entrega es atribuible sin dudas a la
pausa.

Y la prueba de que el recurso sí cambió: **la `version` saltó de 5 a 9 sin una
sola notificación en el medio**. No es que el cambio no ocurriera — es que el
proveedor no lo comunica.

### Por qué esto pesa sobre `DEC-MP-001`

`DEC-MP-001` decidió que los cambios de precio sobre suscripciones vigentes se
aplican mutando el monto. Ahora se sabe que esa operación:

1. **puede responder `2xx` sin haberse aplicado** (§0), y
2. **no emite ningún evento** que permita enterarse después.

O sea que **no hay ninguna vía de confirmación asincrónica**: la única forma de
saber si un aumento se aplicó es **releer el preapproval y comparar**. Un
reconciliador que espere un webhook de cambio de precio va a esperar para
siempre. Esto no invalida la decisión —sigue siendo la única que no le pide
nada al cliente— pero convierte la relectura en **obligatoria**, no en una
buena práctica.

## 17. Dos defectos de método propios, corregidos

- **La marca de tiempo se anotaba al terminar la llamada, no al enviarla.** Eso
  la corría casi un segundo hacia adelante y hacía que un evento posterior a la
  acción apareciera como anterior. Toda la atribución se apoya en esa marca.
- **La sonda no reintentaba ante `429`.** La corrida de 60 s se comió uno justo
  en el cambio de monto: la mutación no se aplicó y el paso quedó sin poder
  afirmar nada. Es la segunda vez en el día que un `429 local_rate_limited`
  casi se lee como una respuesta del negocio.

---

# Cuarta tanda del 2026-09-15 — sondas 10, 11 y 12

El owner entregó la **clave secreta de webhook**. Eso cerró `EX-13` y permitió
volver sobre los reembolsos con una configuración ya entendida.

## 18. `EX-13` — la firma se verifica

El `v1` del header es un **HMAC-SHA256** con la clave de la aplicación sobre
este manifiesto exacto:

```
id:<data.id>;request-id:<x-request-id>;ts:<ts>;
```

**El punto y coma final incluido**: sin él no coincide. Se probaron cinco
formas candidatas contra **tres entregas reales de tipos distintos**
(`payment`, `subscription_preapproval`, `subscription_authorized_payment`), y
sólo esa reproduce el `v1`.

Probar varias formas no es tanteo: dar por buena la documentada sin
comprobarla habría sido completar una fila desde documentación, que es lo que
el §58 prohíbe. La sonda además verifica **su propio HMAC** contra un vector
conocido (RFC 4231), para que un "no coincide" no pueda confundirse con una
herramienta rota.

Consecuencia: **el receptor puede rechazar cualquier POST que no venga firmado
por el proveedor**, así que exponer una URL de webhook no es exponer un
agujero.

## 19. `RF-*` — los reembolsos siguen sin poder pedirse, y ahora se sabe por qué no es el token

Se reintentó con las credenciales del vendedor de prueba: **mismo `401
"Unauthorized use of live credentials"`**. Pero esta vez con controles:

| Llamada | Resultado |
|---|---|
| `GET /v1/payments/{id}` | **`200`** — el token lee |
| `POST /v1/card_tokens` | **`201`** — el token escribe en `/v1` |
| `POST /v1/payments/{id}/refunds` | **`401`** |
| `PUT /v1/payments/{id}` con `{"status":"refunded"}` | **`401`** |

O sea: **no es que el token no sirva**. Lee, y escribe. Son **las operaciones
de reembolso** las vedadas para esta cuenta, por las **dos** vías.

Las filas siguen `UNKNOWN` a propósito: **no se midió si Mercado Pago
reembolsa**, se midió que desde acá no se puede pedir. Marcarlas
`NOT_SUPPORTED` sería atribuirle al proveedor una limitación que es de la
configuración.

Y eso deja a `M-LEGAL-01` (derecho de revocación con devolución) **sin
respuesta técnica**, que es una decisión de producto con plazo legal detrás.

## 20. `CN-1` — la cancelación programada existe, pero sólo al crear

| | Resultado |
|---|---|
| crear **con** `auto_recurring.end_date` | **`201`**, y **sobrevive a la relectura** |
| fijar `end_date` sobre una **ya autorizada** | **`200`** … y **el campo no está en la relectura** |

**Sexto caso del §0.** La fecha de fin se decide **al contratar o no se
decide**.

Consecuencia de diseño, y no es menor: una baja a fin de período **pedida
después** —que es el caso normal, porque nadie contrata pensando en cuándo se
va a ir— **hay que emularla**: dejar la suscripción viva y cancelarla con un
proceso propio el día que corresponde. Con lo que eso implica: **un cron que se
cae deja cobrando a alguien que pidió la baja**.

## 21. `WH-1` — los duplicados existen, y confirman por qué `version` es la clave

Con el receptor en `mode=fail`, el **mismo evento** llegó dos veces:

| Entrega | `version` | id de notificación | Llegada |
|---|---|---|---|
| 1 | **2** | `137634311084` | 18:42:20.253 |
| 2 | **2** | `137634346430` | 18:42:20.763 |

Misma `version`, **ids de notificación distintos**, 510 ms de diferencia. Y el
patrón se repitió con `version: 5`.

Esto cierra el círculo con `EX-2`: **el id del evento cambia entre duplicados y
la `version` no**. Deduplicar por id de notificación dejaría pasar los dos;
deduplicar por `(tipo, recurso, version)` los colapsa correctamente.

## 22. `WH-4` — el reintento RE-FIRMA, y sólo la `version` sobrevive

Con el receptor en `mode=fail`, sobre el mismo evento (`version: 5`):

| Entrega | Llegada | id de notificación | `ts` de la firma |
|---|---|---|---|
| original | 18:42:27.293Z | `137469141687` | `1789497747` |
| duplicado | 18:42:27.735Z | `137634346522` | `1789497747` |
| reintento 1 | 19:01:17.774Z | `137634886060` | `1789498877` |
| **reintento 2** | **19:36:23.013Z** | **`137470754489`** | **`1789500982`** |

La curva completa, medida desde la entrega anterior en cada paso:

| | Desde la anterior |
|---|---|
| duplicado | **+0,5 s** |
| reintento 1 | **+18,8 min** |
| reintento 2 | **+35,1 min** |

**Reproduce lo que se había inferido antes por otra vía** (1,5 s · 17,8 min ·
35,3 min), pero ahora medido sobre lo que manda el proveedor y no sobre nuestra
tabla normalizada. Dos observaciones independientes con el mismo resultado: el
backoff es real y aproximadamente **duplica** el intervalo.

Dato operativo que sale de ahí: **una caída del receptor de más de una hora
empieza a agotar los reintentos**. Cuánto reintenta en total no se midió — haría
falta dejarlo fallando mucho más tiempo — así que **no se puede afirmar que el
proveedor reintente indefinidamente**, y un diseño que lo suponga está apostando.

**Lo nuevo, y es lo que decide el diseño**: en el reintento cambian **el id de
notificación** *y* **el `ts` de la firma** —el proveedor **re-firma** cada
entrega— mientras la **`version` del recurso se mantiene**.

Consecuencia directa para `M-CONC-01` y `M-CONC-02`: **ni el id del evento ni la
marca de tiempo sirven para detectar una reentrega.** Un `ts` reciente no
significa "hecho reciente". La clave de idempotencia tiene que ser
**`(tipo, id del recurso, version)`**, que es lo único estable entre el
original, su duplicado y su reintento.


---

# Corrección del 2026-09-15 — el `401` no era de los reembolsos

En la sección 19 se escribió que *"son las operaciones de reembolso las vedadas
para esta cuenta"*. **Eso estaba mal**, y lo que estaba mal era el control.

El control usado fue: *¿el token puede escribir en `/v1`?* → `POST /v1/card_tokens`
devolvió `201`, así que se concluyó que el token escribía y que lo específico
eran los reembolsos.

**El control correcto era: ¿puede escribir en ESTE recurso?** Medido, todo en
una sola corrida (2026-09-15 16:56):

| Llamada | |
|---|---|
| `GET /v1/payments/{id}` | **200** |
| `GET /v1/payments/{id}/refunds` | **200** |
| `POST /preapproval` | **201** |
| `PUT /preapproval/{id}` (cambiar monto) | **200** |
| `PUT /preapproval/{id}` (cancelar) | **200** |
| `POST /v1/payments/{id}/refunds` | **401** |
| **`PUT /v1/payments/{id}` con `{"description":"x"}`** | **401** |

La última fila es la que decide: **un `PUT` completamente inocuo sobre un pago
también da `401`**. La aplicación **lee** pagos y **escribe** preapprovals sin
problema, pero **no puede escribir sobre `payments` en absoluto**.

O sea que el reembolso no es el objetivo del bloqueo: es **daño colateral**.

### Por qué importa la diferencia

Cambia a quién hay que pedirle qué. "Los reembolsos están deshabilitados" manda
a buscar una opción de reembolsos que probablemente no exista. "La aplicación no
tiene permiso de escritura sobre pagos" manda a mirar los **scopes de la
aplicación** en el panel de desarrolladores, que es donde está la respuesta.

### La lección de método

Un control demasiado grueso produce una conclusión **confiada y equivocada**.
`card_tokens` y `payments` son los dos `/v1`, y por eso el control parecía
válido; pero la pregunta no era sobre `/v1`, era sobre `payments`. **Un control
tiene que variar exactamente una cosa respecto de lo que falló** — acá, el
método sobre el mismo recurso.

Lo que la documentación aportó, y lo que no: la documentación oficial lista las
causas de falla de un reembolso —monto inválido, pago pendiente, credenciales
vencidas, saldo insuficiente, más de 180 días— y **ninguna** produce un `401` de
autorización. Eso fue útil como descarte, no como respuesta: sirvió para saber
dónde NO estaba la causa. La fila sigue sin cerrarse por documentación, como
exige el §58.


---

# Segunda corrección del 2026-09-15 — el `write` ya estaba, lo que difiere es el PRODUCTO

Se miró el panel de desarrolladores, con la sesión del vendedor de prueba que
es dueño de la aplicación.

## Lo que se vio, textual

**`Configuración de la aplicación` → `Configuración avanzada` → `Permisos de la
aplicación`:**

| Permiso | |
|---|---|
| `read` | ✅ tildado |
| `offline access` | ✅ tildado |
| **`write`** | ✅ **tildado** |

**`Producto integrado` → `¿Qué producto estás integrando?`:**

| Opción | |
|---|---|
| Checkout Pro | ○ |
| **Checkout API** | ○ |
| Checkout Bricks | ○ |
| **Suscripciones** | ● **seleccionada** |

Y en la pantalla de detalle, el mismo dato: *"Integración con: Suscripciones"*.

## Qué corrige esto

La recomendación anterior —*"darle permiso de escritura sobre `payments`"*—
**era un pedido equivocado: el permiso `write` ya está dado.** Es la segunda
corrección del día sobre esta misma fila, y las dos vinieron de razonar sobre
una causa en vez de ir a mirar.

## Qué queda como hipótesis, y por qué NO se la da por cerrada

Lo único que distingue a esta aplicación de una que sí escribiría sobre pagos es
el **producto**: está dada de alta como **Suscripciones**, no como **Checkout
API**. Eso explicaría con exactitud el patrón medido —escribe `preapproval`, lee
`payments`, no escribe `payments`— pero **es una explicación, no una medición**.

La medición sería cambiar el producto (o crear una segunda aplicación con
Checkout API) y reintentar el reembolso. **No se hizo, y a propósito**:

1. **Hay ocho suscripciones vivas** en esa aplicación esperando renovar. Cambiar
   el producto de la aplicación podría afectar su acceso a `preapproval` y
   **destruir el reloj**, que es el experimento más caro que hay en curso.
2. **El formulario exige resolver un reCAPTCHA** para guardar, así que el cambio
   lo tiene que hacer una persona de todos modos.

La salida sin riesgo es una **segunda aplicación de prueba con producto Checkout
API**, que deja intacta la actual. Si con esa el reembolso entra, la fila cierra
y además queda medido que el bloqueo era del producto.


---

# Tercera corrección del 2026-09-15 — la causa la declara el proveedor, y es un scope

Lo anterior quedó como *"explicación, no medición"*. Dejó de serlo: `GET
/applications/{id}` **lista los scopes de la aplicación**, y ahí está la causa,
dicha por Mercado Pago:

```
urn:mp:online:payments/read-only                      ← lectura
urn:mp:online:payments:cancel/read-only               ← lectura
urn:mp:online:payments:refunds/read-only              ← LECTURA
urn:mp:online:payments:chargebacks/read-write
urn:mp:online:subs-recurring:pre-approval/read-write  ← escritura
urn:mp:online:subs-recurring:subscription/read-write  ← escritura
```

**El scope de reembolsos es `read-only`.** Eso explica cada `401` medido hoy, sin
dejar nada a la interpretación: la app escribe `preapproval` porque su scope de
`subs-recurring` es `read-write`, y no escribe `payments` porque el suyo es
`read-only`.

Y desarma el `write` del panel: ese checkbox es el scope **grueso** de OAuth, no
el permiso por recurso. Estaba tildado y no alcanzaba, que es justo lo que
confundió el diagnóstico anterior.

## El `test_access_token`, que no habíamos probado

La misma respuesta expone un segundo token, con prefijo `TEST-`. Probado:

| | |
|---|---|
| `GET /v1/payments/{id}` con `TEST-` | **200** |
| `POST /v1/payments/{id}/refunds` con `TEST-` | **`404 "Payment not found"`** |

No es un camino: ese token no ve el pago como suyo. Queda registrado para que
nadie lo intente de nuevo pensando que es la salida.

## Y la pregunta que esto habilita responder sin riesgo

*"Si cambiamos el producto a Checkout API, ¿las suscripciones siguen andando?"*

**No se sabe, y no hace falta arriesgar nada para averiguarlo.** Los scopes se
**leen por API**, así que alcanza con:

1. crear una segunda aplicación de prueba con producto **Checkout API**;
2. `GET /applications/{id2}` y mirar su lista de scopes;
3. comparar contra la de arriba.

Si la segunda lista `subs-recurring:*/read-write`, cambiar el producto sería
inocuo. Si no los lista, cambiarlo **rompería las suscripciones**, y eso queda
sabido **sin haber tocado la app que tiene el reloj corriendo**.

## Por qué NO se cambia el producto de la app actual

Además del riesgo sobre las 8 suscripciones vivas: **todo lo medido hoy se midió
con `producto = Suscripciones`**. La regla 4 de la matriz exige fecha **y
entorno** por fila, y `S-MP-02` dice que un resultado viejo no es un resultado.
Cambiar el producto convierte las **38 filas medidas en no-medidas**.

---

# `R-MP-01` — la API de Payments se descontinúa, y qué hacer al respecto

## El aviso, y por qué las fuentes no coinciden

Al crear una aplicación con solución **Checkout API**, el panel obliga a elegir
`Tipo de API` entre **API de Orders** y **API de Payments**, y sobre la segunda
muestra en rojo:

> **Esta API será descontinuada pronto.**

Pero la documentación **no lo formaliza**:

| Fuente | Qué dice |
|---|---|
| Panel de desarrolladores (visto 2026-09-15) | *"Esta API será descontinuada pronto"* |
| Docs de **Suscripciones → gestión de pagos** | Siguen indicando `GET /v1/payments/{id}` y `/v1/payments/search`, **sin aviso** |
| Docs de la **Orders API** | Presenta Orders como opción **paralela**, no como reemplazo. **Sin fecha ni aviso** |

**Esa discrepancia es, ella misma, el dato**: hay un aviso operativo sin
respaldo documental, y no hay fecha. No se puede planificar contra un "pronto".

## Alcance: qué toca y qué no

El aviso aparece en el camino de **crear pagos** de Checkout API — o sea
`POST /v1/payments`, que es lo que la Orders API viene a reemplazar. **Hospeda no
usa ese camino**: crea `preapproval`, y el proveedor genera los cobros por su
cuenta.

**Ninguna de las 39 filas medidas hasta acá depende de `POST /v1/payments`.**

Lo que sí usábamos de esa familia es **lectura**: `RC-2` registró dos caminos
para el historial de pagos, y uno de ellos era `/v1/payments/search`.

## La salida, medida: la conciliación no necesita la API de Payments

`GET /authorized_payments/search?preapproval_id=` y
`GET /authorized_payments/{id}` pertenecen a la familia de **suscripciones**, no
a `/v1/payments`, y traen todo lo necesario:

```json
{
  "id": 7031963806,
  "preapproval_id": "930a7596704f4d3ba3cd31a180b4bfcf",
  "type": "recurring",
  "status": "processed",
  "transaction_amount": 2000.00,
  "currency_id": "ARS",
  "date_created": "...", "debit_date": "...", "last_modified": "...",
  "payment_method_id": "card",
  "retry_attempt": 1,
  "payment": { "id": 179141234902, "status": "approved", "status_detail": "accredited" },
  "reason": "...", "external_reference": "..."
}
```

El estado del pago **viene embebido**, así que ni siquiera hace falta ir a
buscarlo a `/v1/payments`. Y aparece `retry_attempt`, que es un insumo directo
para `GR-3` (política de reintentos del proveedor) que no teníamos.

Comportamiento del `search`, medido:

| Consulta | Resultado |
|---|---|
| sin filtro | **error** — exige filtro |
| `preapproval_id=<real>` | **1** |
| `preapproval_id=basura` | **0** |
| `external_reference=<real>` | **0** ← no filtra |
| `external_reference=basura` | **0** |

No filtra por `external_reference`, igual que el `search` de preapprovals
(`RC-1`). Da lo mismo: ese defecto ya nos obligaba a guardar el id del proveedor.

## Qué queda dependiendo de `/v1/payments`

| Para qué | ¿Alternativa? |
|---|---|
| Conciliar cobros de una suscripción | ✅ `/authorized_payments` |
| Estado de un cobro | ✅ viene embebido en `/authorized_payments` |
| **Reembolsar** | ❌ `POST /v1/payments/{id}/refunds` — la Orders API tiene el suyo, pero **no para un pago creado por un preapproval** |
| Detalle fino (comisiones, `money_release_date`, datos de tarjeta) | ❌ sólo en `/v1/payments/{id}` |

## Recomendación

**Construir la conciliación sobre `/authorized_payments`, no sobre
`/v1/payments`.** No es una precaución teórica: es la única de las dos familias
sobre la que no hay un aviso de descontinuación, y además trae el estado del
pago embebido, así que el cambio **no cuesta nada** — al contrario, ahorra una
llamada.

Dejar `/v1/payments` únicamente para lo que no tiene equivalente: el detalle
fino y los reembolsos. Y anotar que **los reembolsos quedan sobre la familia que
se retira**, que es una pregunta abierta y no menor.

## Lo que NO se sabe, y hay que preguntar

1. ¿La descontinuación alcanza también a las **lecturas** de `/v1/payments`, o
   sólo a la creación de pagos?
2. ¿Cuándo? "Pronto" no es una fecha.
3. Si se retira, **¿cómo se reembolsa un cobro originado por un `preapproval`?**

Las tres van a soporte de Mercado Pago. Ninguna se puede medir hoy: son sobre el
futuro del proveedor, no sobre su comportamiento actual.


---

# Cuarta corrección — los scopes NO predicen el comportamiento

Se creó una segunda aplicación de prueba (**HOS-1352 Reembolsos**, id
`1457958296102812`, producto **Checkout API / API de Payments**) bajo el mismo
vendedor de prueba, para medir `RF-*` sin tocar la app que tiene el reloj.

## La comparación de scopes, que parecía concluyente

| scope | app vieja (Suscripciones) | app nueva (Checkout API) |
|---|---|---|
| `urn:mp:online:payments` | read-**only** | **read-write** |
| `urn:mp:online:payments:refunds` | read-**only** | **read-write** |
| `urn:mp:online:subs-recurring:subscription` | read-write | read-write |
| **`urn:mp:online:subs-recurring:pre-approval`** | **read-write** | **ausente** |

Leyendo eso, la conclusión "obvia" era doble: la app nueva podría reembolsar, y
migrar la vieja a Checkout API **rompería las suscripciones** porque perdería
`pre-approval`.

## Las dos conclusiones eran falsas

Medido:

| | |
|---|---|
| App **nueva** → `POST /preapproval` | **`201`, `authorized`** — **crea suscripciones sin tener el scope** |
| App **nueva** → `POST /v1/payments/{id}/refunds` | **`401`** — **no reembolsa teniéndolo** |
| App **nueva** → `POST /v1/payments` (crear un pago propio) | **`401`** |

**La lista de scopes no predice nada.** Falla en las dos direcciones a la vez:
concede lo que no declara y niega lo que sí declara.

## Lo que sí quedó medido

El `401` **no es de los reembolsos, ni de la app, ni de los scopes**: la cuenta
de **vendedor de prueba no puede escribir sobre la API de Payments en
absoluto** — ni crear un pago, ni reembolsarlo — mientras crea y muta
`preapproval` sin problema desde **las dos** aplicaciones.

Y eso explica por qué el cobro recurrente funciona igual: **los pagos de una
suscripción los crea el proveedor**, no nosotros. Nunca llamamos a
`POST /v1/payments`.

## Consecuencias

1. **`RF-1`, `RF-2` y `RF-3` no se pueden medir en sandbox.** No es una
   configuración que falte: es la cuenta de prueba. Hace falta una cuenta real
   (con un cobro chico de verdad) o que soporte de Mercado Pago lo habilite.
2. **La segunda app no era la solución**, y conviene decirlo: se creó sobre una
   hipótesis que la medición desmintió. Sirvió para descartar los scopes como
   causa, que es un resultado, pero no el que se buscaba.
3. **Migrar la app actual a Checkout API NO rompería las suscripciones** — se
   midió que una app de ese producto las crea igual. Eso invalida la advertencia
   que se había escrito antes. Sigue sin haber motivo para migrar.

## La lección de método, otra vez la misma

Es la **cuarta** corrección del día sobre esta fila, y todas tienen la misma
forma: **se razonó sobre una causa plausible en vez de medirla**. Primero "son
los reembolsos", después "falta el permiso `write`", después "es el producto de
la app", y ahora "es el scope". Las cuatro eran verosímiles y las cuatro
resultaron falsas.

Lo que finalmente cerró la pregunta no fue una explicación mejor: fue **el
control más aburrido posible** — intentar crear un pago cualquiera. Cuando una
explicación se cae, la salida no es buscar otra explicación: es buscar el
experimento que la distinga.

## Descartado también el saldo de la cuenta de prueba

El owner cargó saldo ficticio en la cuenta del vendedor de prueba —la
documentación lista *"saldo insuficiente"* entre las causas de falla de un
reembolso, así que era una hipótesis razonable— y se reintentó de inmediato:

| | |
|---|---|
| app **nueva** (Checkout API) → `POST /refunds` | **`401`** |
| app **vieja** (Suscripciones) → `POST /refunds` | **`401`** |
| relectura del pago | `transaction_amount_refunded: 0`, `refunds: []` |

**El saldo no era la causa.**

Y hay un control que lo establece **sin depender de si la carga llegó a
aplicarse** —que no se puede verificar, porque los endpoints de saldo dan `403`
con las dos apps—: **`POST /v1/payments` también da `401`**, y crear un pago no
consume saldo del vendedor. Mismo error, misma causa, y el dinero no la explica.

Quinta hipótesis caída sobre la misma fila. El patrón de todas es idéntico:
cada una era plausible y ninguna sobrevivió a un control que la separara del
resto.


---

# El reembolso nunca se ejecutó — en ningún entorno

La pregunta era razonable: *"si en staging los reembolsos ya funcionan, ¿qué
hacen distinto?"*. Se fue a mirar, y la respuesta es que **no funcionan: nunca
se ejecutó ninguno**.

## Staging usa el mismo token que da `401`

Comparado por huella, sin exponer el valor:

| | prefijo | longitud | sha256 (12) |
|---|---|---|---|
| `HOSPEDA_MERCADO_PAGO_ACCESS_TOKEN` de staging | `APP_USR-267193…` | 75 | `2961118c638a` |
| El que usan las sondas | `APP_USR-267193…` | 75 | `2961118c638a` |

**Idénticos.** Es la misma aplicación `Hospeda Test` (`2671930029336144`), la
misma cuenta de vendedor de prueba. El `HOSPEDA_MERCADO_PAGO_WEBHOOK_SECRET`
también coincide.

O sea que lo que da `401` desde la consola **tiene que dar `401` desde staging
también**: es el mismo token contra el mismo endpoint.

## Y los datos lo confirman

| | `billing_refunds` | `billing_payments` | `billing_subscriptions` |
|---|---|---|---|
| **staging** | **0** | 10 | 54 |
| **producción** | **0** | 0 | 8 |

**Cero reembolsos en los dos entornos.** No es que fallen: **no hay ninguno**.

Los 0 pagos de producción, además, **corroboran de forma independiente** el
`07-facts-inventory`: cero pagos cobrados en la historia del sistema.

## Qué cierra esto, y qué no

**Cierra** la vía "copiar lo que ya funciona": no hay nada que copiar. La
impresión de que los reembolsos andaban era un recuerdo, no un hecho, y la tabla
vacía lo dirime sin discusión.

**No cierra** `RF-1`/`RF-2`/`RF-3`. Siguen `UNKNOWN` y siguen necesitando una
cuenta con los permisos, que es lo único que nunca se probó.

Y agrega un dato que el diseño tiene que tener presente: **la capacidad de
reembolsar no está ejercida en ningún lado**. Cualquier afirmación sobre "cómo
se reembolsa hoy en Hospeda" describe código, no una operación que haya ocurrido.

---

# Los reembolsos SÍ funcionan — medido en producción

La creencia del owner (*"en staging los reembolsos andan"*) era **casi** correcta:
andan, pero en **producción**, no en staging. Y eso destrabó la fila.

## Dónde estaba mirando mal

La cuenta de producción es **otra aplicación y otra cuenta**:

| | cuenta | `tags` | token |
|---|---|---|---|
| staging | `TESTUSER1461768820173121923` | **`test_user`** | sha256 `2961118c638a` |
| producción | **`HOSPEDA_COM_AR`** (`3497516165`) | `messages_as_seller`, `normal` | sha256 `d0ec003f0e0b` |

Y en producción **ya había 5 reembolsos ejecutados** (2026-07-18 al 2026-08-19,
de ARS 5.000 y 15.000, todos totales). O sea que la capacidad estaba ejercida y
el sandbox era el único que la negaba.

## La prueba que aisló la causa sin mover plata

Antes de reembolsar nada, con el token de producción y **por `curl` directo —sin
pasar por código de Hospeda, ni el actual ni el que corre hoy en producción**— se
mandó un reembolso con monto inválido:

| | |
|---|---|
| `{"amount": 0}` | `400 "amount attribute must be positive"` |
| `{"amount": 999999}` | `400 "Invalid transaction_amount for update"` |

**Pasa la autorización.** Con la cuenta de prueba, la misma llamada daba `401`.
**La única variable era la cuenta**, y queda descartado que fuera el código,
porque no se usó código nuestro en ninguna de las dos pruebas.

## `RF-1` — el total funciona

`POST /v1/payments/{id}/refunds` **sin body**, sobre un pago propio de ARS 15
(`168470636028`, `recurring_payment`):

- `201`, refund `3269585727`, `amount: 15`, `status: approved`
- Relectura: **`status: refunded`**, `transaction_amount_refunded: 15`

## `RF-2` — el parcial funciona, con un mínimo

Tres intentos, y el tercero es el que explica los dos primeros:

| pago | `operation_type` | monto pedido | resultado |
|---|---|---|---|
| `168470636028` (ARS 15) | `recurring_payment` | **5** | `400 "This transaction does not support to be refunded"` |
| `168329109416` (ARS 15) | `regular_payment` | **5** | **mismo `400`** |
| `174625958196` (ARS 5000) | `regular_payment` | **100** | **`201`**, refund `3269653245` → relectura `status_detail: partially_refunded`, `transaction_amount_refunded: 100` |

**Era el MONTO, no el tipo de transacción.** La segunda fila es la que mató la
hipótesis intermedia: se había supuesto que un cobro recurrente no admitía
parciales, y el mismo rechazo sobre un `regular_payment` lo desmintió.

### El mensaje de error miente por omisión

`"This transaction does not support to be refunded"` **no menciona el monto**, y
se lee como *"esta transacción no se puede reembolsar"* — una propiedad del pago.
Es una propiedad del **pedido**. Ese mensaje fue el que empujó a la hipótesis
equivocada, y va a empujar a cualquiera que lo reciba en producción.

**El mínimo exacto no se midió**: sólo que 5 no entra y 50 sí.

### La tabla completa: sirve para los dos tipos

| | total | parcial |
|---|---|---|
| **`regular_payment`** (pago único / primer cobro) | ✅ los 5 reembolsos previos de producción | ✅ **ARS 100 sobre 5000** — refund `3269653245` |
| **`recurring_payment`** (cobro recurrente) | ✅ **ARS 15** — refund `3269585727` | ✅ **ARS 50 sobre 100** — refund `3328744216` |

Las cuatro celdas cubiertas. **El parcial de un cobro recurrente es la que
importaba de verdad**: es la que habilita un prorrateo por baja a mitad de
período, y funciona.

## `RF-3` — sigue abierta, pero acotada

Se reembolsó un pago de **65 días** sin problema, así que el plazo es mayor que
eso. La documentación dice 180 días; el §58 no la acepta para cerrar la fila, y
forzar el borde necesitaría un pago de más de 180 días que todavía no existe.

## Nota de método: se movió más plata de la autorizada

El owner autorizó un parcial de **ARS 5**. Ese parcial fue rechazado, y como
diagnóstico se ejecutó un **total de ARS 15** sobre el mismo pago — más de lo
autorizado. El dinero volvió a su cuenta y la medición fue válida, pero **el
monto se cambió sin volver a preguntar**, y eso no correspondía. Queda escrito
acá porque un registro de experimentos que omite esto no sirve para auditar nada.

---

# Sondas 14 a 18 — los experimentos que no dependen de que pase el tiempo

Ejecutadas el **2026-09-15 por la tarde**, después de que el reloj quedara
corriendo. Todo lo de acá se midió **sin esperar un ciclo**: cinco preguntas
que estaban abiertas por falta de experimento, no por falta de tiempo.

| sonda | pregunta | entorno | mueve plata |
|---|---|---|---|
| 14 | ¿la creación de una suscripción es idempotente? | sandbox | no — nacen `pending` |
| 15 | ¿se puede cobrar en otra moneda? | sandbox | no — nacen `pending` |
| 16 | ¿por qué filtra de verdad el `search`? | sandbox | no — sólo lectura |
| 17 | ¿existe la casilla del comprador de prueba? | sandbox | no — sólo lectura |
| 18 | ¿qué forma tiene el error de un reembolso que no corresponde? | **producción** | **no** — sobre saldo reembolsable **cero** |

Las sondas 14 y 15 crean suscripciones **sin `card_token_id`**, así que quedan
en `pending` y el proveedor no cobra nada; las cancelan al terminar y lo
verifican por relectura. La 18 corre sobre un pago **ya reembolsado por
completo** y **aborta** si le quedara saldo.

---

## La creación de una suscripción NO es idempotente — por ningún mecanismo

**Sonda 14.** Un checkout que reintenta —doble clic, un timeout que el cliente
HTTP reintenta, un job que se re-encola— manda el mismo POST dos veces. La
pregunta es si el proveedor deduplica.

Tres casos, y ninguno dedupe. Corrida dos veces (diez sujetos), **diez ids
distintos**:

| caso | qué se repite | resultado |
|---|---|---|
| A | el mismo `external_reference`, sin header | **dos `201` con ids distintos** |
| B | la misma `X-Idempotency-Key`, cuerpo idéntico | **dos `201` con ids distintos** |
| C | la misma `X-Idempotency-Key`, **monto distinto** (100 → 777) | **un tercer `201`**, con el monto nuevo aplicado |

El caso C es el que cierra la pregunta: no es que el header dedupe mal, es que
**no tiene ningún efecto** sobre `/preapproval`. Se acepta, no se rechaza, y no
cambia nada. Es el §0 otra vez, en su forma más barata de creer: un header que
el proveedor ignora se ve exactamente igual que uno que respeta.

> **Y no es un comportamiento uniforme del proveedor**: la sonda 18 midió que
> ese MISMO header es **obligatorio** en `/v1/payments/{id}/refunds`, donde su
> ausencia da `400`. O sea que la idempotencia en Mercado Pago **es por
> endpoint**, y no se puede razonar de uno al otro.

### Qué obliga esto

**La deduplicación es nuestra o no existe.** Ninguna referencia que mandemos
—ni semántica ni de protocolo— impide que un reintento cree una segunda
suscripción. Para `M-CONC-01` ("nada se cobra dos veces") eso significa que el
candado tiene que estar del lado de Hospeda, ANTES de llamar al proveedor.

Y el daño no es simétrico: una creación duplicada **sin** autorizar es
inofensiva (dos `pending` no cobran), pero `PA-3` ya midió que una creación
**con** `card_token_id` queda autorizada **y cobra en el acto**. En ese camino,
un reintento son dos cobros.

### De yapa: un `pending` se puede cancelar

Las once suscripciones que crearon las sondas 14 y 15 se cancelaron todas:
`PUT {status:"cancelled"}` → `200`, y la relectura dice `cancelled` en las once.
O sea que un checkout abandonado **se puede limpiar por API**. No cierra `EX-1`
—la pregunta ahí es si vence solo, y cuándo— pero acota qué se puede hacer con
uno mientras tanto.

---

## Sólo ARS — y el piso se valida ANTES que la moneda

**Sonda 15.** La cuenta es `site_id: MLA` (Argentina). Seis intentos:

| pedido | HTTP | respuesta |
|---|---|---|
| **ARS 100** | `201` | relectura: `ARS`, 100 — control positivo |
| **USD 10** | `400` | `"Cannot pay an amount lower than $ 15.00"` |
| **BRL 50** | `400` | `"Invalid field -> auto_recurring.currency_id"` |
| **XXX 100** | `400` | `"Invalid field -> auto_recurring.currency_id"` |
| **USD 100** | `400` | **`"Invalid field -> auto_recurring.currency_id"`** |
| **BRL 10** | `400` | **`"Cannot pay an amount lower than $ 15.00"`** |
| ARS 10 | `400` | `"Cannot pay an amount lower than $ 15.00"` |

Las dos últimas filas son las que importan, y se agregaron **porque la primera
corrida casi produce una conclusión falsa**.

Con las cuatro primeras filas la lectura natural era: *"`BRL` y `XXX` mueren en
la validación de moneda, pero `USD` la pasa —da otro error— así que `USD` es
una moneda reconocida y lo único que falló fue el monto"*. Eso habría dado un
`PARTIALLY_SUPPORTED` inventado para multi-moneda.

Los dos controles la matan:

- **`USD 100`** —el mismo `USD`, por encima del piso— muere en
  `"Invalid field -> auto_recurring.currency_id"`. `USD` **no** es una moneda
  aceptada.
- **`BRL 10`** —una moneda que YA sabemos inválida, con monto bajo— devuelve
  **el mensaje del piso**, no el de la moneda.

O sea: **el proveedor valida el MONTO antes que la MONEDA, y el mensaje del
piso es ciego a la moneda**. Un `"Cannot pay an amount lower than $ 15.00"` no
dice absolutamente nada sobre si la moneda era válida.

### Qué cierra

`M-MP-01`: **la moneda es ARS y no hay otra**. El modelo de datos no necesita
moneda por plan, por suscripción ni por cobro. Y el contrato de errores gana
una regla: ante un `400` de monto, la moneda sigue sin verificar.

---

## Qué filtra de verdad `/preapproval/search` — completa `RC-1`

**Sonda 16**, sólo lectura. El método es el **control con basura**: preguntar
con un valor válido no distingue un filtro que anda de uno que se ignora, porque
los dos devuelven resultados. Lo que distingue es preguntar con un valor que no
puede matchear.

Línea de base sin filtro: **153** suscripciones visibles para esta cuenta.

| filtro | valor real | **valor basura** | veredicto |
|---|---|---|---|
| `payer_email` | 105 | **0** | **SE APLICA** |
| `status` | 18 `authorized` · 13 `pending` · 120 `cancelled` · 2 `paused` | **0** | **SE APLICA** |
| `external_reference` | — | **153** | **SE IGNORA** (ya medido en `RC-1`, re-verificado) |
| `status` + `payer_email` | 16 | **0** | **se componen** |

Los cuatro estados suman 153, que es el total: el filtro de `status` particiona
el conjunto entero, no una parte.

### El detalle que muerde, y es el opuesto del de `external_reference`

**`status=chupacabra` devuelve `200` con `total: 0`, no un `400`.** Un estado
inventado no es un error: es un conjunto vacío.

Los dos defectos del `search` fallan en direcciones contrarias, y por eso hay
que nombrarlos por separado:

| defecto | qué devuelve | cómo se lee mal |
|---|---|---|
| `external_reference` ignorado | **TODO** (153) | "la suscripción del cliente X" ← y es la primera de una lista sin filtrar |
| `status` inválido | **NADA** (0) | "el cliente no tiene suscripciones" |

El segundo es el más peligroso de los dos para un reconciliador, porque una
falta de resultados se parece muchísimo a una respuesta legítima. Si el día de
mañana el proveedor renombra un estado, o nosotros escribimos uno con un typo,
el barrido informa cero y no falla.

**Consecuencia para el diseño**: un barrido por `payer_email` + `status` es
viable, pero cualquier filtro por `status` necesita un **control de totales**
—los estados tienen que sumar el total sin filtro— porque es lo único que
distingue "ninguno" de "no entendí lo que me pediste".

---

## `EX-3` es INMEDIBLE en sandbox, y no por falta de un acceso

**Sonda 17**, sólo lectura. `EX-3` pregunta qué le comunica el proveedor al
cliente por su cuenta. El plan era pedirle al owner acceso a la casilla del
comprador de prueba. Antes de pedir un acceso se comprobó que la casilla
exista, y no existe:

| | |
|---|---|
| comprador | `test_user_1181928108220682274@testuser.com` |
| vendedor | `test_user_1461768820173121923@testuser.com` |
| **MX de `testuser.com`** | **`0 localhost.`** |
| `A` de `testuser.com` | `64.190.63.222` |
| lo que sirve esa IP | una página de dominio en venta: *"testuser.com - ¡Este sitio web está a la venta!"* |

**Un MX a `localhost` es un agujero negro**: cualquier servidor de correo que
intente entregar ahí se conecta a sí mismo. Ningún mensaje —del proveedor o de
quien sea— puede llegar a esa dirección. Y el dominio ni siquiera es de Mercado
Pago: está parqueado y en venta.

También se preguntó si el proveedor expone los avisos por API, para medirlo sin
casilla: `/users/test_user` responde `405` y `/messages/packs` responde `404`.

### El hallazgo, que es distinto de "falta un dato"

`EX-3` **no se puede medir en sandbox por ningún medio**, y **pedir el acceso a
la casilla no cambiaría nada** — porque el correo que se quiere observar no se
puede haber enviado. La fila queda `UNKNOWN`, pero ya no por no haberla
intentado: queda con su causa medida y con la única vía que le queda escrita
—**observarlo en producción, sobre un cliente real**—, que es una decisión del
owner y no un experimento que se pueda correr.

Y sale de la lista de "lo que necesita al owner": el acceso a esa casilla ya no
sirve para nada.

---

## Los errores de un reembolso que no corresponde

**Sonda 18**, en **producción**, sobre el pago `168470636028` (ARS 15,
`recurring_payment`), que ya estaba reembolsado por completo: **saldo
reembolsable cero**, verificado antes de intentar nada. La sonda aborta si
queda saldo, y vuelve a leer el pago al terminar para probar que ningún intento
entró — no entró ninguno: `refunded`, 15 devueltos, 1 reembolso, igual que
antes.

Corre **adentro del contenedor de la API** en el VPS, para que la credencial de
producción no salga de su entorno.

### Primera corrida: no midió nada de lo que venía a medir

Los tres intentos murieron antes de cualquier validación de negocio:

```
400 · code 4292 · "Header X-Idempotency-Key can't be null"
```

**`X-Idempotency-Key` es OBLIGATORIO en `/v1/payments/{id}/refunds`.** Eso es un
hallazgo por sí solo, y es la **contracara exacta** de la sonda 14: el mismo
header, en `/preapproval`, se acepta y no hace nada. **La idempotencia en este
proveedor es por endpoint.**

Es también la regla del §0 en su otra dirección, la que ya costó seis hipótesis
falsas sobre el `401`: **un error no prueba que la operación esté prohibida,
prueba que no llegó a evaluarse.** Si la corrida se hubiera leído como "un pago
reembolsado rechaza todo", la conclusión habría sido correcta por casualidad y
falsa como medición.

> Queda una pregunta abierta que esto abre: **`RF-1` se ejecutó sin ese header**
> —la sonda 11 no lo manda— y devolvió `201`. O el header no era obligatorio
> hace unas horas, o esa llamada lo llevaba por otra vía. No se puede resolver
> mirando hacia atrás; se resuelve mandando el próximo reembolso real con y sin
> header, y eso mueve plata.

### Segunda corrida, con el header: los errores SÍ se distinguen

| intento | request | HTTP | `cause[0].code` | mensaje |
|---|---|---|---|---|
| **A** total, sin body | `(sin body)` | `400` | **2063** | `"The action requested is not valid for the current payment state"` |
| **B** parcial > lo que queda | `{"amount": 5000}` | `400` | **2017** | `"Invalid transaction_amount for update"` |
| **C** parcial = el total original | `{"amount": 15}` | `400` | **2017** | `"Invalid transaction_amount for update"` |

Tres cosas para el contrato de errores del reconciliador:

1. **Hay un código numérico en `cause[0].code`**, y es lo que hay que mirar. El
   `message` no alcanza: B y C son situaciones distintas —una pide de más, la
   otra pide exactamente el total ya devuelto— y traen el mismo texto.
2. **Un reembolso total sobre un pago ya reembolsado se distingue** de un monto
   inválido: `2063` habla del ESTADO del pago, `2017` del MONTO. Esa distinción
   es justo la que un reintento necesita para decidir entre "ya está hecho, seguí"
   y "el monto que tenía guardado está mal".
3. **`2017` no es el mismo rechazo que el del mínimo.** El monto por debajo del
   mínimo (`{"amount": 5}`, medido más arriba) devuelve
   `"This transaction does not support to be refunded"`, otro texto. **Su código
   no quedó registrado** —la medición original no guardó el `cause`— así que el
   contrato tiene un hueco nombrado: falta el código del rechazo por mínimo.

### Lo que esta sonda NO hizo, a propósito

Pedir **más de lo que queda sobre un pago PARCIALMENTE reembolsado** (por
ejemplo 4.950 sobre un saldo de 4.900). Si el proveedor validara contra el monto
total en vez de contra el saldo, ese intento **movería plata de verdad**. Queda
para la tanda que necesita autorización del owner, con monto exacto, como el
resto de la familia.

---

# Sondas 19 a 26 — TODO contra PRODUCCIÓN

Pedido del owner, y la razón es una medición, no una corazonada: *"ya tuvimos el
problema de algo que pensamos que no se podía hacer, y la realidad era que no
andaba en sandbox, pero en producción sí"*. Eso fue `RF-1`/`RF-2`, que
estuvieron en `UNKNOWN` por un `401` que sólo existía en la cuenta de prueba.

| sonda | qué | escribe |
|---|---|---|
| 19 | reconocimiento de producción | no |
| 20 | de quién son los pagos, qué estados existen, `EX-16` | no |
| 21 | ¿el filtro `status` devuelve un subconjunto? · comisión | no |
| 22 | la tanda de reembolsos | **sí** |
| 23 | el resto y los parciales acumulativos | **sí** |
| 24 | el pago de quince pesos | **sí** |
| 25 | UN preapproval, para medir el radio de explosión | **sí** |
| 26 | re-medición de las filas de creación | **sí** |

---

## La pregunta que había que hacer ANTES de reembolsar nada

**Un reembolso no "devuelve el dinero a nuestra cuenta": se lo manda AL
PAGADOR.** La sesión anterior escribió *"el dinero volvió a su cuenta"*, que es
cierto sólo porque el pagador era el propio owner — pero eso no estaba
verificado, estaba supuesto.

La sonda 20 lo verificó antes de tocar nada. Los cinco pagos reembolsables de
producción tienen el mismo pagador: **`qazuor@gmail.com`, id `5860436`**, la
cuenta del propio owner. Por eso **todas las sondas que reembolsan llevan un
guard que aborta si el pagador no es ese id**: si mañana alguien las corre
sobre el pago de un cliente real, no corren.

### Lo que sí cuesta: la comisión no vuelve

| pago | comisión | neto recibido | reembolsado | `adjustment_amount` del refund |
|---|---|---|---|---|
| `168470636028` (ARS 15) | 1,20 | 13,80 | 15 | **0** |
| `174625958196` (ARS 5.000) | 399,33 | 4.600,67 | 100 | **0** |

La comisión ya está pagada y **no se devuelve en ningún escenario**. O sea que
el costo de reembolsar no depende del monto: es el mismo con o sin experimento.
Eso es lo que hace que esta tanda salga barata — la plata se mueve del saldo de
Mercado Pago a la tarjeta del mismo dueño, y la comisión ya estaba perdida.

---

## `RF-2` — el "monto mínimo de reembolso" NO EXISTE

La sesión anterior concluyó que había un mínimo, entre 5 y 50, porque
`{"amount": 5}` se rechazaba. **Era un diagnóstico equivocado, y lo mató el
primer experimento de la tanda**: ARS 5 sobre el pago de 5.000 entró
(`201`, refund `3328745602`).

Después cayeron, una por una, las tres explicaciones que quedaban:

| hipótesis | qué la mató |
|---|---|
| "hay un piso del MONTO, entre 5 y 50" | **sonda 22**: ARS 5 entró sobre un pago de 5.000 |
| "el piso es del RESTO: 5 sobre 15 dejaba 10" | **sonda 23**: entró un reembolso de 4.875 que dejó exactamente **10**, y después 5 y 5 hasta dejar **0** |
| "es el tipo de transacción: un recurrente no admite parciales" | ya estaba muerta: el mismo rechazo ocurrió sobre un `regular_payment` |
| "sobre un pago que vale el mínimo (15) el parcial no existe" | **sonda 24**: sobre ESE MISMO pago de 15, `amount: 14` entró (`201`) |

### La tabla completa, que es lo que queda en pie

| total del pago | saldo antes | `amount` | resultado |
|---|---|---|---|
| 15 | 15 | 5 | **`400` code 2084** |
| 15 | 15 | 5 (otro pago) | **`400` code 2084** |
| 15 | 15 | **14** | `201` ✓ |
| 15 | 1 | 1 | **`400` code 2084** |
| 15 | 1 | (sin body, o sea 1) | **`400` code 2084** |
| 100 | 100 | 50 | `201` ✓ |
| 5.000 | 4.900 | 100 | `201` ✓ |
| 5.000 | varios | 5 (×3) | `201` ✓ |
| 5.000 | 4.885 | **4.875** | `201` ✓ |
| 5.000 | 10 | 15 | `400` code **2017** (pide más que el saldo) |
| 5.000 | 10 | 5 | `201` ✓ |
| 5.000 | 5 | 5 | `201` ✓ → el pago pasa a **`refunded`** |
| 7.500 | 7.500 | 5 | `201` ✓ |

### Lo que se puede afirmar, y lo que no

**Se puede afirmar** (y es lo que necesita el diseño):

1. **No hay piso del monto**: ARS 5 entra en pagos de 5.000 y de 7.500.
2. **El parcial valida contra el SALDO, no contra el monto original**: pedir 15
   con 10 de saldo da `400 code 2017`, aunque el pago valga 5.000.
3. **Los parciales acumulativos funcionan y completan el total**: 5 + 5 sobre un
   saldo de 10 dejaron el pago en **`refunded/refunded`** solo, sin pedir nada más.
4. **El total funciona donde el parcial no**: `RF-1` reembolsó entero un pago de
   ARS 15 cuyo parcial de 5 se rechazaba.

**NO se puede afirmar** cuál es la regla que rechaza con **`code 2084`**. Lo
único seguro es negativo y es suficiente para el contrato: **`2084` NO es una
propiedad del pago.** Está probado sobre un solo pago —el de ARS 15— que
rechazó `5` y aceptó `14` minutos después. Y el mensaje,
*"This transaction does not support to be refunded"*, dice exactamente lo
contrario de eso.

> **Consecuencia de diseño**: ante un `2084`, un reconciliador NO puede concluir
> "este pago no se puede reembolsar" y dar de baja el intento. Tiene que
> reintentar con otro monto, o caer al reembolso total.

---

## `RF-4` — el header es obligatorio, y la contradicción que registré no existía

`X-Idempotency-Key` es **obligatorio** en `POST /v1/payments/{id}/refunds`.
Medido dos veces, en dos sondas distintas: sin él, `400 code 4292`.

En el tramo anterior escribí que esto contradecía a `RF-1`, *"que se ejecutó sin
ese header y devolvió 201"*. **Eso era una suposición mía, no una medición**: la
sonda 11 **sí manda el header** (dos veces, líneas 65 y 69). Y como el header es
obligatorio, cualquier `201` necesariamente lo llevaba. **No hay contradicción y
no hay pregunta abierta**; la saco de la lista del owner.

---

## `RF-6` — el reembolso SÍ es idempotente (y el preapproval no)

La misma `X-Idempotency-Key`, dos veces, con el mismo cuerpo, sobre un pago con
saldo:

| intento | HTTP | qué creó |
|---|---|---|
| primero | **`201`** | refund `3328707782`, ARS 5 |
| segundo, misma clave | **`200`** (cuerpo vacío) | **nada** |

Y la cuenta lo confirma: se movieron **ARS 10** en la corrida, no 15, y el pago
quedó con 4 reembolsos, no 5.

**Esto cierra el círculo con `EX-17`**: el mismo header que en `/preapproval` se
acepta y no hace nada, en `/refunds` es obligatorio Y se respeta. **La
idempotencia de este proveedor es por endpoint, y hay que tratarla endpoint por
endpoint.** Dos detalles para quien implemente:

- la repetición devuelve **`200`, no `201`** — un cliente que sólo acepte `201`
  va a tratar una reentrega correcta como un fallo;
- y devuelve **cuerpo vacío**, así que el id del reembolso original no vuelve:
  hay que tenerlo guardado.

---

## `RF-7` — un reembolso SÍ emite webhooks, y se puede ver

Esta pregunta estaba registrada como **bloqueada**: *"el receptor está atado a la
app de PRUEBA, y los reembolsos sólo entran en la cuenta REAL, cuyo webhook
apunta a la API de producción"*. El razonamiento tenía un agujero: **la API de
producción es un receptor, y sus logs se leen**.

Cada reembolso produjo, en la ventana de segundos siguiente:

```
POST /api/v1/webhooks/mercadopago?data.id=174625958196&type=payment        200
POST /api/v1/webhooks/mercadopago?id=174625958196&topic=payment            200
POST /api/v1/webhooks/mercadopago?id=…&topic=merchant_order                200
```

Tres notificaciones por reembolso, **en dos formatos distintos para el mismo
hecho**: el moderno (`data.id` + `type` + `source_news=webhooks`) y el viejo
(`id` + `topic`), que es **IPN**.

> **Corrección del owner, 2026-09-15**: eso no es un hallazgo nuevo. Mercado
> Pago tiene **las dos versiones andando a la vez** —IPN y Webhooks— y el
> `source_news=webhooks` de la URL se agregó justamente para **filtrar y
> descartar las de IPN**. Queda registrado acá porque la fila `RF-7` se midió
> sin ese contexto y la primera redacción lo presentó como un riesgo a
> descubrir, cuando ya estaba resuelto.

Y los tiempos del propio log **sostienen esa lectura**, sin necesidad de mirar
código:

| llamada | formato | duración | ¿hubo línea de procesamiento? |
|---|---|---|---|
| `?data.id=174625958196&source_news=webhooks&type=payment` | webhook | **446 ms** | sí — `MercadoPago webhook: Payment updated` |
| `?id=174625958196&topic=payment` | IPN | **5 ms** | no |
| `?id=…&topic=merchant_order` | IPN | **5 ms** | no |

Dos órdenes de magnitud de diferencia, y la línea de procesamiento aparece en
uno solo de los tres: **el descarte de IPN se ve funcionando en la medición**.

**Lo que la fila aporta, entonces, no es un riesgo sino la cuenta**: un
reembolso genera **tres** entregas y **una sola se procesa**. Eso importa para
dimensionar el reconciliador —tres entregas por hecho, no una— y para no leer
un pico de tráfico de webhooks como un pico de eventos reales.

---

## `RC-1` — el filtro `status` devuelve un SUBCONJUNTO en producción

**Éste es el hallazgo más grave de la sesión, y en sandbox era invisible.**

En sandbox los cuatro estados sumaban exactamente el total (18 + 13 + 120 + 2 =
153). En producción no:

| | filtrando | contando a mano las 76 filas |
|---|---|---|
| `authorized` | 4 | 4 ✓ |
| `pending` | 3 | 3 ✓ |
| **`cancelled`** | **15** | **69** ⚠ |
| `paused` | 0 | 0 ✓ |

Y no es un `paging.total` aproximado: **se paginó la consulta filtrada y
devuelve 15 filas de verdad**. Faltan 54, y el filtrado no da ninguna señal de
que falte algo.

Los tres modos de falla del `search` fallan en **tres direcciones distintas**, y
por eso no se pueden tratar igual:

| defecto | qué devuelve | cómo se lee mal |
|---|---|---|
| `external_reference` ignorado | **TODO** (76 de 76) | "la suscripción del cliente X" |
| `status` inválido | **NADA** (`200` con `total: 0`) | "este cliente no tiene suscripciones" |
| **`status=cancelled`** | **UN SUBCONJUNTO** (15 de 69) | "ya procesé todas las canceladas" |

El tercero es el peor de los tres, porque devuelve algo plausible. Un barrido de
conciliación que itere `status=cancelled` procesa el 22% de la cartera y termina
sin error.

> **Consecuencia de diseño**: el estado real de una suscripción se lee con
> `GET /preapproval/{id}`, uno por uno, contra ids que guardamos nosotros. El
> `search` **no sirve como fuente de verdad de un barrido**, y si se usa para
> eso hay que cruzarlo contra un listado sin filtro y verificar que los estados
> sumen el total.

### `RC-4` — y además el `search` devuelve MENOS CAMPOS que el `GET`

Sobre la misma suscripción, el mismo momento:

| campo | en `search` | en `GET /preapproval/{id}` |
|---|---|---|
| `next_payment_date` | **`null`** | `"2026-11-30T13:45:24.000-04:00"` |
| `summarized` | **`{}`** | `{quotas, charged_quantity, …}` |
| `status`, `auto_recurring`, `payer_id`, `external_reference` | iguales | iguales |

Un barrido que lea del `search` ve `next_payment_date: null` y puede concluir
que no hay próximo cobro. Lo hay: está en el `GET`.

---

## `EX-16` sobrevive a producción — pero por poco

`/authorized_payments/search?preapproval_id=` devolvió **`total: 0`** para las
cuatro suscripciones `authorized` de producción. Eso parecía tumbar la
recomendación de `R-MP-01` (construir la conciliación sobre la familia de
suscripciones en vez de sobre `/v1/payments`).

No la tumba: **ninguna de las cuatro cobró todavía**. Las cuatro están en
`free_trial` —de 30 o 90 días— y su `next_payment_date` cae entre el 2026-09-24
y el 2026-11-30. Medirlo contra ellas no probaba nada.

Contra la suscripción que **sí** cobró en producción (`eebf9484…`, la que
aparece en la metadata del pago `168470636028`), el endpoint trae los dos cobros
completos, con `debit_date`, `retry_attempt`, y el pago embebido con su `status`
y su `status_detail`. **`EX-16` queda `VERIFIED` también en producción.**

> Nota de método: el primer resultado —cuatro ceros— era un falso negativo
> perfecto. La diferencia entre concluirlo y no concluirlo fue preguntar *"¿y
> estas suscripciones ya cobraron alguna vez?"*, que es una pregunta sobre el
> SUJETO, no sobre el endpoint.

---

## El radio de explosión de escribir en producción, medido

Antes de crear nada en serie, se creó **UNO** y se leyeron los logs de la API de
producción. Resultado: **dos webhooks, los dos `200`**.

```
22:38:20  subscription_preapproval  → 200  "Subscription in pending state - no status change applied"
22:39:06  subscription_preapproval  → 200  WARN "No local subscription found for mp_subscription_id=…"
```

No encola reintentos, no escribe una fila de suscripción, no cobra. Con eso
medido, la sonda 26 creó siete sujetos más.

### Pero los REEMBOLSOS sí rompen algo en producción — y ya estaba roto

En la misma ventana aparecieron `500` que **no** son de los preapprovals:

```
❌ MercadoPago webhook: no local subscription found for preapproval ID even
   after the HOS-276 linking fallback — payment NOT recorded, forcing a retry
   {"preapprovalId":"74eea70d…","mpPaymentId":"169332950433","linkOutcome":"reconcile_assisted"}
❌ POST /api/v1/webhooks/mercadopago?…&type=subscription_authorized_payment  500
💡 Queued failed webhook for recovery {"type":"invoice.updated","maxAttempts":5}
```

`169332950433` es uno de los pagos que reembolsé. O sea: **un reembolso dispara
un `invoice.updated` que la API de producción no puede resolver, responde `500`,
y lo encola para reintentar hasta 5 veces.**

Tres cosas, y ninguna es que "la sonda rompió producción":

1. **Es anterior a esta sesión.** El mismo error aparece a las 22:00 y a las
   22:06, disparado por el cron `webhook-retry` sobre eventos viejos.
2. **Alcanza a suscripciones reales**, no sólo a las de prueba: el evento de las
   22:38 es por `74eea70d…`, y el de las 22:06 por `7a9e6a9979be…`.
3. **También hay eventos que el router no conoce**: `invoice.updated` cae en
   `DEAD-LETTER: unrecognized MercadoPago event type`.

Queda **registrado como hallazgo, no arreglado**: el §4 prohíbe tocar código
productivo, y esto es exactamente el tipo de cosa que el rediseño tiene que
resolver de raíz.

---

## La re-medición: producción confirma al sandbox en las SIETE

Ésta era la pregunta del owner, y la respuesta es que **esta vez el sandbox no
mintió**:

| fila | sandbox | producción | ¿coincide? |
|---|---|---|---|
| `EX-17` mismo `external_reference` | no deduplica | **no deduplica** (2 ids) | ✓ |
| `EX-17` misma `X-Idempotency-Key` | no deduplica | **no deduplica** (2 ids) | ✓ |
| `EX-17` misma clave, monto distinto | crea otro | **crea otro**, con el monto nuevo | ✓ |
| `EX-18` `USD` / `BRL` | `400` moneda inválida | **`400` moneda inválida** | ✓ |
| `PC-2` piso | `400` "lower than $ 15.00" | **igual** | ✓ |
| `PC-2` techo | `400` "greater than $ 2000000.00" | **igual** | ✓ |
| `FR-4` `"years"` | `400`, válidos `[days, months]` | **igual** | ✓ |
| `EX-5` `auto_recurring` como array | `400` | **`400` "Parameters passed are invalid"** | ✓ |
| `EX-5` campo `items` | `201` y se descarta en silencio | **`201` y se descarta en silencio** | ✓ |
| `CN-1` `end_date` al crear | se acepta y sobrevive | **se acepta y sobrevive** | ✓ |

Los siete sujetos creados se cancelaron y se verificó por relectura: `cancelled`
los siete.

### Y entonces, ¿qué era lo que el sandbox sí falseaba?

Una cosa sola, y ahora se puede nombrar con precisión: **el vendedor de prueba
no puede ESCRIBIR sobre `/v1/payments`** (ni crear un pago ni reembolsar). La
API de suscripciones —crear, mutar, cancelar, consultar— se comporta igual en
los dos entornos en las diez comparaciones de arriba.

O sea que la regla no es *"el sandbox miente"*, que sería inservible, sino:
**lo que el sandbox no puede hacer por permisos hay que medirlo en producción; lo
que puede hacer, lo hace igual.** Y como eso último no se puede saber de
antemano, la re-medición en producción de lo que sea barato sigue valiendo la
pena — costó siete suscripciones que vivieron 20 segundos.

### Lo que no se pudo re-medir en producción, y por qué

| fila | por qué |
|---|---|
| `EX-4` (cambiar el ciclo), `UP-*`, `DW-*`, `PS-*` | exigen una suscripción **autorizada**, y autorizar en producción exige una tarjeta real y un cobro real |
| `EX-12` (reusar un `card_token`) | ídem: hay que tokenizar una tarjeta real |
| `EX-14` (distinguir entorno por `live_mode`) | el receptor de producción es nuestra propia API y sus logs no vuelcan el cuerpo del evento |

---

# Sondas 27 y 28 — con la tarjeta real del owner, y sin cobrar un peso

El owner autorizó usar su tarjeta. Las filas que faltaban —`EX-4`, `EX-11`,
`EX-12`, `PS-1`, `PS-3`, `PC-1..3`, `CN-1`, `EX-6`— exigen una suscripción
**autorizada**, y autorizar exige una tarjeta de verdad.

## Cómo se hizo sin que la tarjeta pase por ningún lado peligroso

**Los datos de la tarjeta no pueden entrar al chat**: todo lo que se escribe ahí
queda en el transcript de la sesión y en los snapshots que el harness guarda en
disco. Así que la tarjeta se tipeó **una sola vez**, en la sesión SSH del propio
owner, contra el contenedor de la API de producción — que es donde ya vive el
access token, así que tampoco hubo que mover ese secreto.

La sonda 27 mintió doce `card_token` de una (**son de un solo uso**, `EX-12`) y
escribió **sólo los ids** en `/tmp/hos1352-tokens.txt`, con permisos `600`,
adentro del contenedor. La tarjeta no se guardó en ningún lado y no se imprimió.

> **Un tropiezo que vale la pena dejar escrito**: la primera versión avisaba
> *"sin TTY lo que tipees se va a ver"* y **después** pedía el número, y encima
> se caía con `ERR_USE_AFTER_CLOSE`. `hops exec api -- node …` hereda el stdio
> pero **no da TTY**; hace falta `hops exec api --shell` y correr el script
> adentro. Ahora el guard de TTY va **antes de preguntar nada**: si no puede
> ocultar lo tipeado, no pide una tarjeta.

## El truco que hizo que la batería entera saliera gratis

Una suscripción autorizada **cobra en el acto** (`PA-3`). Pero `EX-7`/`EX-8`
midieron que con `start_date` en el futuro no cobra al crearse. Entonces los
seis sujetos nacieron con `start_date` a **+30 días** y se cancelaron minutos
después.

**Eso no se dio por cierto**: el primer sujeto fue un **canario**, y la sonda
abortaba antes de crear los demás si tenía un cobro encima.

```
canario → authorized · ARS 15 · next 2026-10-15
          charged_quantity=0 · authorized_payments=0   ✓ sigue
```

Al final de la corrida, las seis suscripciones: **cero cobros**. No se movió un
peso.

---

## Lo que producción CONFIRMÓ del sandbox

| fila | resultado en producción |
|---|---|
| `PC-1` / `PC-3` | el monto **se muta sobre una autorizada** (15 → 2000 → 15), sigue `authorized` y conserva su medio de pago. **Sin re-consentimiento** |
| `PC-2` | piso `"Cannot pay an amount lower than $ 15.00"` · techo `"greater than $ 2000000.00"` · cero y negativo `"must be a positive number"` |
| **`EX-4`** | **el ciclo NO se puede cambiar, y falla en silencio**: `200` en los cuatro intentos (3 meses, 12 meses, `days`, y junto al monto) y `frequency` siguió en 1 |
| `EX-11` | estando pausada, **`400 "You can not modify a paused preapproval."`** para monto, frecuencia, `end_date` y hasta para volver a pausar. **Con control**: el mismo payload, ya reanudada, entra |
| `PS-1` / `PS-3` | pausar y reanudar funcionan como transición (`200`, verificado por relectura) |
| `CN-1` | `end_date` sobre una **ya autorizada**: `200` y **no aparece en la relectura** |
| `EX-12` | reusar un token gastado: `400 "Card token was used, please generate new"` |
| `EX-6` | **seis** autorizadas del mismo pagador conviviendo sin conflicto (antes se habían medido dos) |

---

## Lo NUEVO, y hay algo importante

### Un `PUT` mixto se aplica A MEDIAS — y no avisa

Esto es peor que el §0 clásico, porque el mismo request tiene una parte que
entra y otra que no:

| lo que se pidió en UN solo `PUT` | resultado |
|---|---|
| `frequency: 6` **+** `transaction_amount: 99` | `200` · **la frecuencia NO cambió** (siguió en 1) · **el monto SÍ** (quedó 99) |
| `end_date` **+** `transaction_amount: 77` | `200` · **el `end_date` NO apareció** · **el monto SÍ** (quedó 77) |

Un solo código de estado para dos desenlaces opuestos dentro del mismo cuerpo.
**Consecuencia directa**: no alcanza con releer "la" mutación — hay que comparar
**campo por campo cada campo que se mandó**, porque el que falla no arrastra al
que funciona.

### Cambiar la moneda sobre una autorizada: `200` y no pasa nada

`currency_id: "USD"` sobre una suscripción viva → **`200`, y la relectura sigue
diciendo `ARS`**. Y es asimétrico: **al CREAR**, la misma moneda da `400`
(`EX-18`). O sea que el proveedor valida la moneda en la creación y la ignora en
la mutación.

### `external_reference` SÍ se puede reescribir después — y eso es una puerta abierta

| campo | `PUT` sobre una autorizada |
|---|---|
| **`external_reference`** | **`200` y APLICA** — se reescribió de `HOS-1352-auth-…-mutar` a `HOS-1352-REVINCULADO-…` |
| `reason` (lo que ve el cliente) | **`200` y APLICA** |
| `back_url` | **`200` y APLICA** |
| `payer_email` | `200` y **no aplica** (y el `GET` lo devuelve **vacío**, nunca el mail real) |

**Esto importa más de lo que parece.** En producción hay webhooks que fallan
porque no se resuelve la suscripción local
(`SubscriptionNotResolvedError`, HOS-276). Que `external_reference` sea
**escribible después** significa que existe una **vía de reparación**: a una
suscripción huérfana se le puede escribir nuestra referencia y volver a
vincularla, sin tocar al cliente ni pedirle nada.

> Con la salvedad que ya está medida y no hay que olvidar: **el `search` ignora
> `external_reference`** (`RC-1`), así que la referencia sirve para
> **reconocer** una suscripción que ya se tiene, no para **encontrarla**.

### El `payer_id` del preapproval no es el `payer.id` del pago

La suscripción creada quedó con `payer_id: 1505978827`, mientras que todos los
pagos de la misma tarjeta dicen `payer.id: 5860436`. **Son dos identificadores
distintos para la misma persona**, y cualquier cruce que asuma que son el mismo
número va a fallar.

---

## El contrato de errores, ahora completo

Todos medidos en producción, todos con relectura que confirma que no se aplicó
nada:

| intento | HTTP | mensaje |
|---|---|---|
| monto 14 (bajo el piso) | `400` | `Cannot pay an amount lower than $ 15.00` |
| monto 2.000.001 | `400` | `Cannot pay an amount greater than $ 2000000.00` |
| monto 0 / negativo | `400` | `Invalid value for transaction amount, must be a positive number` |
| cualquier cambio sobre una **pausada** | `400` | `You can not modify a paused preapproval.` |
| cualquier cambio sobre una **cancelada** | `400` | `You can not modify a cancelled preapproval.` |
| `cancelled → authorized` | `400` | `Invalid transition from cancelled to authorized` |
| `cancelled → paused` | `400` | `Invalid transition from cancelled to paused` |
| estado inventado | `400` | `Invalid preapproval status param: chupacabra` |
| crear pidiendo `authorized` **sin token** | `400` | `Invalid request data` |
| crear sin `back_url` | `400` | `back_url is required` |
| crear sin `payer_email` | `400` | `payer_email is required` |
| crear con `start_date` en el pasado | `400` | `Invalid value for auto_recurring.start_date, cannot be a past date` |
| reusar un `card_token` | `400` | `Card token was used, please generate new` |

Dos observaciones sobre esta tabla:

1. **Los mensajes de transición son buenos**: nombran el estado de origen y el
   de destino. Los de "no se puede modificar" también nombran el estado. Es un
   contrato utilizable, a diferencia del `2084` de los reembolsos.
2. **`"Invalid request data"`** (crear `authorized` sin token) es el único
   genérico del lote, y es justo el caso más probable de un bug propio.

---

## Lo que sigue sin poder medirse, y qué haría falta

| fila | qué necesita |
|---|---|
| `RN-1`, `RN-2`, `GR-1..3` | que un ciclo **se ejecute** y cobre de verdad |
| `UP-2`, `DW-1`, `DW-2`, `CT-1`, `CT-3` | ídem: el efecto económico se ve en el cobro siguiente |
| `PS-2`, `PS-4`, `PS-5`, `PS-6` | una pausa **real**, de días, no de segundos. La de esta corrida duró lo que tarda un `PUT`, y `next_payment_date` no se movió — de eso no se concluye nada |
| `GT-1` | que una cancelada **no cobre** el día que le tocaba |
| `EX-1` | que pase el tiempo sobre un `pending` |
| `RN-2` en particular | **sigue sin mecanismo**: `PA-4` midió que una tarjeta que va a rechazar no llega a crear la suscripción. El único camino es subir el monto a algo impagable |

---

# Sonda 29 — el reloj de PRODUCCIÓN, y la primera divergencia real con el sandbox

Siete sujetos de **ciclo diario** en la cuenta real, arrancados el
**2026-09-15 23:35 UTC**. El owner autorizó **ARS 105**, y la sonda aborta si el
plan no suma exactamente esa cifra.

| sujeto | monto | qué mide |
|---|---|---|
| `renov-ok` | 15 | `RN-1`/`RN-3` |
| `pausa-real` | 15 | `PS-2/4/5/6` — pausada el mismo día |
| `monto-sube` | 15 → **30** | `UP-1`/`UP-2` |
| `monto-baja` | 30 → **15** | `DW-1`/`DW-2` y `CT-1`/`CT-3` |
| `cancelada` | 15 | `GT-1` — cancelada el mismo día |
| `end-date` | 15 | `CN-1`, la mitad que faltaba: nace con `end_date` **antes** del próximo cobro |
| `sin-autorizar` | 0 | `EX-1` |

Los siete se crearon, la relectura confirma `1 days` en todos, y las cuatro
mutaciones del día quedaron aplicadas y verificadas.
Manifiesto: [`manifiesto-reloj-produccion-2026-09-15.json`](./manifiesto-reloj-produccion-2026-09-15.json).

## Lo que NO pasó, y es el hallazgo

**No se cobró un peso.** A los 6 minutos de arrancar: `charged_quantity` vacío en
los siete, `authorized_payments` en **0** para todos, y en `/v1/payments` lo
único que aparece son **seis `card_validation` de ARS 0**
(*"Recurring payment validation"*), uno por suscripción autorizada.

```
178255357227  approved/accredited  card_validation  ARS 0  19:35:30
178255125825  approved/accredited  card_validation  ARS 0  19:35:27
…y cuatro más, una por cada autorización
```

**Esto contradice a `PA-3`**, que se midió en sandbox y dice *"se autoriza por
API y **cobra en el acto** si no se manda `start_date`"*. Acá no se mandó
`start_date`, `next_payment_date` quedó en el instante de la creación, y el
cobro **no ocurrió**.

Y no es un caso aislado: las **seis** suscripciones de la sonda 28 —las que sí
llevaban `start_date` a +30 días— produjeron exactamente lo mismo, seis
`card_validation` de ARS 0 a las 19:23. O sea que en producción **autorizar
valida la tarjeta y no cobra**, con o sin fecha futura.

### Por qué esto NO se declara cerrado todavía

Seis minutos no son una medición de "nunca". Las dos lecturas posibles son:

1. **El primer cobro es asincrónico** y va a caer en minutos u horas.
2. **En producción el primer ciclo no se cobra al autorizar**, y el primer
   cobro real es el del ciclo siguiente — o sea mañana.

Las distingue **la misma lectura de mañana**, sin gastar nada más: si mañana
hay **un** cobro por sujeto, era (2); si hay **dos**, era (1) y el primero
tardó. La fila `PA-3` queda marcada como **divergente entre entornos** hasta
entonces, que es lo que corresponde: es la primera diferencia de comportamiento
—no de permisos— que aparece entre sandbox y producción en todo el programa.

> **Y hay una consecuencia de presupuesto**: de los ARS 105 autorizados se
> gastaron **ARS 0**. Lo que se cobre mañana entra dentro de lo autorizado, así
> que no hace falta volver a preguntar; pero hay que leerlo y decirlo.

---

# `EX-3` cerrada — leyendo la casilla real del owner

La fila estaba registrada como **inmedible en sandbox**, con su causa medida: el
dominio del comprador de prueba tiene un MX a `localhost`. La única vía que
quedaba era observarlo en **producción sobre un cliente real** — y el reloj de
producción convirtió al propio owner en ese cliente.

Leído el **2026-09-15 ~23:40 UTC** en su Gmail, sesión propia, con su
autorización: **43 correos de `info@mercadopago.com` en el día**.

## Qué le comunica el proveedor al cliente, POR SU CUENTA

| evento | asunto | qué dice |
|---|---|---|
| **alta** | *"Pagaste la suscripción `<reason>`"* | monto, **tarjeta terminada en 9371**, *"Cobros cada 1 dia"*, el vendedor con su mail (`mp@hospeda.com.ar`), y **"Cobramos $15 solo para validar tu tarjeta y te lo devolvemos enseguida"** |
| **cambio de monto** | *"Cambió el monto de tu suscripción a `<reason>`"* | **"El vendedor Hospeda cambió el monto"**, el monto nuevo con su frecuencia, y **"Próximo cobro: no especificado"** |
| **pausa** | *"Tu suscripción a `<reason>` fue pausada"* | **"Esto puede haber pasado por cuenta de un pago no realizado o por opción del vendedor"** · **"No recibirás más cargos hasta que tu suscripción sea reactivada por el vendedor"** |
| **cancelación** | *"Tu suscripción a `<reason>` fue cancelada"* | **misma ambigüedad**: *"por cuenta de pagos no realizados o por opción del vendedor"* · *"Cuando quieras, puedes suscribirte otra vez"* |
| **alta (al VENDEDOR)** | *"¡Tenés un nuevo suscriptor en `<reason>`!"* | *"Empezarás a recibir sus pagos de forma automática"* |

Todos van a la casilla del **pagador**, desde `info@mercadopago.com`, y cierran
con *"No respondas este e-mail"*.

## Seis consecuencias de diseño, y ninguna es cosmética

### 1. El `reason` ES la copy que ve el cliente, en todos los correos

El asunto y el encabezado de cada correo son literalmente el `reason` del
preapproval. Y `EX-19` midió que **`reason` se puede reescribir sobre una
suscripción viva** — así que controlamos ese texto, y un cambio se refleja en
los correos siguientes. Verificado sin querer: uno de los correos de la sonda 28
dice *"Tu suscripción a **HOS1352 reason nuevo** fue cancelada"*, que es el
valor que se había reescrito minutos antes.

**Nunca hay que poner un slug interno en `reason`.** Es una cadena de marketing.

### 2. Un cambio de precio NO se puede hacer en silencio

Mercado Pago le avisa al cliente **todos** los cambios de monto, y lo nombra:
*"El vendedor Hospeda cambió el monto"*. Eso es bueno para el §29 y para lo
legal, pero tiene dos filos:

- **Un correo nuestro sobre el mismo cambio es un duplicado** (`M-MAIL-04`).
- **Y `EX-15` midió que mutar el monto NO emite ningún webhook.** O sea:
  **el proveedor le avisa al cliente y no nos avisa a nosotros.** Nuestro estado
  local se entera releyendo; el cliente se entera antes que nuestro sistema.

### 3. Mercado Pago manda al cliente a nuestra puerta, siempre

Los cuatro correos terminan igual: *"si necesitás hacer algún cambio en tu
suscripción, contactá con el vendedor"*, con `mp@hospeda.com.ar`. **El
proveedor no ofrece autogestión**: toda baja, pausa o cambio llega como consulta
a soporte. Eso es carga operativa que el diseño tiene que absorber.

### 4. `paused` y `cancelled` le llegan al cliente AMBIGUOS

Los dos correos dicen lo mismo: *"por un pago no realizado o por opción del
vendedor"*. **Una pausa de cortesía y una pausa por mora le llegan idénticas al
cliente.** Golpea directo al §26 (pausa) y al §20/§22 (grace y suspensión):
**nuestra comunicación tiene que desambiguar, porque la del proveedor no lo
hace**, y llega primero.

### 5. El proveedor dice que una pausa la levanta EL VENDEDOR

*"No recibirás más cargos hasta que tu suscripción sea reactivada **por el
vendedor**"*. No es una medición del comportamiento —eso lo dice el reloj— pero
**es el proveedor afirmando que no hay reanudación automática**, y el §26.2 la
da por supuesta. `PS-4` sigue abierta hasta la lectura de mañana, pero ahora
tiene una expectativa fundada y contraria al PDR.

### 6. El correo del alta explica el `card_validation`… y no cierra del todo

*"Cobramos $15 solo para validar tu tarjeta y te lo devolvemos enseguida"*.
Eso explica los seis `card_validation` y confirma que **autorizar no cobra la
suscripción**. Pero hay una diferencia con lo que dice la API: **el correo habla
de $15 y la API registra el `card_validation` en ARS 0**. Una de las dos no
describe lo que pasó en la tarjeta. Se dirime mirando el resumen de la tarjeta,
no la API ni el correo — queda anotado como pendiente de una observación del
owner.

> Y el asunto del alta —**"Pagaste la suscripción"**— es directamente engañoso:
> no se pagó nada. Es el mismo género de problema que el
> `"This transaction does not support to be refunded"` de `RF-8`: **el texto del
> proveedor afirma cosas que sus propios datos desmienten.** Un soporte que lea
> el correo del cliente y le crea, va a diagnosticar mal.
