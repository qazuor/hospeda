# Coordinador — continuación de los 11 arreglos previos a la promoción

> Pegá este archivo entero como primer mensaje. **No lo commitees.**
> Sesión anterior: 2026-09-10, cortada a las 20:39 por límite de ventana.

---

## Quién sos

Sos el **coordinador** de los 11 arreglos que entran antes de promocionar
`staging` → `main`. Repartís, verificás y mergeás. Las reglas completas —tu rol, cómo
verificar, cómo mergear, las trampas del repo— se leen de:

```
.specs/HOS-1257-paridad-billing-verticales/docs/COORDINADOR.md
```

**Leelo entero antes de tocar nada.** Acá va sólo lo que ese archivo no sabe: el estado
al cierre de la sesión anterior.

El plan de los 11 está en
https://claude.ai/code/artifact/e371292a-5171-4a65-8d9a-3bb301a00b1b —
**no se tocó en toda la sesión** (el dueño pidió actualizarlo al cierre), así que está
desactualizado respecto de lo que sigue.

**Los seis issues de opencode los ejecuta el dueño.** Vos sólo esperas a que te avise que estan listos para revisar sus cambios y si estan ok se mergean, y si consideras que hace falta una revision adversarial la haces y
mergeás los PRs que lleguen. Nada más.

---

## ANTES DE CREERLE A NADA DE ESTE ARCHIVO

### ⚠️ HAY TRABAJO SIN COMMITEAR EN DOS WORKTREES — miralo ANTES de tocar nada

Se les pidió punto seguro, pero la ventana se agotó y **hubo que matarlos antes de que
llegaran a commitear**. El de HOS-1335 murió en el momento exacto de la frase
*"22 en verde; ahora verifico las otras suites, typecheck, lint y commiteo"*: **su trabajo
sobre los tres bloqueantes está en el working tree, sin commit y sin push.**

**Lo primero de tu sesión, antes que cualquier otra cosa:**

```bash
git -C /home/qazuor/projects/WEBS/hospeda-hos-1335-trial-no-puede-pagar status
git -C /home/qazuor/projects/WEBS/hospeda-hos-1322-guard-duplicado-en-primitivas status
```

Si hay cambios sin commitear, **son trabajo real y hay tests que ya pasaban en verde**.
Revisalos, commiteálos y pusheálos antes de arrancar nada nuevo. Un worktree sucio que
nadie mira es trabajo que se pierde en silencio.

**El de HOS-1322 está al revés y es un caso distinto**: murió justo en la frase
*"ahora el PR"*, o sea que **ya commiteó y pusheó** y lo único que faltó fue abrirlo.
Buscá su rama en el remoto (`git log origin/fix/hos-1322-guard-duplicado-en-primitivas -1`)
y fijate si hay commits sin PR — si los hay, el PR se abre con
`GITHUB_TOKEN= gh pr create --base staging`, título
`[HOS-1322] fix(api): <descripción>` (**scope de un solo valor, sin comas**), y recién
después CI + revisión adversarial.

### Y los SHAs

Dos agentes alcanzaron a reportar, **dos no**. Puede haber commits WIP posteriores a los
SHAs que anoto.

```bash
git fetch origin
git log origin/<cada-rama> -1 --oneline
```

**El CI de los tres PRs está verificado con `gh` por el coordinador** y los tres dan
18 checks / 17 pass / 1 SKIPPED / `CI Pass` SUCCESS / `MERGEABLE` `CLEAN`. Pero eso fue
sobre los SHAs anotados: si aparece un commit nuevo, **la verificación caduca y hay que
repetirla**. Y ninguno de los tres se mergea sólo por estar verde — los tres tienen
trabajo pendiente que el CI no ve.

---

## Estado del repo

`staging` **fe361ee89** · `main` **2832e06e0** · 2158 commits · `merge-base == main`.

**NO SE MERGEÓ NADA.** Cero PRs entraron a `staging` en toda la sesión.

---

## Los tres PRs abiertos

### PR #3344 — HOS-1335 + HOS-1236 — **NO MERGEAR, tres bloqueantes**

Rama `fix/hos-1335-trial-no-puede-pagar` · SHA verificado `2f7e129f7` ·
worktree `/home/qazuor/projects/WEBS/hospeda-hos-1335-trial-no-puede-pagar`
CI: 18 checks, 17 pass, 1 SKIPPED (`Coverage Threshold`), `CI Pass` SUCCESS ·
`MERGEABLE`/`CLEAN`.

**Verde, mergeable, y aun así frenado.** Dos revisores adversariales ciegos dijeron NO
MERGEAR y **cada uno encontró un bloqueante que el otro no vio**. Ése es el argumento
más fuerte a favor de no saltear la revisión.

Al agente se le mandaron los tres bloqueantes y **no alcanzó a reportar**: puede haber
avance sin registrar.

**B1 — confirmado por los dos revisores y verificado a mano por el coordinador.**
La UI de comercio rutea por **status**, nunca por `reason`:
`apps/web/src/components/commerce/CommercePlanChange.client.tsx:196` →
``t(`commerce.owner.planChange.error.${status}`)``, y
`packages/i18n/src/locales/es/commerce.json:483` para 409 dice
**"Tenés una cancelación pendiente — no podés cambiar de plan hasta que se resuelva."**
El PR cambia esa ruta de 502 a 409 ⇒ un dueño de gastronomía en prueba lee una
afirmación **falsa y terminal**; antes leía un genérico vago. Arreglo: leer `reason`
antes que `status`, con la cadena que `apps/web/src/lib/api-errors.ts` ya implementa.

**B2 — un solo revisor. Anclas SIN verificar por el coordinador.**
Pagar estando `trialing` colisiona con el cron de expiración, que revalida contra el
snapshot reclamado en vez de releer la fila:
`apps/api/src/services/trial.service.ts:929-945` (advisory lock que **se suelta al
commitear**), `:1056`; `apps/api/src/services/billing/trial-local-expiry.service.ts:326-330`
lee `from: subscription.status` del snapshot; el dedup de `:304` busca `TRIAL_EXPIRED`
mientras el supersede escribe `TRIAL_SUPERSEDED_BY_PAID`, así que **no lo ve**; y
`:216-233` despublica TODAS las accommodations ACTIVE del owner.
Escenario: cron reclama 02:00:00 → webhook supersede 02:00:05 → cron procesa 02:00:20 →
**despublica la cartera entera de un host que acaba de pagar**.

**B3 — el otro revisor. Anclas SIN verificar.**
La exención se aplica sobre accommodation **OR** tourist
(`apps/api/src/routes/billing/start-paid.ts:218-229`) pero el barrido compara dominio
**exacto** (`apps/api/src/services/billing/trial-supersede-on-activation.ts:147-153`):
un trial de accommodation sobrevive a una compra tourist, dos filas otorgando ~30 días.
**Dato que los revisores no tenían: el dueño pateó turista VIP, no se vende.** No es
alcanzable hoy; igual se pidió angostar la exención al mismo dominio, que es barato y
cierra la clase.

**Además, pedido en el mismo PR:** las 8 aserciones nuevas son **puramente negativas**
(`not.toBe('ALREADY_SUBSCRIBED')`, `start-paid-trial-conversion.test.ts:233,263,275,326,373,403`)
y ninguna afirma que el checkout AVANCE — un gate nuevo aguas arriba las deja verdes con
el bug de vuelta. Y dos docblocks que el PR escribió afirman de más (`start-paid.ts:38-40`
dice "the three verticals that publish" y su propio test lo desmiente en `:364-368`;
`:218-226` dice "No second row can survive this" cuando el barrido es por dominio).

### PR #3348 — HOS-1247 — **CI VERDE VERIFICADO. Falta la revisión adversarial**

Rama `fix/hos-1247-tope-comercio-por-dominio` · SHA `34f46da45` ·
worktree `/home/qazuor/projects/WEBS/hospeda-hos-1247-tope-comercio-por-dominio`
Árbol limpio, 4 commits.

**CI verificado por el coordinador con `gh`, ya cerrado**: 18 checks · 17 pass ·
1 SKIPPED (`Coverage Threshold`) · **0 pending, 0 fail** · `CI Pass` **SUCCESS** ·
`MERGEABLE` / `CLEAN`. Cumple las tres condiciones (`pending = 0`, `total >= 16`,
`CI Pass` concluido en SUCCESS).

**Lo único que le falta para mergear es la revisión adversarial: no se corrió ninguna.**
Es billing y cambió un contador que decide cupo, así que no se saltea. Cuando la corras,
apuntá a: que `countOwn` no altere el camino de alojamiento (el autor afirma que no, con
argumento: `AccommodationService` no extiende `BaseCommerceListingService`); que el mapeo
clave → dominio del endpoint de uso sea exhaustivo en las dos direcciones; y que los
tests nuevos no stubbeen el número que el sistema real no puede producir — que es el modo
de falla que el propio PR vino a corregir.

> Nota de método que vale registrar: el primer snapshot del CI mostraba **8 checks**, y
> el rollup creció a 18 a medida que los jobs se registraban. Contar temprano y cantar
> verde es exactamente el falso positivo que la regla de «≥16 + `CI Pass` concluido»
> existe para atajar.

**El hallazgo más importante de la sesión, y corrige la premisa con la que se delegó.**
La decisión del dueño fue "arreglar la raíz: el motor de límites resuelve por dominio".
Al medir, **la raíz era otra**: el 201 del alta no lo causaba el resolvedor de dominio,
lo causaba un **conteo estructuralmente cero**.

- `GastronomyService._executeCount` (`gastronomy.service.ts:550-558`) y
  `ExperienceService._executeCount` (`experience.service.ts:510-518`) **fuerzan
  `visibility: PUBLIC` + `lifecycleState: ACTIVE`** — correcto, espejan la búsqueda pública.
- Toda ficha creada por el dueño nace `PRIVATE`/`DRAFT`.
- ⇒ `count(actor,{ownerId})` devuelve 0 justo para las filas que el tope cuenta. `0 < 1`
  permite. Sin `X-Usage-Warning` porque `calculateThreshold(0,1)` es `'ok'`; sin log
  porque nunca se alcanzó el tope. **El gate corrió bien sobre un número imposible.**
- Alojamiento se salvó por un `isOwnScope` que apaga `activeOnly`
  (`accommodation.service.ts:2666`).
- **Es también la raíz de HOS-933.**

El resolvedor que **sí** asumía accommodation era el del endpoint de uso
(`routes/billing/usage.ts`, `productDomain ?? 'accommodation'`), y se arregló derivando
el dominio de la CLAVE.

**Qué cambió para alojamiento: NADA**, y está argumentado — `countOwn` es método nuevo de
`BaseCommerceListingService`, que `AccommodationService` no extiende. Verificalo igual.

**Quedó afuera deliberadamente**: `max_favorites`/`max_collections` siguen dando 404 a un
dueño sólo-comercio que no manda `?productDomain=`. Resolverlas exige el fallback
ordenado de dominios de consumidor de `loadEntitlements` — o sea **tocar el camino de
alojamiento**, y el agente paró por la restricción, como correspondía.

**Y un dato que desinfla parte del issue original**: `apps/web/src/lib/commerce/usage-badge.ts:66`
**ya manda `productDomain`**. Los 404 del issue se reprodujeron **omitiendo** el
parámetro a mano. Lo que vaciaba el badge en el producto era el mismo bug de conteo.

### PR #3345 — HOS-1337 (vino de opencode) — **revisión cortada, SIN veredicto**

Rama `fix/hos-1337-ficha-comercio-invisible` · SHA `d83b4a4de` ·
worktree `/home/qazuor/projects/WEBS/hospeda-hos-1337-ficha-comercio-invisible`
CI: 18 checks, 17 pass, 1 SKIPPED, `CI Pass` SUCCESS · `MERGEABLE`/`CLEAN` ·
5 archivos, ~610 inserciones (~91 de código real).

El revisor alcanzó a leer **sólo el código productivo, cero tests (84% del diff)**, y
cerró **sin veredicto**. Lo que dejó:

**H3 — hay que cerrarlo sí o sí antes de mergear.** `commerce-visibility.ts:104` acepta
`tx` y `:324` escribe con él; la purga (`:383`) dispara `void` inmediatamente después,
**antes del commit del caller**. El caller principal (`:380-389`) **no pasa `tx`** — ese
camino está verificado y sano. Pero hay otros tres (`commerce-downgrade-remediation.service.ts:305`,
`commerce-trial-start.service.ts`, `billing/trial-local-expiry.service.ts`) que **no
llegó a leer**. Si alguno pasa `tx`, el escenario es **peor que el bug original**: purga →
el siguiente lector re-cachea el estado VIEJO → congelado una hora más.

**H1 — reserva, no bloqueante.** El early-return idempotente (`:310-319`, estado ya
correcto → `updated:false`) sale **antes** del bloque de purga (`:341`). Una ficha ya
afectada por el bug en producción **nunca se auto-sana**: cualquier reconcile posterior
encuentra el estado correcto y no purga. Hay que esperar el `s-maxage 3600` igual.
Conviene saberlo antes de firmar el smoke.

**H2 — a chequear, no confirmado.** `apps/api/src/services/commerce-reconcile.service.ts:63,66`
usan `as unknown as CommerceEntityModel`, y el doble cast **anula** la verificación de que
`findById` devuelva `slug`. Si algún modelo acota columnas, `entity.slug` es `undefined`,
se emite un payload de purga con slug vacío y **no falla nada**.

**Verificado sin hallazgos**: no hay N+1 (`slug`/`destinationId` salen del `entity` ya
traído, `:253`); `destinationId` nulo bien manejado; cubre gastronomía **y** experiencias
en ambas direcciones (`:379`); el docblock que dice haber corregido **es cierto**.
**Divergencia real** contra las llamadas hermanas: la nueva es `void` (fire-and-forget)
mientras la de media hace `await` — deliberado y comentado (`:380-382`), pero es divergencia.

**Nadie leyó los tests de este PR.** Y `HOS-1181` toca este mismo servicio y va DESPUÉS.

---

## Los carriles sin PR

**HOS-1322** — worktree `/home/qazuor/projects/WEBS/hospeda-hos-1322-guard-duplicado-en-primitivas`,
rama `fix/hos-1322-guard-duplicado-en-primitivas`. **No alcanzó a reportar.** Revisá
`git log origin/<rama>` y el `git status` del worktree antes de asumir que no hay nada.

Lo que más vale rescatar de ahí, aunque no haya código: el veredicto sobre la
contradicción B (abajo) y qué encontró sobre `start-subscription`.

**HOS-1252** — no se arrancó. Va **después** de que HOS-1236 esté mergeado: los dos tocan
resolución de dominio.

---

## Los seis worktrees de opencode

Creados de `origin/staging` fresco, con env, instalados y buildeados. Cada prompt tiene su
path absoluto ya escrito adentro. **Linear sin tocar** en todos.

| Issue | Worktree (bajo `/home/qazuor/projects/WEBS/`) | Estado |
|---|---|---|
| HOS-1337 | `hospeda-hos-1337-ficha-comercio-invisible` | **PR #3345** |
| HOS-1338 | `hospeda-hos-1338-attach-trial-fuera-de-transaccion` | libre |
| HOS-1336 | `hospeda-hos-1336-entitlements-vacios-6h` | libre |
| HOS-1244 | `hospeda-hos-1244-medio-de-pago-403` | libre |
| HOS-867 | `hospeda-hos-867-checkout-muerto-3h` | libre |
| HOS-1181 | `hospeda-hos-1181-ficha-no-vuelve-online` | **espera a 1337** |

Prompts en `.specs/HOS-1257-paridad-billing-verticales/docs/prompts/`.

---

## Decisiones del dueño — no se re-litigan

1. **Los seis de opencode los ejecuta él.** El coordinador crea worktrees y mergea.
2. **HOS-1247 → la raíz, no el parche**, con la orden de PARAR si el cambio toca el camino
   de alojamiento. (Se cumplió: paró en `max_favorites`/`max_collections`.)
3. **HOS-1322 → se RECHAZA la segunda compra**, guard dentro de las cuatro primitivas, con
   mensaje honesto. La opción correcta —ofrecer cambio de plan— quedó como **HOS-1339**,
   fuera de esta promoción.
4. **Turista VIP está pateado**, no se vende.

**Decisión de diseño del coordinador, ya implementada en #3344**: `/start-paid` es el
camino de conversión de trial a pago; `plan-change` NO aprende a crear preapprovals —
existe para MUTAR un preapproval vivo, y un trial local que paga por primera vez es un
ALTA. Sólo deja de mentir con 502 y devuelve un 4xx ruteable.

---

## Hallazgos nuevos que merecen issue propio

1. **El mock global de `ServiceError` vuelve inaseverables contratos públicos enteros,
   y apareció DOS VECES por caminos independientes** (los carriles de 1335 y de 1247 lo
   encontraron por separado — eso mide cuánto tapa). El stub declara `(code, message)`
   donde la clase real toma `(code, message, details, reason)`: los dos últimos se
   descartan en silencio. Consecuencias medidas: `error.reason` era `undefined` en **todo
   `apps/api`**, y `error.details` también — o sea que **el criterio de verificación de
   HOS-1247 (`details.limitKey`) era inaseverable**. Los dos PRs arreglan las copias que
   tocan, pero **quedan ~85 suites con su propia copia de 2 argumentos**
   (p. ej. `test/routes/user/admin-delete.test.ts:81`,
   `test/routes/commerce/admin-moderate-listing.test.ts:73`). Nada impide que crezca.
2. **`mp_subscription_id` puede guardar CADENA VACÍA además de `NULL`** (forma de
   HOS-1326) y el mapper de qzpay lo esconde tras un chequeo de verdad. #3344 alineó dos
   sitios contra `hasNoLinkedPreapprovalCondition()`; **quedan 5 preguntando `IS NULL`**:
   `apps/api/src/cron/jobs/preapproval-less-expiry.job.ts:158,211`,
   `apps/api/src/services/billing/link-preapproval.service.ts:1126`,
   `trial-series-cohort.ts:166,248`, `payment-reconcile.service.ts:235`. El de
   `preapproval-less-expiry` deja **un trial otorgando entitlements para siempre**, sin
   error y sin log.
3. **El downgrade durante el trial responde 200 "programado" para algo que nunca ocurre.**
   `plan-change.ts:435` gatea la rama de trial con `applyImmediately && status==='trialing'`;
   un plan más barato cae en `:628` → `applyAt = trialEnd`, y
   `apply-scheduled-plan-changes.ts:238-243` **no filtra por status**, así que aplica sobre
   una fila ya expirada. Subir dice "andá a pagar"; bajar dice "listo, programado".
4. **Docblock caduco que sostiene un argumento de seguridad**:
   `subscription-checkout.service.ts:1699-1706` describe la vía anual como un
   `mode:'payment'` activado por `metadata.annualSubscriptionId`; el cuerpo crea un
   preapproval recurrente y **no estampa esa metadata en ningún lado**.
5. **La afirmación central de HOS-1236 era falsa**: `502` no está en
   `HTTP_STATUS_MESSAGE_KEY` (`packages/i18n/src/api-errors.ts:80-84`, sólo `0`,`408`,`429`),
   así que "la web traduce todo 5xx a «no pudimos conectar»" no es cierto — sólo un fetch
   fallido produce ese texto. La conclusión se sostenía; la explicación no.
6. **Un e2e con el número stubbeado puede estar verde mientras la feature no existe.**
   `listing-cap.e2e.test.ts` afirmaba el tope end-to-end stubbeando `count()` — el único
   valor que el sistema real no podía producir.

---

## DOS CONTRADICCIONES SIN DIRIMIR

**A · `POST /protected/commerce/listings/:type/:id/start-subscription`** con un dueño ya
en trial de esa vertical. Los dos revisores se contradicen:
uno dice que `start-subscription.ts:338` corta antes con `isLiveSubscriptionStatus` →
**409**; el otro dice que `:400` toma la rama de **attach** → **201
`appliedEffect:'attached'`**, sin checkout y sin cobro
(`commerce-subscription-attach.service.ts:117-120`). Probablemente son dos guards de la
misma ruta y el ORDEN decide. **Importa porque el mensaje nuevo de #3344 le dice al
usuario "andá a elegir plan y suscribite"**: si ese camino lo rechaza o le adjunta la
ficha sin cobrar, el PR promete algo que la plataforma no cumple en gastronomía y
experiencias.

**B · HOS-1322 vs HOS-1122.** 1322 afirma que "para comercio no hay cambio de plan
cruzado"; 1122 afirma que "un dueño de comercio puede subir de plan pero no bajar (422)".
Ninguna de las dos es evidencia. De esto depende qué dice el mensaje de rechazo de 1322 y
si HOS-1339 es "rutear" (angosto) o "construir" (spec propia).

---

## Linear

En **In Progress**: HOS-1335, HOS-1236, HOS-1247, HOS-1322. HOS-1337 lo movió sola la
automatización al abrirse el PR (comportamiento conocido).
**Creado**: **HOS-1339** (Backlog/Medium) — el cambio de plan de comercio, con los cuatro
vecinos linkeados y la contradicción B documentada adentro.
Nada quedó en Done.

---

## Lo próximo, y por qué ése

1. **Rescatar lo que no reportó**: `git fetch` + `git log origin/<rama> -1` en las cuatro
   ramas y `git status` en cada worktree. Interesa sobre todo HOS-1322.
2. **Cerrar los tres bloqueantes de #3344.** Es el bloqueante mayor: los dos únicos
   caminos por los que alguien en prueba puede empezar a pagar. Mientras no esté, una
   campaña trae tráfico que no puede convertir.
3. **Terminar de verificar el CI de #3348** y correrle revisión adversarial: nunca la tuvo,
   y cambió la premisa del issue.
4. **Cerrar H3 de #3345** (los tres callers sin leer) y revisar sus tests, que nadie miró.
   Destraba HOS-1181.
5. **Abrir los seis issues** de la sección de hallazgos.

**Lo que no se negocia**: los subagentes no mergean · el CI se verifica con `gh` (`hops`
no existe en los worktrees y su `127` se lee como verde) · `pending = 0` **Y**
`total >= 16` **Y** `CI Pass` SUCCESS · revisión adversarial antes de cualquier merge de
billing · no entrás a worktrees ajenos · toda medición contra `origin/staging` · el scope
del título del PR admite **un solo valor sin comas** (`fix(api,web)` falla el check).
