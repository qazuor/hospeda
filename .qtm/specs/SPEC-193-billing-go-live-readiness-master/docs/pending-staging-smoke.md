# SPEC-193 — Pending staging-smoke batch (cumulative)

> **Why this exists.** Owner decision (2026-06-04): instead of running the
> SPEC-143 staging smoke per billing PR, the MP-dependent sections are
> BATCHED and executed once at the end of the SPEC-193 billing series,
> before the staging → main promotion. Conditions:
>
> 1. `main` stays FROZEN for billing changes until this batch passes.
> 2. Every merged billing PR adds its deferred sections to this list.
> 3. Each PR documents the deferral in its own smoke-signoff doc.
>
> Reference checklists: [`SPEC-143 staging`](../../SPEC-143-billing-testing-coverage/docs/staging-smoke-checklist.md)
> · [`SPEC-143 prod`](../../SPEC-143-billing-testing-coverage/docs/prod-smoke-checklist.md)

## Accumulated sections to run (staging, MP sandbox)

| Added by | PR | Sections | Notes |
|----------|----|----------|-------|
| SPEC-192 (catalog to DB) | #1428 | 1.1, 1.2 (checkout happy paths), 1.7 (addon purchase), 1.8–1.11 (webhooks: idempotency, signature, past_due, concurrency), 1.15-A2 (photo limit — needs media infra), 2.5 (addon-expiry cron, production-like timing), 3.1 (promo apply at checkout) | MP-independent sections (1.14, 1.15-A1/A3, 3.1 read path, 3.5 admin ops) already PASSED locally — see [SPEC-192 sign-off](../../SPEC-192-billing-catalog-to-db/docs/smoke-signoff-checklist.md). Highest risk: webhook `payment-logic.ts` + cron cutovers. |
| SPEC-127 (addon checkout → qzpay) | #1448 | 1.7 (addon purchase — full flow: checkout create via qzpay, MP redirect, webhook confirm; verify metadata duals + `order_id` arrive, `external_reference` is the qzpay session UUID), 1.8 (webhook idempotency — addon paymentId dedup), 3.1 (promo apply on addon checkout incl. 100% discount → zero-amount preference acceptance), NEW: addon polling fallback (block the webhook, verify the `one_time_payment` polling job confirms the purchase within the poll window; verify webhook+polling race stays idempotent) | Billing CORE (checkout + webhook + cron paths rewritten). Intentional behavior changes to verify live: prod-first init point selection (sandbox URL must still be used in sandbox mode), removed `HOSPEDA_MERCADO_PAGO_ACCESS_TOKEN` guard. Deferral sign-off: [SPEC-192 deferred-checkout-cutover](../../SPEC-192-billing-catalog-to-db/docs/deferred-checkout-cutover.md). |
| SPEC-194 (lifecycle robustness) | #1455 | NEW: refund lifecycle (admin full refund → sub cancelled + entitlements revoked + cache cleared; partial refund → `refunded_amount` accumulates, sub stays active; accumulated partials reaching total → cancels; refund of an ADDON payment must NOT cancel the subscription; webhook-sourced refund applies the same policy), 2.x crons production-like timing: trial-expiry (lock claim + TRIAL_EXPIRED email now actually sends), dunning non-payment cancel (entitlement cache cleared — re-check access right after), scheduled plan-change apply (no double-apply on bookkeeping retry), addon-expiry (notification gap closed + Phase 7 grant reconciliation), abandoned-pending-subs (sub marked `abandoned` + user notification), NEW: annual sub pause → clear 400 PAUSE_NOT_SUPPORTED_FOR_ANNUAL, NEW: checkout return URLs respect user language | Billing CORE (refund/dunning/trial/scheduled-change rewritten on the new state-machine). ⚠️ PRE-SMOKE deploy step: `hops db-apply-extras --target=staging` (extras migration 010 canonicalizes `incomplete_expired` → `abandoned`). Adversarial review fixed 2 blockers + 4 majors pre-merge. |
| SPEC-145 (enforcement) | #1462 | 1.15 enforcement (fresh cheap-plan user: 403 ENTITLEMENT_REQUIRED on expensive-only features, 2xx on included — run the full new-403 inventory in [`spec-145-behavior-changes.md`](../../../docs/billing/spec-145-behavior-changes.md): 16 routes × wrong-plan/right-plan), staff bypass (staff actor passes every gate), admin one-off override (grant via `/admin/billing/customer-entitlements/grant` → access immediately; revoke → 403 immediately), addon limit elevation live (extra-accommodations addon → cap rises; expiry cron → cap drops with immediate effect post INV-1 fix), limit N+1 on accommodations/promotions/favorites | Enforcement layer (security-adjacent). ⚠️ USER-FACING: hosts can no longer WRITE reviews (intentional, owner 2026-06-05 — verify the web UI handles the 403 gracefully or hides the CTA); basico tiers lose promotion create (was silent limit-0). Verify ServiceError envelope (`ENTITLEMENT_REQUIRED`/`LIMIT_REACHED` codes) renders properly in web + admin clients. |
| SPEC-149 (provider error propagation + Sentry) | #1468 | NEW: provider error surfacing (force an MP sandbox error on annual checkout / addon purchase / plan-change — e.g. invalid card token or sandbox 4xx — verify the response is the MAPPED status (400/502/503/504) with `PROVIDER_*`/`VALIDATION_ERROR` code in the body, NOT a uniform 500; verify `Retry-After` header on any 503), Sentry verification (each forced error produces ONE event with `operation`/`planId`/`providerStatus` tags and NO PII), webhook mark-failed (induce a transient processing failure — verify the event row goes `failed` and the retry cron re-processes it instead of silent mark-processed), dead-letter `subscription_preapproval.created` + `subscription_authorized_payment.*` (retried payment records carry REAL customerId/subscriptionId, no duplicates on re-run), cron soft-failure Sentry (force a job `success:false` → warning-level event with `job_name` tag) | Error-handling layer over billing CORE. ⚠️ KNOWN ASYMMETRY: monthly preapproval (`subscriptions.create`) still returns uniform 500 (qzpay-core re-throws raw, not wrapped) — NOT a regression; documented in the spec scope note. Server-side retries deliberately DESCOPED (idempotency middleware doesn't cache in-flight) — pinned by 15 no-retry tests. |
| SPEC-167 (downgrade grandfather + restrict) | #1484 | NEW: downgrade restriction full cycle on staging MP sandbox — host over-cap (3 accommodations/cap 1, photos over cap, active promos over cap) schedules downgrade via plan-change → response carries `restrictionPreview` with correct counts + `PLAN_DOWNGRADE_LIMIT_WARNING` email received → cron applies at period end (or force-run) → over-cap accommodations get `plan_restricted` (public pages EXCLUDED after Cloudflare revalidation — verify the cached page actually purges, this is the ISR path that motivated §6), photos moved to archivedGallery (NOT deleted — verify count conservation in DB), promos deactivated, destination accommodationsCount updated, `PLAN_CHANGE_CONFIRMATION` email received → owner dashboard still SHOWS restricted items → re-upgrade (real MP checkout) → webhook confirms → restricted items restored up to cap + public pages revalidated. ALSO: keepSelections honored when supplied (API call with accommodationIds), admin ChangePlanDialog immediate downgrade triggers the same restriction synchronously, pause/resume now purges cached public pages (the §6 fix). | Billing CORE (plan-change flow + scheduled-change cron + admin hook + upgrade webhook all touched). ⚠️ Adversarial review fixed 1 blocker (archivedGallery survival through host edits) + 3 majors pre-merge — the photo archive/restore round-trip and the destination recount are the highest-novelty surfaces; exercise them with real data. Restriction failure recovery is MANUAL-ONLY (Sentry warning `restriction_catalog_miss` / error alerts — check Sentry during the smoke). |
| SPEC-147 (user self-service cancellation) | #1510 | ⚠️ PRE-SMOKE: set `HOSPEDA_USER_CANCEL_ENABLED=true` in Coolify for `hospeda-api-staging` (ships dark/false by default — the route returns 404 until enabled). NEW: soft-cancel full cycle on staging MP sandbox — active paid sub → `POST /api/v1/protected/billing/subscriptions/:id/cancel` (as owner) → 200, sub stays `status=active` + `cancelAtPeriodEnd=true` + `canceledAt` set, MP preapproval actually PAUSED (verify in MP sandbox dashboard — NOT cancelled, it must be resumable), `SUBSCRIPTION_CANCEL_CONFIRMED` email received with the correct access-until date, entitlements STILL load (no immediate access loss). Then: `change-plan` and re-`start-paid` on that sub → 409 `SUBSCRIPTION_CANCEL_PENDING` (cancel wins). D3 reminder: a sub with period_end ~3 days out → `SUBSCRIPTION_ACCESS_ENDING_SOON` email (cron 4:30 AM, production-like timing). Finalize: when period_end passes → finalize-cancelled-subs cron flips to `cancelled`, addons revoked, entitlements GONE, `FINALIZE_CANCELLED_SUB` event. Ownership: a NON-owner calling cancel on someone else's sub → 403/FORBIDDEN. | Billing CORE (new cancel route + service + finalize cron + webhook PAUSED-collision patch + change-plan/start-paid gate). Requires qzpay-core@1.12.0 + mercadopago@2.2.0 (B.1 cancel→provider propagation, published). ⚠️ Verify the WEBHOOK COLLISION live: after the soft-cancel, MP emits `subscription_preapproval.updated status=paused` — the sub MUST stay `active` (NOT flip to paused). Adversarial review fixed M1 (non-atomic finalize → now transactional) + M2 (past_due/trialing soft-cancelled subs now finalize too). past_due-during-grace is the highest-novelty edge — try a failed renewal on a soft-cancelled sub if feasible. |
| SPEC-148 (defensive cron-lag grace + plan-disable lifecycle) | #1515 | NEW Part A — cron-lag grace: simulate a late MP renewal (an `active` sub whose `currentPeriodEnd` is in the past because the renewal webhook was delayed) → access CONTINUES (no 503); within 6h the response carries `X-Cron-Lag-Grace-Hours-Remaining`; past 6h a Sentry `cron_lag_grace_exceeded` warning fires (check Sentry) and access STILL continues. Verify a normal not-yet-expired sub gets no header/alert. NEW Part B — plan-disable lifecycle: admin disables a plan that has ≥1 live sub on staging (toggle active→inactive) → each live sub gets `cancelAtPeriodEnd=true` + a `PLAN_DISABLED_MIGRATION` event + a `PLAN_BEING_RETIRED` retirement email (verify the email actually ARRIVES with the correct access-until date and a real recipient — the T-011 critical fix), plus one `PLAN_DISABLED_BY_ADMIN` audit row carrying `affectedSubCount`; a NEW signup / change-plan onto the disabled plan → HTTP `410 PLAN_DISABLED`; when period_end passes → the `finalize-cancelled-subs` cron flips the fanned-out subs to `cancelled` (reuses SPEC-147 cron). Verify re-enabling the plan does NOT auto-restore the cancellations (manual). | Billing CORE (entitlement middleware grace path + start-paid/plan-change 410 guard + admin plan toggle fan-out + plan-disable lifecycle service). ⚠️ Adversarial review (T-011) fixed 1 CRITICAL: `PLAN_BEING_RETIRED` was queued with an empty recipient (reached nobody) — now resolves the customer email; the smoke MUST confirm the retirement email actually arrives. The cron-lag grace is the 3rd distinct grace (alongside past_due dunning + soft-cancel) — see [`grace-period-source-of-truth.md`](../../../docs/billing/grace-period-source-of-truth.md). |

## Go-live gate (prod smoke)

Prod Flows 1–3 (annual checkout, monthly checkout, addon purchase) remain
the staging → main promotion gate, unchanged.

## Sign-off log (batch run)

| Date | Executor | Sections run | Result | Notes |
|------|----------|--------------|--------|-------|
| _pending_ | | | | |
