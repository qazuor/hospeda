{
    "version": "1.0",
    "specRef": "SPEC-277",
    "title": "Accommodation import — scraper resilience (retry, fallback chain, async, provider mitigation, manual path)",
    "created": "2026-06-24T00:00:00.000Z",
    "tasks": [
        {
            "id": "T-001",
            "title": "Add RETRYABLE_FAILURE_CODES constant and withRetry wrapper to apify-client.ts",
            "description": "Implement the R1 retry layer closest to the failure point — inside `apify-client.ts` so the orchestrator and response contract are unchanged.\n\n**What to create/modify:**\n- `packages/service-core/src/services/accommodation-import/adapters/apify-client.ts`\n  - Export `RETRYABLE_FAILURE_CODES: ReadonlySet<ImportFailureCode>` = `new Set(['source_blocked', 'timeout'])`\n  - Export `WithRetryInput<T>` interface: `{ fn: () => Promise<RunApifyActorResult>; maxRetries?: number; baseDelayMs?: number }`\n  - Export `withRetry(input: WithRetryInput): Promise<RunApifyActorResult>` — wraps one `runApifyActor` call: if `failureCode` is in `RETRYABLE_FAILURE_CODES`, sleep a jittered delay and retry; otherwise return immediately. Default `maxRetries = 2`, `baseDelayMs = 500`. Jitter: `delay = baseDelayMs + Math.random() * baseDelayMs` (uniform 0–100% jitter on the base). Hard cap: never exceed `maxRetries` attempts total (1 original + 2 retries = 3 calls max).\n  - Sleep helper: `function sleep(ms: number): Promise<void>` using `new Promise(resolve => setTimeout(resolve, ms))`.\n  - JSDoc on all exports.\n\n**Key constraints:**\n- `withRetry` does NOT import anything not already in this file.\n- Non-retryable codes (`invalid_url`, `credentials_missing`, `provider_error`, `nothing_found`) return immediately without any sleep.\n- The function never throws.\n\n**Acceptance criteria:**\n- `RETRYABLE_FAILURE_CODES` is exported and contains exactly `source_blocked` and `timeout`.\n- `withRetry` retries at most `maxRetries` times for retryable codes.\n- `withRetry` returns immediately (no sleep) for non-retryable codes.\n- `withRetry` returns the first successful result (items.length > 0 and no failureCode).\n- If all retries fail, returns the last failure result.\n\n**Testing:** See T-006.",
            "status": "completed",
            "complexity": 2,
            "blockedBy": [],
            "blocks": ["T-002", "T-003", "T-006"],
            "subtasks": [
                {
                    "title": "Export RETRYABLE_FAILURE_CODES constant",
                    "completed": true
                },
                {
                    "title": "Implement sleep helper",
                    "completed": true
                },
                {
                    "title": "Implement withRetry with jittered backoff",
                    "completed": true
                },
                {
                    "title": "Add JSDoc to all new exports",
                    "completed": true
                }
            ],
            "tags": ["backend", "service-core", "resilience", "retry"],
            "phase": "core",
            "qualityGate": {
                "lint": "passed",
                "typecheck": "passed",
                "tests": "passed"
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": "2026-06-24T21:31:59.000Z",
                "completed": "2026-06-24T21:37:43.000Z"
            }
        },
        {
            "id": "T-002",
            "title": "Wire withRetry into AirbnbAdapter.extract()",
            "description": "Replace the direct `runApifyActor(...)` call in `AirbnbAdapter.extract()` with `withRetry({ fn: () => runApifyActor(...) })` so the Airbnb path automatically retries on `source_blocked`/`timeout`.\n\n**What to modify:**\n- `packages/service-core/src/services/accommodation-import/adapters/airbnb.adapter.ts`\n  - Import `withRetry` from `./apify-client.js`.\n  - Locate the single `runApifyActor(...)` call and wrap it: `const { items, failureCode } = await withRetry({ fn: () => runApifyActor({ token, actor, actorInput, timeoutMs }) })`.\n  - No other changes to logic or interfaces.\n\n**Key constraints:**\n- The `extract()` method signature and return type (`Promise<RawExtraction>`) are unchanged.\n- The `ImportContext` shape is unchanged.\n- If `ctx.credentials.apifyToken` or `ctx.credentials.apifyAirbnbActor` is absent, the credential-missing early-return fires BEFORE `withRetry` is called (existing guard).\n\n**Acceptance criteria:**\n- Airbnb adapter now retries up to 2 times on `source_blocked` or `timeout` before returning a failure.\n- All existing airbnb adapter tests still pass.\n- The one-line diff is visible: only the `runApifyActor` call site changes.\n\n**Testing:** Covered by T-007 (adapter retry integration tests) plus the existing airbnb adapter test suite must remain green.",
            "status": "pending",
            "complexity": 2,
            "blockedBy": ["T-001"],
            "blocks": ["T-007"],
            "subtasks": [
                {
                    "title": "Import withRetry from apify-client",
                    "completed": false
                },
                {
                    "title": "Wrap runApifyActor call site in withRetry",
                    "completed": false
                },
                {
                    "title": "Verify existing adapter tests still pass",
                    "completed": false
                }
            ],
            "tags": ["backend", "service-core", "airbnb", "retry"],
            "phase": "core",
            "qualityGate": {
                "lint": null,
                "typecheck": null,
                "tests": null
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": null,
                "completed": null
            }
        },
        {
            "id": "T-003",
            "title": "Wire withRetry into BookingAdapter.extract()",
            "description": "Replace the direct `runApifyActor(...)` call in `BookingAdapter.extract()` with `withRetry({ fn: () => runApifyActor(...) })` — same pattern as T-002 for the Booking adapter.\n\n**What to modify:**\n- `packages/service-core/src/services/accommodation-import/adapters/booking.adapter.ts`\n  - Import `withRetry` from `./apify-client.js`.\n  - Locate the `runApifyActor(...)` call(s) and wrap with `withRetry`.\n  - No other changes to logic or interfaces.\n\n**Key constraints:**\n- Booking adapter has a JSON-LD→actor escalation path (if JSON-LD succeeds, actor is skipped). Only the actor call site needs the retry wrapper — do NOT wrap the JSON-LD fetch.\n- The `extract()` method signature and return type are unchanged.\n\n**Acceptance criteria:**\n- Booking adapter retries actor call up to 2 times on `source_blocked`/`timeout`.\n- JSON-LD extraction path is unaffected.\n- All existing booking adapter tests still pass.\n\n**Testing:** Covered by T-007 (adapter retry integration tests) plus the existing booking adapter test suite must remain green.",
            "status": "pending",
            "complexity": 2,
            "blockedBy": ["T-001"],
            "blocks": ["T-007"],
            "subtasks": [
                {
                    "title": "Import withRetry from apify-client",
                    "completed": false
                },
                {
                    "title": "Identify and wrap only the actor call site (not JSON-LD)",
                    "completed": false
                },
                {
                    "title": "Verify existing adapter tests still pass",
                    "completed": false
                }
            ],
            "tags": ["backend", "service-core", "booking", "retry"],
            "phase": "core",
            "qualityGate": {
                "lint": null,
                "typecheck": null,
                "tests": null
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": null,
                "completed": null
            }
        },
        {
            "id": "T-004",
            "title": "Implement runFallbackGenericExtract helper function",
            "description": "Add a private helper `_runFallbackGenericExtract(url: URL, context: ImportContext): Promise<RawExtraction | null>` (or a standalone module-private function) that invokes `GenericAdapter.extract()` against a given URL and returns the result, or `null` on any throw.\n\n**What to create/modify:**\n- `packages/service-core/src/services/accommodation-import/accommodation-import.service.ts`\n  - Add private method `_runFallbackGenericExtract(url: URL, context: ImportContext): Promise<RawExtraction | null>` to `AccommodationImportService`.\n  - The method: instantiates (or reuses) `GenericAdapter`, calls `adapter.extract(url, context)` in a try/catch, returns the result on success or `null` on any throw.\n  - This is a single cheap `safeExternalFetch` (the GenericAdapter path) — no actor call, no Apify token required.\n  - JSDoc explaining: fallback purpose, source platforms it applies to, cost contract (single fetch).\n\n**Key constraints:**\n- `GenericAdapter` is already in the adapter registry — reuse the existing instance at `this.adapters[this.adapters.length - 1]` (it is always the GenericAdapter) rather than constructing a new one.\n- The method never throws — returns `null` on any failure.\n- No changes to any public interface or the adapter registry.\n\n**Acceptance criteria:**\n- `_runFallbackGenericExtract` exists as a private method on `AccommodationImportService`.\n- It returns a `RawExtraction` when GenericAdapter succeeds.\n- It returns `null` when GenericAdapter throws.\n- It makes exactly one `safeExternalFetch` call (via the GenericAdapter) — no additional network calls.\n\n**Testing:** Covered by T-008 (fallback chain tests).",
            "status": "pending",
            "complexity": 2,
            "blockedBy": [],
            "blocks": ["T-005"],
            "subtasks": [
                {
                    "title": "Add _runFallbackGenericExtract private method",
                    "completed": false
                },
                {
                    "title": "Reuse existing GenericAdapter instance from adapter registry",
                    "completed": false
                },
                {
                    "title": "Guard with try/catch returning null on throw",
                    "completed": false
                }
            ],
            "tags": ["backend", "service-core", "fallback", "generic-adapter"],
            "phase": "core",
            "qualityGate": {
                "lint": null,
                "typecheck": null,
                "tests": null
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": null,
                "completed": null
            }
        },
        {
            "id": "T-005",
            "title": "Wire R2 fallback chain into AccommodationImportService.importFromUrl()",
            "description": "After Step 3 (primary extraction), check if the primary adapter returned `source_blocked` AND the source is Airbnb or Booking. If so, call `_runFallbackGenericExtract()` and merge its result as a partial draft.\n\n**What to modify:**\n- `packages/service-core/src/services/accommodation-import/accommodation-import.service.ts`\n  - Between Step 3 (adapter extraction) and Step 4 (mapRawToDraft), insert R2 fallback logic:\n    1. Condition: `raw.failureCode === 'source_blocked' && (source === 'airbnb' || source === 'booking')`\n    2. When true: call `await this._runFallbackGenericExtract(effectiveParsedUrl, input.context)`\n    3. If fallback returns a non-null `RawExtraction` with at least one useful field (name, summary, or imageUrls): replace `raw` with the fallback extraction but:\n       - Force `raw.failureCode = undefined` (fallback succeeded at transport level)\n       - Force `partial: true` via the existing `partial` computation path (fallback rarely has name+summary+type)\n       - Keep `sourcePlatform` as the original source (`airbnb`/`booking`) for accurate labelling — do NOT relabel as `generic`\n    4. If fallback returns null or yields zero useful fields: keep original `raw` (the `source_blocked` result); the response degrades normally.\n  - Add an inline comment explaining the R2 decision: which sources, cost guard, partial=true invariant.\n\n**Key constraints:**\n- Fallback is only attempted ONCE (no retry on the fallback itself).\n- `partial: true` on a fallback result — the existing `hasMandatoryFields` check handles this automatically since OG/JSON-LD rarely returns all three mandatory fields (name + summary + type).\n- Never relabel the `source` field on the response — it stays `airbnb`/`booking` so the host knows where the data came from.\n- The orchestrator contract (`importFromUrl` signature, `AccommodationImportResponse` shape) is unchanged.\n\n**Acceptance criteria:**\n- When Airbnb/Booking primary returns `source_blocked`, the orchestrator attempts GenericAdapter fallback.\n- A fallback result with at least name or description yields `partial: true` with real per-field source/confidence in the draft.\n- When fallback returns nothing, the response is the original `source_blocked` failure.\n- No other source (Google, MercadoLibre, Generic) triggers the fallback.\n\n**Testing:** Covered by T-008.",
            "status": "pending",
            "complexity": 3,
            "blockedBy": ["T-004"],
            "blocks": ["T-008"],
            "subtasks": [
                {
                    "title": "Add fallback trigger condition (source_blocked + airbnb/booking)",
                    "completed": false
                },
                {
                    "title": "Call _runFallbackGenericExtract and inspect result",
                    "completed": false
                },
                {
                    "title": "Merge fallback RawExtraction preserving sourcePlatform",
                    "completed": false
                },
                {
                    "title": "Add inline comment explaining R2 decision",
                    "completed": false
                }
            ],
            "tags": ["backend", "service-core", "fallback", "orchestrator"],
            "phase": "core",
            "qualityGate": {
                "lint": null,
                "typecheck": null,
                "tests": null
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": null,
                "completed": null
            }
        },
        {
            "id": "T-006",
            "title": "Write unit tests for withRetry (retry policy, fail-fast, jitter, cap)",
            "description": "Comprehensive unit tests for the `withRetry` function in `apify-client.ts`.\n\n**Test file:** `packages/service-core/test/services/accommodation-import/adapters/apify-client.test.ts` (extend existing file — add a new `describe('withRetry', ...)` block).\n\n**Test cases to cover (AAA pattern):**\n1. Retries exactly `maxRetries` times on `source_blocked` before returning the failure.\n2. Retries exactly `maxRetries` times on `timeout` before returning the failure.\n3. Does NOT retry on `invalid_url` — returns immediately after first call.\n4. Does NOT retry on `credentials_missing` — returns immediately after first call.\n5. Does NOT retry on `provider_error` — returns immediately after first call.\n6. Does NOT retry on `nothing_found` — returns immediately after first call.\n7. Returns first success (items.length > 0) without retrying remaining attempts.\n8. Defaults to `maxRetries = 2` when not specified.\n9. Respects custom `maxRetries` (e.g. 0 = no retry).\n\n**Mocking approach:**\n- Mock `sleep` or use `vi.useFakeTimers()` to keep tests fast (no real delays).\n- The `fn` parameter is a `vi.fn()` that returns controlled `RunApifyActorResult` values.\n- Count how many times `fn` is called to assert retry count.\n\n**Acceptance criteria:**\n- All 9 test cases pass.\n- Tests do not take real wall-clock time (sleep is mocked or fake timers used).\n- Total call count assertions are exact (not ≥).",
            "status": "completed",
            "complexity": 2,
            "blockedBy": ["T-001"],
            "blocks": [],
            "subtasks": [
                {
                    "title": "Set up describe block for withRetry in existing test file",
                    "completed": true
                },
                {
                    "title": "Mock sleep / use vi.useFakeTimers",
                    "completed": true
                },
                {
                    "title": "Write 9 test cases covering retry policy and fail-fast",
                    "completed": true
                }
            ],
            "tags": ["testing", "service-core", "retry", "unit-test"],
            "phase": "testing",
            "qualityGate": {
                "lint": "passed",
                "typecheck": "passed",
                "tests": "passed"
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": "2026-06-24T21:37:43.000Z",
                "completed": "2026-06-24T21:37:43.000Z"
            }
        },
        {
            "id": "T-007",
            "title": "Write adapter retry integration tests for Airbnb and Booking adapters",
            "description": "Add integration-level tests to the Airbnb and Booking adapter test files that verify `withRetry` is active and behaves correctly end-to-end through each adapter.\n\n**Test files to extend:**\n- `packages/service-core/test/services/accommodation-import/adapters/airbnb.adapter.test.ts`\n- `packages/service-core/test/services/accommodation-import/adapters/booking.adapter.test.ts`\n\n**Test cases per adapter (add as a new `describe('R1 retry integration', ...)` block):**\n1. When `runApifyActor` returns `source_blocked` twice then succeeds: adapter returns the successful extraction (verifies retry wiring is active).\n2. When `runApifyActor` returns `source_blocked` three times (exceeds cap): adapter returns `source_blocked` failure (verifies cap is enforced).\n3. When `runApifyActor` returns `credentials_missing` on the first call: adapter returns `credentials_missing` immediately without retrying.\n\n**Mocking:**\n- Use `vi.spyOn` or `vi.mock` on `runApifyActor` (or on `withRetry`) to control the sequence of return values.\n- Keep tests fast with fake timers or by mocking `sleep` inside `apify-client.ts`.\n\n**Acceptance criteria:**\n- 6 tests total (3 per adapter), all passing.\n- Tests confirm retry is wired in each adapter without testing `withRetry` itself (that's T-006).",
            "status": "pending",
            "complexity": 2,
            "blockedBy": ["T-002", "T-003"],
            "blocks": [],
            "subtasks": [
                {
                    "title": "Add R1 retry integration tests to airbnb.adapter.test.ts",
                    "completed": false
                },
                {
                    "title": "Add R1 retry integration tests to booking.adapter.test.ts",
                    "completed": false
                },
                {
                    "title": "Verify fast execution with mocked sleep/timers",
                    "completed": false
                }
            ],
            "tags": ["testing", "service-core", "airbnb", "booking", "integration-test"],
            "phase": "testing",
            "qualityGate": {
                "lint": null,
                "typecheck": null,
                "tests": null
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": null,
                "completed": null
            }
        },
        {
            "id": "T-008",
            "title": "Write service-level tests for R2 fallback chain",
            "description": "Extend the accommodation import service test file with scenarios covering the R2 fallback chain (Airbnb/Booking primary blocked → GenericAdapter fallback).\n\n**Test file:** `packages/service-core/test/services/accommodation-import/accommodation-import.service.test.ts` (add a new `describe('R2 fallback chain', ...)` block).\n\n**Test cases (AAA pattern):**\n1. Airbnb primary returns `source_blocked`; fallback Generic returns name+description via OG: response has `partial: true`, `source: 'airbnb'`, populated `draft.name`, and no `failureCode`.\n2. Booking primary returns `source_blocked`; fallback Generic returns name via OG: same assertions for Booking.\n3. Airbnb primary returns `source_blocked`; fallback Generic returns nothing (empty extraction): response has `failureCode: 'source_blocked'` (original failure propagated).\n4. Airbnb primary returns `credentials_missing` (non-retryable): fallback is NOT triggered; response has `failureCode: 'credentials_missing'`.\n5. GooglePlaces primary returns `source_blocked`: fallback is NOT triggered (only Airbnb/Booking trigger it); response has `failureCode: 'source_blocked'`.\n\n**Mocking:**\n- Mock the individual adapters (AirbnbAdapter, BookingAdapter, GenericAdapter) via `vi.spyOn` on `extract()`.\n- The service is instantiated normally; adapters are spied on after construction.\n\n**Acceptance criteria:**\n- 5 test cases, all passing.\n- Tests confirm the source-scoping rule (only Airbnb/Booking trigger fallback).\n- Tests confirm `partial: true` on a successful fallback.\n- Tests confirm fallback failure gracefully propagates original `source_blocked`.",
            "status": "pending",
            "complexity": 3,
            "blockedBy": ["T-005"],
            "blocks": [],
            "subtasks": [
                {
                    "title": "Add R2 fallback chain describe block to service test file",
                    "completed": false
                },
                {
                    "title": "Mock AirbnbAdapter.extract and GenericAdapter.extract via vi.spyOn",
                    "completed": false
                },
                {
                    "title": "Write 5 test cases covering trigger/no-trigger and success/failure",
                    "completed": false
                }
            ],
            "tags": ["testing", "service-core", "fallback", "integration-test"],
            "phase": "testing",
            "qualityGate": {
                "lint": null,
                "typecheck": null,
                "tests": null
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": null,
                "completed": null
            }
        },
        {
            "id": "T-009",
            "title": "Write ImportFromUrl.client.tsx invariant test — form stays usable after full failure",
            "description": "Add a test file for `ImportFromUrl.client.tsx` asserting R5: after a fully-failed import (all retries and fallback exhausted), the web form is 100% usable for manual entry — no spinner trap, submit button re-enabled, non-blaming error note shown.\n\n**Test file to create:** `apps/web/src/components/host/__tests__/ImportFromUrl.test.tsx`\n\n**Test cases (AAA pattern):**\n1. After a 200 response with `failureCode: 'source_blocked'`: `isSubmitting` reverts to false (submit button re-enabled), a non-blaming i18n error key is displayed (e.g. `host.importFromUrl.errors.failure.sourceBlocked`), the URL input is still editable.\n2. After a 200 response with `failureCode: 'timeout'`: same assertions.\n3. After a non-200 HTTP error: submit button re-enabled, network error message shown, URL input is still editable.\n4. While the request is in flight (`isSubmitting = true`): submit button is disabled, no error message shown.\n\n**Mocking:**\n- Mock `accommodationsImportApi.importFromUrl` via `vi.mock` or `vi.fn()`.\n- Use `@testing-library/react` with jsdom environment.\n- Mock `createTranslations` to return key-as-value.\n\n**Key assertions:**\n- `button[type=button]` (or the submit button) is NOT disabled after failure resolves.\n- No `aria-busy` or loading spinner element is present after failure resolves.\n- An error message element is visible with the correct i18n key.\n- The URL input `value` is preserved (user can edit without re-typing).\n\n**Acceptance criteria:**\n- 4 test cases pass.\n- No test uses `screen.getByRole('button', { disabled: true })` after the async failure resolves — the button must be enabled.",
            "status": "pending",
            "complexity": 2,
            "blockedBy": [],
            "blocks": [],
            "subtasks": [
                {
                    "title": "Create __tests__/ImportFromUrl.test.tsx with jsdom environment header",
                    "completed": false
                },
                {
                    "title": "Mock API and translations",
                    "completed": false
                },
                {
                    "title": "Write 4 test cases covering spinner-clear invariant",
                    "completed": false
                }
            ],
            "tags": ["testing", "web", "r5", "manual-path", "react", "unit-test"],
            "phase": "testing",
            "qualityGate": {
                "lint": null,
                "typecheck": null,
                "tests": null
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": null,
                "completed": null
            }
        },
        {
            "id": "T-010",
            "title": "Write ImportFromUrlSection.tsx invariant test — form stays usable after full failure",
            "description": "Extend `ImportFromUrlSection.test.tsx` (admin) with an R5 invariant `describe` block asserting the section returns to a fully usable state after a completely failed import.\n\n**Test file to extend:** `apps/admin/src/features/accommodations/components/__tests__/ImportFromUrlSection.test.tsx`\n\n**Test cases to add (new `describe('R5 manual path invariant', ...)` block):**\n1. After mutation returns with `failureCode: 'source_blocked'` (all retries exhausted on server): `isPending` reverts to false, an error alert is shown, the URL input is editable, and `setFieldValue` is NOT called (no partial prefill on classified failure).\n2. After mutation returns with `failureCode: 'timeout'`: same assertions.\n3. After mutation throws (network error): `isPending` reverts to false, error alert shown, inputs editable.\n\n**Key assertions:**\n- `currentMutationState.isPending` is `false` after the failure resolves (using the existing mock pattern in this test file).\n- An error `role='alert'` element is present.\n- No `LoaderIcon` or spinner element is visible.\n- `mockSetFieldValue` is not called (no form prefill on classified failure).\n\n**Acceptance criteria:**\n- 3 new test cases pass alongside the existing 7 tests.\n- Tests follow the existing mock setup pattern in this file (`currentMutationState`, `mockMutateAsync`, etc.).",
            "status": "pending",
            "complexity": 2,
            "blockedBy": [],
            "blocks": [],
            "subtasks": [
                {
                    "title": "Add R5 invariant describe block to ImportFromUrlSection.test.tsx",
                    "completed": false
                },
                {
                    "title": "Write 3 test cases reusing existing mock infrastructure",
                    "completed": false
                },
                {
                    "title": "Assert isPending=false, error visible, setFieldValue not called",
                    "completed": false
                }
            ],
            "tags": ["testing", "admin", "r5", "manual-path", "react", "unit-test"],
            "phase": "testing",
            "qualityGate": {
                "lint": null,
                "typecheck": null,
                "tests": null
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": null,
                "completed": null
            }
        },
        {
            "id": "T-011",
            "title": "Write provider-mitigation runbook (R4)",
            "description": "Create the R4 runbook documenting how to swap a blocked actor or route through a residential proxy without a code deploy, using the existing `ctx.credentials.apify*Actor` config fields.\n\n**File to create:** `packages/service-core/src/services/accommodation-import/docs/provider-mitigation-runbook.md`\n\n**Content to cover:**\n1. **Overview:** Why provider mitigation is config-only (actors are already configurable via `ctx.credentials`; no code change needed).\n2. **Config fields:** Table listing `apifyAirbnbActor`, `apifyBookingActor`, `apifyGooglePlacesActor` (and any other `apify*Actor` fields present in `ImportContext`), their env var names, current defaults, and which adapters read them.\n3. **Runbook — swapping an actor:** Step-by-step for an ops person:\n   a. Identify the blocked actor and alternative (e.g. `tri_angle/airbnb-rooms-urls-scraper` → `dtrungtin/airbnb-scraper`).\n   b. Locate the env var on Coolify (or `hops env-set api HOSPEDA_APIFY_AIRBNB_ACTOR <new-slug>`).\n   c. Trigger a redeploy.\n   d. Verify with a test import from the admin panel.\n4. **Runbook — residential proxy:** Note that the Apify actor input accepts a `proxyConfiguration` key; document how to set it if the actor supports it (reference to Apify docs for the specific actor).\n5. **Config seam audit result:** State whether all adapter `apify*Actor` fields are correctly sourced from `ImportContext.credentials` (confirming no code seam is missing, or flagging one if found during implementation).\n\n**Acceptance criteria:**\n- File exists and covers all 5 content areas.\n- No new code is written — this is documentation only.\n- If a config seam is found missing during the audit (step 5), file a note in this doc AND create a follow-up GitHub issue (do NOT silently skip it).",
            "status": "pending",
            "complexity": 1,
            "blockedBy": [],
            "blocks": [],
            "subtasks": [
                {
                    "title": "Create docs/ directory in accommodation-import service",
                    "completed": false
                },
                {
                    "title": "Write runbook covering actor swap and proxy steps",
                    "completed": false
                },
                {
                    "title": "Document all apify*Actor config fields and their env vars",
                    "completed": false
                },
                {
                    "title": "Record config seam audit result",
                    "completed": false
                }
            ],
            "tags": ["docs", "r4", "provider-mitigation", "runbook", "ops"],
            "phase": "docs",
            "qualityGate": {
                "lint": null,
                "typecheck": null,
                "tests": null
            },
            "timestamps": {
                "created": "2026-06-24T00:00:00.000Z",
                "started": null,
                "completed": null
            }
        }
    ],
    "summary": {
        "total": 11,
        "pending": 9,
        "inProgress": 0,
        "completed": 2,
        "blocked": 0,
        "averageComplexity": 2.2
    }
}
