#!/usr/bin/env bun
// Standalone alias for `hops run`. Everything funnels through the same
// dispatcher, so this binary and the sub-command cannot drift apart.
import { runCommand } from '../src/index.ts';

process.exit(await runCommand({ name: 'run', argv: process.argv.slice(2) }));
