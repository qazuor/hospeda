#!/usr/bin/env bun
// Standalone alias for `hops servers-down`. Everything funnels through the same
// dispatcher, so this binary and the sub-command cannot drift apart.
import { runCommand } from '../src/index.ts';

process.exit(await runCommand({ name: 'servers-down', argv: process.argv.slice(2) }));
