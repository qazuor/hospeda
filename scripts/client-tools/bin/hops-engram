#!/usr/bin/env bun
// Standalone alias for `hops engram`; all policy lives in the shared command.
import { runCommand } from '../src/index.ts';

process.exit(await runCommand({ name: 'engram', argv: process.argv.slice(2) }));
