#!/usr/bin/env bun
// Standalone alias for `hops db-seed`. Everything funnels through the same
// dispatcher, so this binary and the sub-command cannot drift apart.
import { runCommand } from '../src/index.ts';

process.exit(await runCommand({ name: 'db-seed', argv: process.argv.slice(2) }));
