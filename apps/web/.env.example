# Environment variables for apps/web

# Node environment
NODE_ENV=development

# Database connection (used by server-side services via @repo/db)
# Example: postgres://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URL=

# Public site URL (used in metadata, redirects, etc.)
# Example: https://hospeda.local or http://localhost:4321
PUBLIC_SITE_URL=

# Clerk (authentication)
# Frontend publishable key (exposed to client)
PUBLIC_CLERK_PUBLISHABLE_KEY=
# Backend secret key (server only)
CLERK_SECRET_KEY=


