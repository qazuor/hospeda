/**
 * @file content.config.ts
 * @description Astro Content Collections for the web app.
 *
 * No content collections are currently defined. The private beta tester
 * documentation site (previously served under `/beta/*` via the `beta`
 * collection) was retired (HOS-116).
 */

export const collections = {};
