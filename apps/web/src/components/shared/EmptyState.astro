---
import { AlertTriangleIcon } from '@repo/icons';

interface Props {
    title?: string;
    message?: string;
    type?: 'accommodations' | 'destinations' | 'events' | 'posts';
}

const {
    title = 'No se encontraron resultados',
    message = 'Lo sentimos, no pudimos encontrar lo que estás buscando.',
    type = 'accommodations',
} = Astro.props;

// Icons are no longer needed as we use a generic AlertTriangleIcon
---

<div class='flex flex-col items-center justify-center py-16 text-center'>
    <div class='bg-gray-100 dark:bg-gray-800 rounded-full p-8 mb-6'>
        <AlertTriangleIcon className='h-16 w-16 text-gray-400' />
    </div>
    <h2 class='text-2xl font-bold mb-2'>{title}</h2>
    <p class='text-gray-600 dark:text-gray-400 mb-8 max-w-lg mx-auto'>{message}</p>
</div>
