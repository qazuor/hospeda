---
import { Waves } from 'lucide-react';

interface Props {
    class?: string;
    size?: number | string;
    color?: string;
    strokeWidth?: number;
}

const { class: className, size, color, strokeWidth } = Astro.props as Props;
---

<Waves className={className} size={size} color={color} strokeWidth={strokeWidth} />
