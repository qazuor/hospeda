---
/**
 * @file presentacion/gastronomia/index.astro
 * @description Commercial presentation for the gastronomy vertical (HOS-978).
 * A verbatim migration of the owner-approved artifact "Presentación
 * Gastronomía"; the copy and the stylesheet below are the artifact's own and
 * must not be rewritten here — the artifact is the source of truth for both.
 *
 * WHY IT IS NOT THE `/publicar-restaurante/` LANDING. That landing sells and
 * is optimized to rank. This page is what the owner sends personally to a
 * prospective restaurant over WhatsApp, or dictates by phone, and it carries
 * things a public landing must not: negotiable pricing, comparisons against
 * competitors, and operational detail. That is why `noindex, nofollow` is a
 * requirement here rather than a precaution, and why the page is deliberately
 * excluded from the sitemap (see `lib/seo-config.ts` → SITEMAP_EXCLUDED_PATHS
 * and the exclusion list in `lib/seo/static-sitemap-pages.ts`).
 *
 * WHY IT IS NOT TRANSLATED. The audience is local (owner decision, HOS-978).
 * The page lives under `[lang]/` anyway so the short, dictable URL works:
 * `hospeda.com.ar/presentacion/gastronomia` is 301-redirected to
 * `/es/presentacion/gastronomia/` by the middleware's locale step, which means
 * no middleware exemption was needed to get the dictable slug. It therefore
 * serves the same Spanish copy under `/en/` and `/pt/` BY DESIGN — unlike
 * HOS-908, where that was a defect.
 *
 * WHY `StandaloneLayout`. The page owns 100% of its chrome, exactly like the
 * `/funcionalidades/` brochure (HOS-119) it follows. No Header, no Footer, no
 * site nav — it must look like the artifact it came from.
 *
 * WHY THE STYLESHEET IS A PLAIN `<style>` BLOCK. Astro extracts it to a real
 * stylesheet. An `is:inline` block would be dropped in production: the CSP
 * omits `'unsafe-inline'` from `style-src` outside dev (see
 * `lib/middleware-helpers.ts`), so the page would render unstyled in prod while
 * looking perfect locally. Do not add `is:inline` here.
 *
 * THE "NOT FOR SALE YET" CLAUSE IS GONE (HOS-975). Until this change several
 * bullets ended with "que hoy no está a la venta: el único plan que se puede
 * comprar ahora es el Básico". HOS-975 priced the six commerce tiers and
 * activated the three that were still dark, so ALL of them — Básico,
 * Profesional and Premium, in both verticals — are on sale, and the sentence
 * became a claim the page could no longer make honestly.
 *
 * Only the clause was removed, and only because the merge made it false;
 * nothing was rewritten. The bullets still name their tier ("va en el escalón
 * Premium"), which is what a reader needs and is still true. The commercial
 * rewrite of this section — the prices themselves, which tier to push — stays
 * an owner decision and is deliberately NOT attempted here, on the same
 * reasoning HOS-1041 used when it declined to touch the sentence at all: this
 * page's copy is the owner-approved artifact verbatim (see above).
 *
 * That earlier deferral is worth keeping in view. HOS-895 PR2 had already made
 * the clause false (it activated `gastronomy-pro` for sale) and HOS-1119 made
 * that tier reachable from checkout, so the page under-sold its own paid tiers
 * for two releases before anyone deleted one sentence.
 *
 * THE PRICE BLOCK IS NO LONGER A NUMBER (HOS-1134). "El costo" used to say
 * "está en el orden de los quince mil pesos por mes" — half of what
 * `gastronomy-basico` actually costs ($30.000) once HOS-975 priced the six
 * tiers. Hardcoded prose goes stale the moment the base repriced and nobody
 * remembered to edit this artifact; the base itself does not. The owner's
 * decision (2026-09-03) was to remove the number altogether and link to
 * `/{lang}/publicar-restaurante/`, which reads the real price from
 * `fetchPublicPlans({ domain: 'gastronomy' })` — not `/{lang}/planes/`,
 * which does not exist, and not the per-vertical `/precios/` page from
 * HOS-1032, which does not exist either. This is now the SECOND documented
 * exception to "verbatim artifact" after the HOS-975 clause removal above:
 * the price paragraph was rewritten (owner-approved text, transcribed
 * verbatim) and one bullet was added for HOS-1043's multi-language carta,
 * which the artifact predates and had no bullet for at all.
 *
 * @route /{lang}/presentacion/gastronomia/
 * @rendering SSR + Cloudflare cache (matches funcionalidades/nosotros)
 */

import StandaloneLayout from '@/layouts/StandaloneLayout.astro';
import SEOHead from '@/components/seo/SEOHead.astro';

import '@/styles/presentacion.css';
import PresentationThemeToggle from '@/components/presentacion/PresentationThemeToggle.astro';
import type { SupportedLocale } from '@/lib/i18n';
import { getBrandPhoneLocalDisplay } from '@/lib/brand-phone';
import { getSiteUrl } from '@/lib/env';
import { buildUrl } from '@/lib/urls';

import { CACHE_TAG_SITE_CONFIG } from '@repo/cache-tags';
import { applyCacheHeaders } from '@/lib/cache/response-cache';

const locale = Astro.locals.locale as SupportedLocale;
// Brand contact phone, single source of truth (HOS-364) — this page's
// copy is the owner-approved artifact verbatim; only the phone digits are
// parameterized so a real number change does not require re-editing prose.
const brandPhone = getBrandPhoneLocalDisplay();

// --- Edge cacheability (HOS-369 W2-2) ---
// No session and no API call: the content is hardcoded copy, so it only
// changes on deploy. Same class and tag as the `/funcionalidades/` brochure.
applyCacheHeaders({
	locals: Astro.locals,
	headers: Astro.response.headers,
	cacheable: true,
	cacheClass: 'static',
	tags: [CACHE_TAG_SITE_CONFIG]
});

const canonical = `${getSiteUrl().replace(/\/$/, '')}/${locale}/presentacion/gastronomia/`;
---

<StandaloneLayout locale={locale}>
	<SEOHead
		slot="head"
		title="Presentación Gastronomía"
		description="Tu restaurante donde se planea el viaje. Qué es Hospeda para tu restaurante, bar, parrilla o café: cómo publicás, qué mostrás y cuánto cuesta."
		canonical={canonical}
		locale={locale}
		noindex
		nofollow
	/>
	<link
		slot="head"
		rel="stylesheet"
		href="https://fonts.googleapis.com/css2?family=Archivo:wght@600;700;800&family=IBM+Plex+Mono:wght@500;600&family=Source+Sans+3:wght@400;600&display=swap"
	/>

<PresentationThemeToggle />

<div class="wrap">

  <header class="masthead">
    <p class="eyebrow">Hospeda &middot; Gastronomía</p>
    <h1>Tu restaurante donde se planea el viaje</h1>
    <div class="rule"></div>
    <p class="masthead__lede">
      El turista que viene al Litoral primero busca dónde dormir, y en la misma vuelta decide
      dónde va a comer. Hospeda es el portal donde hace las dos cosas. Tu restaurante, bar,
      parrilla o café tiene su propia página, la cargás vos, y el que quiere reservar mesa
      <strong>te llama directo</strong>.
    </p>
    <div class="factsheet">
      <div class="fact">
        <span class="fact__k">Comisión</span>
        <span class="fact__v fact__v--free">Ninguna</span>
      </div>
      <div class="fact">
        <span class="fact__k">Prueba</span>
        <span class="fact__v">30 días</span>
      </div>
      <div class="fact">
        <span class="fact__k">Cómo se entra</span>
        <span class="fact__v">Solo, en el momento</span>
      </div>
    </div>
  </header>

  <section>
    <div class="sec-head">
      <p class="eyebrow">Lo primero, sin vueltas</p>
      <h2>Qué es y qué no es</h2>
    </div>
    <div class="notice">
      <h3>No es una app de pedidos ni de reservas de mesa.</h3>
      <p>
        No tomamos pedidos, no cobramos delivery y <strong>no nos llevamos un porcentaje de lo
        que facturás</strong>. Nadie reserva una mesa desde acá.
      </p>
      <p>
        Es <strong>tu página</strong> dentro del portal donde el turista está armando el viaje:
        con tus fotos, tu carta, tus horarios y tu teléfono. Lo que hace es que te encuentren y
        te llamen. Se paga una cuota fija por mes, y punto.
      </p>
    </div>
  </section>

  <section>
    <div class="sec-head">
      <p class="eyebrow">Paso a paso</p>
      <h2>Cómo publicás</h2>
    </div>
    <ol class="steps">
      <li>
        <span class="num">01</span>
        <div class="step__body">
          <h3>Creás tu cuenta y armás la ficha</h3>
          <p>
            No hay que esperar que nadie te apruebe ni que te llamen: entrás, creás la cuenta y
            cargás tu local vos mismo, en el momento.
          </p>
          <p class="step__meta">Gratis &middot; No se pide tarjeta &middot; Se hace desde el celular</p>
        </div>
      </li>
      <li>
        <span class="num">02</span>
        <div class="step__body">
          <h3>La completás con tiempo</h3>
          <p>
            Queda guardada y sin que nadie la vea hasta que vos digas. Cargás las fotos, la
            descripción, los horarios de cada día, el rango de precios, tu carta, el teléfono y
            las redes.
          </p>
          <p class="step__meta">Para publicar hacen falta: nombre, resumen, descripción, ciudad, tipo de local, foto principal, un contacto, horarios y rango de precios</p>
        </div>
      </li>
      <li>
        <span class="num">03</span>
        <div class="step__body">
          <h3>Le das publicar</h3>
          <p>
            Ahí elegís el plan y cargás la tarjeta, y arrancan los <strong>30 días de prueba</strong>.
            Tu página sale al aire en ese momento. Si en el mes decidís que no, cancelás y no se
            te cobra nada.
          </p>
          <p class="step__meta">Se paga con Mercado Pago &middot; El primer cobro es al día 31</p>
        </div>
      </li>
    </ol>
  </section>

  <section>
    <div class="sec-head">
      <p class="eyebrow">El costo</p>
      <h2>Una cuota fija, sin sorpresas</h2>
    </div>
    <p>
      Una cuota fija por mes, con tres escalones según cuánto quieras mostrar. Es lo mismo si
      te llaman dos personas o doscientas: no hay comisión ni cargo por consulta.
    </p>
    <p class="subhead">
      Los valores de cada escalón están siempre publicados en
      <a href={buildUrl({ locale, path: 'publicar-restaurante' })}>el sitio</a>.
    </p>
    <p class="subhead">
      Con eso tenés <strong>una ficha publicada</strong>. Si manejás más de un local, se suma
      una ficha extra por el mismo valor.
    </p>
  </section>

  <section>
    <div class="sec-head">
      <p class="eyebrow">Tu página</p>
      <h2>Qué podés mostrar</h2>
    </div>
    <ul class="plain">
      <li><span class="tick">&check;</span><span>Fotos del local y de los platos, con una principal destacada.</span></li>
      <li><span class="tick">&check;</span><span>Descripción con formato, y videos si tenés.</span></li>
      <li><span class="tick">&check;</span><span><strong>Horarios día por día</strong>, con más de un turno si cerrás al mediodía.</span></li>
      <li><span class="tick">&check;</span><span>El <strong>enlace a tu carta</strong> y el rango de precios, para que el que llega sepa a qué atenerse. El enlace está en <strong>todos los planes</strong>, incluido el Básico.</span></li>
      <li><span class="tick">&check;</span><span><strong>La carta cargada plato por plato</strong>: secciones (entradas, principales, postres), cada plato con su nombre, su descripción y su precio — o &laquo;a consultar&raquo;, que también es un precio honesto. Se lee en tu página sin que nadie tenga que abrir un archivo, y Google la lee igual. Si preferís, subís una <strong>foto o un PDF</strong> del menú impreso y listo. Las dos formas van <strong>desde el plan Profesional</strong>.</span></li>
      <li><span class="tick tick--no">!</span><span><strong>Una foto por plato</strong>, al lado del plato en la carta — no una galería suelta donde nadie sabe cuál milanesa es cuál. La subís desde la fila del plato mientras cargás la carta. <strong>Va en el escalón Premium</strong>.</span></li>
      <li><span class="tick tick--no">!</span><span><strong>La carta traducida al inglés y al portugués</strong>, con un selector de idioma en tu página para que el turista la lea en el suyo. Se arma sola a partir de la carta que ya cargaste, plato por plato. <strong>Va en el escalón Premium</strong>.</span></li>
      <li><span class="tick">&check;</span><span><strong>Lo que ofrece el local</strong>, tildado de una lista: si aceptás tarjeta, si hacés delivery o take away, si tomás reservas, si hay estacionamiento, terraza, wifi, música en vivo, si se puede ir con perro o si <strong>aceptás eventos</strong> (cumpleaños, empresas, casamientos) — con un botón de contacto directo en tu ficha.</span></li>
      <li><span class="tick">&check;</span><span><strong>Los aptos de tu carta</strong>: sin TACC, vegano, vegetariano, sin lactosa, sin frutos secos. Se tildan igual que el resto, aparecen en tu página y <strong>el que busca puede filtrar por ellos</strong> antes de elegir dónde comer. Están en todos los planes, desde el más básico: no le vamos a cobrar a nadie por avisar algo que a otro le puede hacer daño no saber.</span></li>
      <li><span class="tick">&check;</span><span><strong>El tipo de cocina</strong>: argentina, de río, italiana, peruana, internacional, asiática, mediterránea o de fusión — se tilda todo lo que corresponda, porque un mismo local puede ser parrilla y cocina de río a la vez. Aparece en tu página y sirve para que te encuentre el que ya sabe qué se le antoja comer.</span></li>
      <li><span class="tick">&check;</span><span>Teléfono, WhatsApp, email, redes y tu ubicación en el mapa.</span></li>
      <li><span class="tick">&check;</span><span><strong>Preguntas frecuentes propias</strong>: si aceptás tarjeta, si hay estacionamiento, si se puede ir con perro, o cualquier cosa que te pregunten seguido por teléfono.</span></li>
      <li><span class="tick">&check;</span><span>Tu página en español, inglés y portugués.</span></li>
      <li><span class="tick tick--no">!</span><span><strong>El menú del día, con fecha de vencimiento</strong>: cargás el plato del día o la sugerencia del chef, le ponés hasta cuándo vale, y cuando esa fecha pasa <strong>deja de mostrarse solo</strong>. Nadie tiene que acordarse de sacarlo — que es justamente lo que hoy no pasa, y por eso las fichas terminan anunciando el pescado del martes pasado. <strong>Va en el escalón Profesional.</strong></span></li>
      <li><span class="tick tick--no">!</span><span><strong>Tu ficha en PDF, lista para imprimir</strong>: la foto, los horarios, el contacto y un QR que devuelve a tu página. Para el folleto del mostrador del hotel, el volante de la cabaña o la pila de la oficina de turismo — el único lugar donde tu ficha funciona sin internet. Ya está construida, pero <strong>va en el escalón Premium</strong>.</span></li>
      <li><span class="tick">&check;</span><span><strong>La agenda de tu local</strong>: la noche de música en vivo, el happy hour, la cena show, la promo de los martes. Se cargan con fecha, o «todos los jueves de 18 a 20» si es algo que se repite todas las semanas, y aparecen en tu página. Podés apagar uno sin borrarlo — el show de invierno vuelve el año que viene y no hace falta volver a escribirlo. <strong>Va desde el escalón Profesional.</strong></span></li>
      <li><span class="tick tick--no">!</span><span><strong>Un chat con IA en tu ficha</strong>: el que entra pregunta lo que quiera —¡a qué hora abren, si hay opción sin TACC, cuánto sale una milanesa— y contesta con lo que vos cargaste: tus horarios, tu carta, tus preguntas frecuentes. No inventa: si algo no está cargado, lo dice y manda a preguntarte a vos. <strong>Va en el escalón Premium.</strong></span></li>
    </ul>
    <p class="subhead">
      Tu página vive en <strong>hospeda.com.ar/es/gastronomia/tu-local</strong>, entra en el mapa
      del sitio y está escrita para que Google la encuentre.
    </p>
  </section>

  <section>
    <div class="sec-head">
      <p class="eyebrow">Quién manda sobre qué</p>
      <h2>Lo que cambiás solo y lo que no</h2>
    </div>
    <div class="tablewrap">
      <table>
        <thead>
          <tr><th scope="col">Dato</th><th scope="col">Quién lo cambia</th></tr>
        </thead>
        <tbody>
          <tr><td>Fotos, descripción, horarios, carta, rango de precios, lo que ofrece el local, contacto y redes</td><td><strong>Vos, cuando quieras</strong></td></tr>
          <tr><td>Tus preguntas frecuentes</td><td><strong>Vos</strong></td></tr>
          <tr><td>Nombre del local, tipo (restaurante, bar, café&hellip;) y ciudad</td><td>El equipo. Escribinos y lo cambiamos.</td></tr>
        </tbody>
      </table>
    </div>
  </section>

  <section>
    <div class="sec-head">
      <p class="eyebrow">Reseñas</p>
      <h2>Cómo funcionan, para que no te agarre de sorpresa</h2>
    </div>
    <ul class="plain">
      <li><span class="tick">&check;</span><span>Las puede dejar cualquier persona con cuenta en Hospeda. Una por persona.</span></li>
      <li><span class="tick">&check;</span><span><strong>Ninguna se publica sola:</strong> las revisamos antes de que aparezcan. Eso frena al que escribe caliente o al que nunca pisó tu local.</span></li>
      <li><span class="tick tick--no">&times;</span><span><strong>Todavía no podés responder una reseña.</strong> Está pendiente. Mientras tanto, si aparece una que te parece injusta, escribinos y la miramos.</span></li>
    </ul>
  </section>

  <section>
    <div class="sec-head">
      <p class="eyebrow">Para ser justos</p>
      <h2>Lo que conviene que sepas antes</h2>
    </div>
    <ul class="plain">
      <li><span class="tick tick--no">!</span><span><strong>Las estadísticas todavía son básicas.</strong> Ya podés ver cuántas visitas tuvo tu página, en total y por día. Lo más fino —qué plato mira más la gente, cuántos escanean tu QR— está en la lista, pero hoy no existe y preferimos decírtelo.</span></li>
      <li><span class="tick tick--no">!</span><span><strong>La tarjeta se pide para publicar</strong>, antes de los 30 días de prueba. Crear la cuenta y armar la ficha no cuesta nada; publicarla exige dejarla cargada.</span></li>
      <li><span class="tick tick--no">!</span><span>Si dejás de pagar, tu página <strong>deja de verse pero no se borra</strong>: fotos, textos y reseñas quedan, y vuelve a estar en línea cuando reactivás.</span></li>
      <li><span class="tick tick--no">!</span><span>Una ficha incompleta no se publica. Sin foto principal, horarios y rango de precios, el sistema no te deja — porque una página a medias no la elige nadie.</span></li>
    </ul>
  </section>

  <footer class="close">
    <h2>Probalo</h2>
    <p>
      Armar la ficha es gratis y no te compromete. Recién decidís cuando la querés publicar, y aun
      ahí tenés 30 días para arrepentirte sin que se te cobre un peso.
    </p>
    <a class="cta" href="https://hospeda.com.ar/es/publicar-restaurante">hospeda.com.ar/es/publicar-restaurante</a>
    <div class="phone">
      <span class="fact__k">¿Preferís que te acompañemos?</span>
      <strong>Llamanos o escribinos por WhatsApp al {brandPhone}</strong>
      <span class="finelines">Te ayudamos a cargar la ficha la primera vez, si querés.</span>
    </div>
    <div class="finelines">
      Hospeda cobra una suscripción por tener la ficha publicada, nunca un porcentaje de lo que
      facturás, y no interviene entre el comensal y tu local. Los valores y las condiciones son
      los vigentes al día de hoy y pueden cambiar; los del momento están siempre publicados en el sitio.
    </div>
  </footer>

</div>
</StandaloneLayout>

