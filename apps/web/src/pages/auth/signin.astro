---
import { SignInFormWrapper } from '../../components/auth/SignInFormWrapper';
import AuthLayout from '../../layout/AuthLayout.astro';

const title = 'Iniciar Sesión - Hospeda';
const description = 'Inicia sesión en tu cuenta de Hospeda para acceder a todas las funcionalidades.';
---

<AuthLayout title={title} description={description}>
    <main class='min-h-screen bg-gray-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8'>
        <div class='sm:mx-auto sm:w-full sm:max-w-md'>
            <div class='flex justify-center'>
                <a href='/' class='flex items-center space-x-2'>
                    <div
                        class='w-8 h-8 bg-gradient-to-r from-cyan-500 to-green-500 rounded-lg flex items-center justify-center'
                    >
                        <span class='text-white font-bold text-lg'>H</span>
                    </div>
                    <span class='text-2xl font-bold text-gray-900'>Hospeda</span>
                </a>
            </div>
            <h2 class='mt-6 text-center text-3xl font-bold tracking-tight text-gray-900'>Iniciar sesión</h2>
            <p class='mt-2 text-center text-sm text-gray-600'>
                ¿No tienes cuenta?{' '}
                <a href='/auth/signup/' class='font-medium text-cyan-600 hover:text-cyan-500'> Regístrate aquí </a>
            </p>
        </div>

        <div class='mt-8 sm:mx-auto sm:w-full sm:max-w-md'>
            <div class='bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10'>
                <SignInFormWrapper
                    client:load
                    apiBaseUrl={import.meta.env.PUBLIC_API_URL || 'http://localhost:3001'}
                    redirectTo='/'
                />
            </div>
        </div>

        <div class='mt-8 text-center'>
            <a href='/' class='text-sm text-gray-500 hover:text-gray-700'> ← Volver al inicio </a>
        </div>
    </main>
</AuthLayout>
