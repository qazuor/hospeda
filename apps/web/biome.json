{
    "root": false,
    "$schema": "https://biomejs.dev/schemas/2.5.2/schema.json",
    "extends": ["../../packages/biome-config/biome.json"],
    "files": {
        "includes": ["!**/a11y-baseline.json"]
    },
    "css": {
        "parser": {
            "cssModules": true
        }
    },
    "linter": {
        "rules": {
            "nursery": {},
            "style": {
                "noRestrictedImports": {
                    "level": "error",
                    "options": {
                        "paths": {
                            "@repo/media/server": "Server-only module. The web app is browser-first (Astro SSR + islands) and must not import the Cloudinary SDK, file-magic validation, or runtime environment resolution. Use the browser-safe entrypoint `@repo/media` (getMediaUrl, MEDIA_PRESETS, extractPublicId, generateGalleryId) instead."
                        }
                    }
                }
            }
        }
    },
    "overrides": []
}
