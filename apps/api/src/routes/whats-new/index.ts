/**
 * What's New route barrel.
 *
 * Re-exports all What's New route bundles for registration in `routes/index.ts`.
 *
 * @see SPEC-175
 */
export { protectedWhatsNewRoutes } from './protected/index.js';
