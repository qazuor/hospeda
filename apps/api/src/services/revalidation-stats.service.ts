/**
 * Re-export shim for backward compatibility.
 * The canonical implementation lives in @repo/service-core.
 *
 * @module services/revalidation-stats
 */
export { RevalidationStatsService } from '@repo/service-core';
