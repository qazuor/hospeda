/**
 * Re-export shim for backward compatibility.
 * The canonical source has moved to @repo/service-core.
 *
 * @module services/promo-code-defaults
 * @deprecated Import from '@repo/service-core' instead.
 */
export { ensureDefaultPromoCodes, getDefaultPromoCodeConfigs } from '@repo/service-core';
