# =============================================================================
# apps/admin — Test Environment Variables
# =============================================================================
#
# Loaded automatically by Vitest when NODE_ENV=test.
# Never commit real secrets here — all values are safe test-only placeholders.
# =============================================================================

NODE_ENV=test

# Core URLs
VITE_API_URL=http://localhost:3001
VITE_BETTER_AUTH_URL=http://localhost:3001/api/auth

# Application identity
VITE_APP_NAME=Hospeda Admin Test
