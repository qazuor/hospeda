{
    "root": false,
    "$schema": "https://biomejs.dev/schemas/2.5.2/schema.json",
    "extends": ["../../packages/biome-config/biome.json"],
    "files": {
        "includes": ["!**/docs/development/examples", "!**/docs/examples"]
    },
    "linter": {
        "rules": {
            "nursery": {},
            "style": {
                "noRestrictedImports": {
                    "level": "error",
                    "options": {
                        "paths": {
                            "@repo/media/server": "Server-only module. The admin app is browser-side and must not import the Cloudinary SDK, file-magic validation, or runtime environment resolution. Use the browser-safe entrypoint `@repo/media` (getMediaUrl, MEDIA_PRESETS, extractPublicId, generateGalleryId) instead."
                        }
                    }
                }
            }
        }
    }
}
