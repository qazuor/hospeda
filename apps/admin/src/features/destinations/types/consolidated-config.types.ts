// Re-export from accommodations since the types are the same
export type {
    ConfigMode,
    ConsolidatedEntityConfig,
    ConsolidatedFieldConfig,
    ConsolidatedSectionConfig,
    ModeSpecificFieldConfig,
    SectionFilterOptions
} from '../../accommodations/types/consolidated-config.types';
