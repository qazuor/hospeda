/**
 * SectionAccordion — multi-open accordion shell for entity view / edit pages.
 *
 * Re-exports the container and the item so consumers can import both from one
 * path:
 *
 * @example
 * ```ts
 * import { SectionAccordion, SectionAccordionItem } from '@/components/entity-form/accordion';
 * ```
 */
export type { SectionAccordionItemProps, SectionAccordionProps } from './SectionAccordion';
export { SectionAccordion, SectionAccordionItem } from './SectionAccordion';
