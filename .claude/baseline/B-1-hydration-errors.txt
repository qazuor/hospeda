SPEC-099 B-1 — Hydration mismatch baseline capture
====================================================

Captured: 2026-05-07T21:17 (against MAIN dev server at http://localhost:4321/)
Locale/viewport: /pt/ at 375x812

----- Source code locations of the two mismatches -----

1) FeedbackFAB.tsx:317-318  (portal ternary)
   `typeof document !== 'undefined' ? createPortal(modalContent, document.body) : modalContent`
   - SSR returns `modalContent` (form rendered inline as child of FAB wrapper).
   - First client render returns `createPortal(...)` → the React subtree that
     was a sibling on the server is now reparented into document.body. React
     sees a structural mismatch in that subtree.

2) FeedbackModal.tsx:89-92  (isMobile lazy initializer)
   `useState<boolean>(() => typeof window === 'undefined' ? false : window.innerWidth < MOBILE_BREAKPOINT)`
   - SSR: isMobile = false → drag handle is NOT rendered (line 255: `{isMobile && (...)}`).
   - Client first render at 375px wide: isMobile = true → drag handle IS rendered.
   - This is the visible diff that React reports below.

----- Observed runtime error (first error in console) -----

Error: Hydration failed because the server rendered HTML didn't match the client.
As a result this tree will be regenerated on the client. This can happen if a
SSR-ed Client Component used:

- A server/client branch `if (typeof window !== 'undefined')`.
- Variable input such as `Date.now()` or `Math.random()` which changes each time it's called.
- Date formatting in a user's locale which doesn't match the server.
- External changing data without sending a snapshot of it along with the HTML.
- Invalid HTML tag nesting.

  <FeedbackFABClient data-astro-transition-persist="feedback-fab" apiUrl="http://loc..." ...>
    <FeedbackFAB apiUrl="http://loc..." appSource="web" ...>
      <div>
      <FeedbackModal isOpen={false} onClose={function} formProps={{...}}>
        <dialog ref={{current:null}} aria-labelledby="feedback-m..." ...>
          <div ref={{current:null}} className="content" tabIndex={-1} data-testid="feedback-m...">
            <span>
+           <div className="dragHandle" aria-hidden="true">
-           <button type="button" className="closeBtn" aria-label="Cerrar" data-testid="feedback-modal-close">
            ...

    at throwOnHydrationMismatch (@astrojs/react/client.js)

----- Total console messages on /pt/ at 375x812 -----
errors: 1, warnings: 0  (the hydration error above)


====================================================
SPEC-099 B-1 — Post-fix verification (T-008 / T-009)
====================================================

Captured: 2026-05-07T21:40-21:41 (against worktree dev server at http://localhost:4322/)
Build: @repo/feedback rebuilt with mounted-flag and isMobile=false fixes.

Sweep matrix (3 locales x 2 viewports):

| Locale | Viewport   | Status                                                            |
|--------|------------|-------------------------------------------------------------------|
| /es/   | 375x812    | OK — no hydration warning                                          |
| /en/   | 375x812    | OK — no hydration warning                                          |
| /pt/   | 375x812    | OK — no hydration warning, page renders (h1 = "Sua escapada...")   |
| /es/   | 1280x800   | OK — no hydration warning                                          |
| /en/   | 1280x800   | OK — no hydration warning                                          |
| /pt/   | 1280x800   | OK — no hydration warning                                          |

Remaining console errors on every page:
- CORS preflight blocked on http://localhost:3001/api/v1/public/auth/me
  (worktree origin http://localhost:4322 not in API trusted-origins).
  This is NOT a B-1 regression — same CORS policy would also fail on a
  fresh install of the main port-mismatched setup. The user's API server
  was started with HOSPEDA_SITE_URL=http://localhost:4321; we use :4322
  for the worktree. Production / main repo on :4321 is unaffected.

T-008 outcome: PT mobile blank-page issue resolved by the structural fix
alone. No missing PT translation key was needed; the previous blank was
caused by React regenerating the FeedbackFAB subtree on hydration mismatch
which racially raced with first paint and produced an empty viewport.

T-009 outcome: zero React hydration warnings across all 6 locale/viewport
combinations. Acceptance criteria met.
